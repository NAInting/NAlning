{
  "name": "edu-ai-platform",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "ci": "pnpm lint && pnpm typecheck && pnpm test && pnpm e2e && pnpm build",
    "e2e": "playwright test",
    "scan:project-health": "node scripts/project-health-scan.mjs",
    "scan:project-health:docs": "node scripts/project-health-scan.mjs --include-docs",
    "scan:project-health:history": "node scripts/project-health-scan.mjs --include-docs --include-history",
    "validate:knowledge-graph": "node scripts/validate-knowledge-graph.mjs content/units/math-g8-s1-linear-function-concept/knowledge-graph.seed.json",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "lint": "turbo lint",
    "clean": "turbo clean"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "turbo": "^2.6.1",
    "typescript": "^5.9.3"
  }
}
