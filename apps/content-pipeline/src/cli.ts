import { readFile, writeFile } from "node:fs/promises";
import { pathToFileURL } from "node:url";

import type { LlmGateway } from "@edu-ai/llm-gateway";

import { buildUnitReviewInboxDeliveryPlan, validateUnitReviewInboxDeliveryPlan } from "./inbox-delivery-plan";
import {
  buildUnitReviewInboxDeliveryFollowUp,
  validateUnitReviewInboxDeliveryFollowUpSourceContract,
  validateUnitReviewInboxDeliveryFollowUpSource,
  validateUnitReviewInboxDeliveryFollowUp
} from "./inbox-delivery-follow-up";
import {
  buildUnitReviewInboxDeliveryFollowUpPlan,
  validateUnitReviewInboxDeliveryFollowUpPlan
} from "./inbox-delivery-follow-up-plan";
import {
  buildUnitReviewInboxDeliveryFollowUpReceipt,
  validateUnitReviewInboxDeliveryFollowUpReceipt
} from "./inbox-delivery-follow-up-receipt";
import {
  buildUnitReviewInboxDeliveryFollowUpReconciliation,
  validateUnitReviewInboxDeliveryFollowUpReconciliation
} from "./inbox-delivery-follow-up-reconciliation";
import {
  buildUnitReviewInboxDeliveryReconciliation,
  validateUnitReviewInboxDeliveryReconciliation
} from "./inbox-delivery-reconciliation";
import {
  buildUnitReviewInboxDeliveryReceipt,
  type UnitReviewInboxDeliveryOperationStatus,
  validateUnitReviewInboxDeliveryReceipt
} from "./inbox-delivery-receipt";
import { buildUnitReviewInboxHandoff } from "./inbox-handoff";
import {
  recordUnitReviewProviderExecutionAttempt,
  validateUnitReviewProviderExecutionAttempt,
  validateUnitReviewProviderExecutionAttemptSource
} from "./provider-execution-attempt";
import {
  recordUnitReviewProviderExecutionDecision,
  validateUnitReviewProviderExecutionDecision,
  validateUnitReviewProviderExecutionDecisionSource
} from "./provider-execution-decision";
import {
  buildUnitReviewProviderExecutionFollowUp,
  validateUnitReviewProviderExecutionFollowUp,
  validateUnitReviewProviderExecutionFollowUpSource
} from "./provider-execution-follow-up";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUp,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUp,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpSource
} from "./provider-execution-follow-up-delivery-follow-up";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUp,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUp,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-plan";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-receipt";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-reconciliation";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource
} from "./provider-execution-follow-up-n12";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource
} from "./provider-execution-follow-up-n12-plan";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource
} from "./provider-execution-follow-up-n12-receipt";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource
} from "./provider-execution-follow-up-n12-reconciliation";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp as buildUnitReviewProviderExecutionFollowUpN13,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp as validateUnitReviewProviderExecutionFollowUpN13,
   as validateUnitReviewProviderExecutionFollowUpN13Source
} from "./provider-execution-follow-up-n13";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan as buildUnitReviewProviderExecutionFollowUpN13Plan,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan as validateUnitReviewProviderExecutionFollowUpN13Plan,
   as validateUnitReviewProviderExecutionFollowUpN13PlanSource
} from "./provider-execution-follow-up-n13-plan";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt as buildUnitReviewProviderExecutionFollowUpN13Receipt,
   as validateUnitReviewProviderExecutionFollowUpN13Receipt,
   as validateUnitReviewProviderExecutionFollowUpN13ReceiptSource
} from "./provider-execution-follow-up-n13-receipt";
import {
   as buildUnitReviewProviderExecutionFollowUpN13Reconciliation,
   as validateUnitReviewProviderExecutionFollowUpN13Reconciliation,
   as validateUnitReviewProviderExecutionFollowUpN13ReconciliationSource
} from "./provider-execution-follow-up-n13-reconciliation";
import {
   as buildUnitReviewProviderExecutionFollowUpN14,
   as validateUnitReviewProviderExecutionFollowUpN14,
   as validateUnitReviewProviderExecutionFollowUpN14Source
} from "./provider-execution-follow-up-n14";
import {
   as buildUnitReviewProviderExecutionFollowUpN15,
   as validateUnitReviewProviderExecutionFollowUpN15,
   as validateUnitReviewProviderExecutionFollowUpN15Source
} from "./provider-execution-follow-up-n15";
import {
   as buildUnitReviewProviderExecutionFollowUpN16,
   as validateUnitReviewProviderExecutionFollowUpN16,
   as validateUnitReviewProviderExecutionFollowUpN16Source
} from "./provider-execution-follow-up-n16";
import {
   as buildUnitReviewProviderExecutionFollowUpN17,
   as validateUnitReviewProviderExecutionFollowUpN17,
   as validateUnitReviewProviderExecutionFollowUpN17Source
} from "./provider-execution-follow-up-n17";
import {
   as buildUnitReviewProviderExecutionFollowUpN16Plan,
   as validateUnitReviewProviderExecutionFollowUpN16Plan,
   as validateUnitReviewProviderExecutionFollowUpN16PlanSource
} from "./provider-execution-follow-up-n16-plan";
import {
   as buildUnitReviewProviderExecutionFollowUpN16Receipt,
   as validateUnitReviewProviderExecutionFollowUpN16Receipt,
   as validateUnitReviewProviderExecutionFollowUpN16ReceiptSource
} from "./provider-execution-follow-up-n16-receipt";
import {
   as buildUnitReviewProviderExecutionFollowUpN16Reconciliation,
   as validateUnitReviewProviderExecutionFollowUpN16Reconciliation,
   as validateUnitReviewProviderExecutionFollowUpN16ReconciliationSource
} from "./provider-execution-follow-up-n16-reconciliation";
import {
   as buildUnitReviewProviderExecutionFollowUpN17Plan,
   as validateUnitReviewProviderExecutionFollowUpN17Plan,
   as validateUnitReviewProviderExecutionFollowUpN17PlanSource
} from "./provider-execution-follow-up-n17-plan";
import {
   as buildUnitReviewProviderExecutionFollowUpN17Receipt,
   as validateUnitReviewProviderExecutionFollowUpN17Receipt,
   as validateUnitReviewProviderExecutionFollowUpN17ReceiptSource
} from "./provider-execution-follow-up-n17-receipt";
import {
   as buildUnitReviewProviderExecutionFollowUpN17Reconciliation,
   as validateUnitReviewProviderExecutionFollowUpN17Reconciliation,
   as validateUnitReviewProviderExecutionFollowUpN17ReconciliationSource
} from "./provider-execution-follow-up-n17-reconciliation";
import {
   as buildUnitReviewProviderExecutionFollowUpN14Plan,
   as validateUnitReviewProviderExecutionFollowUpN14Plan,
   as validateUnitReviewProviderExecutionFollowUpN14PlanSource
} from "./provider-execution-follow-up-n14-plan";
import {
   as buildUnitReviewProviderExecutionFollowUpN14Receipt,
   as validateUnitReviewProviderExecutionFollowUpN14Receipt,
   as validateUnitReviewProviderExecutionFollowUpN14ReceiptSource
} from "./provider-execution-follow-up-n14-receipt";
import {
   as buildUnitReviewProviderExecutionFollowUpN14Reconciliation,
   as validateUnitReviewProviderExecutionFollowUpN14Reconciliation,
   as validateUnitReviewProviderExecutionFollowUpN14ReconciliationSource
} from "./provider-execution-follow-up-n14-reconciliation";
import {
   as buildUnitReviewProviderExecutionFollowUpN15Plan,
   as validateUnitReviewProviderExecutionFollowUpN15Plan,
   as validateUnitReviewProviderExecutionFollowUpN15PlanSource
} from "./provider-execution-follow-up-n15-plan";
import {
   as buildUnitReviewProviderExecutionFollowUpN15Receipt,
   as validateUnitReviewProviderExecutionFollowUpN15Receipt,
   as validateUnitReviewProviderExecutionFollowUpN15ReceiptSource
} from "./provider-execution-follow-up-n15-receipt";
import {
   as buildUnitReviewProviderExecutionFollowUpN15Reconciliation,
   as validateUnitReviewProviderExecutionFollowUpN15Reconciliation,
   as validateUnitReviewProviderExecutionFollowUpN15ReconciliationSource
} from "./provider-execution-follow-up-n15-reconciliation";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource
} from "./provider-execution-follow-up-n11-plan";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource
} from "./provider-execution-follow-up-n11-receipt";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource
} from "./provider-execution-follow-up-n11-reconciliation";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource
} from "./provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpPlanSource
} from "./provider-execution-follow-up-delivery-follow-up-plan";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpReceiptSource
} from "./provider-execution-follow-up-delivery-follow-up-receipt";
import {
  buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpReconciliationSource
} from "./provider-execution-follow-up-delivery-follow-up-reconciliation";
import {
  buildUnitReviewProviderExecutionFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpPlan,
  validateUnitReviewProviderExecutionFollowUpPlanSource
} from "./provider-execution-follow-up-plan";
import {
  buildUnitReviewProviderExecutionFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpReceipt,
  validateUnitReviewProviderExecutionFollowUpReceiptSource
} from "./provider-execution-follow-up-receipt";
import {
  buildUnitReviewProviderExecutionFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpReconciliation,
  validateUnitReviewProviderExecutionFollowUpReconciliationSource
} from "./provider-execution-follow-up-reconciliation";
import {
  buildUnitReviewProviderExecutionReconciliation,
  validateUnitReviewProviderExecutionReconciliation
} from "./provider-execution-reconciliation";
import {
  buildUnitReviewProviderExecutionRequest,
  validateUnitReviewProviderExecutionRequest,
  validateUnitReviewProviderExecutionRequestSource
} from "./provider-execution-request";
import {
  buildUnitReviewRepairRequest,
  validateUnitReviewRepairRequest,
  validateUnitReviewRepairRequestSource
} from "./repair-request";
import {
  recordUnitReviewProviderExecutionReceipt,
  validateUnitReviewProviderExecutionReceipt,
  validateUnitReviewProviderExecutionReceiptSource
} from "./provider-execution-receipt";
import { runLlmMockUnitWorkflow } from "./agent-runtime";
import { runMockUnitWorkflow } from "./mock-runner";
import { applyReviewedPatchesToUnit, approveReviewArtifact } from "./review-apply";
import { renderUnitReviewMarkdownReport } from "./review-report";
import { runLlmReviewUnitWorkflow } from "./review-runner";
import type { UnitReviewArtifact } from "./review-runner";
import type { CurriculumAgentRole } from "./agent-specs";
import { validateUnitSemanticIntegrity } from "./unit-semantic-validation";
import { loadUnitSpecFromFile, serializeUnitSpecToYaml } from "./unit-io";

export interface ContentPipelineCliIO {
  stdout: (message: string) => void;
  stderr: (message: string) => void;
}

const defaultCliIo: ContentPipelineCliIO = {
  stdout: (message) => console.log(message),
  stderr: (message) => console.error(message)
};

const N13_PROVIDER_FOLLOW_UP = `provider-execution-follow-up${"-delivery-follow-up".repeat(13)}`;
const N13_PROVIDER_REVIEW = `review-${N13_PROVIDER_FOLLOW_UP}`;
const N13_SOURCE_PATH_COUNT = 57;
const N13_SOURCE_PLAN_INDEX = 54;
const N13_SOURCE_RECONCILIATION_INDEX = 56;
const N13_PLAN_SOURCE_PATH_COUNT = N13_SOURCE_PATH_COUNT + 1;
const N13_RECEIPT_SOURCE_PATH_COUNT = N13_PLAN_SOURCE_PATH_COUNT + 1;
const N13_RECONCILIATION_SOURCE_PATH_COUNT = N13_RECEIPT_SOURCE_PATH_COUNT + 1;
const N13_FOLLOW_UP_SOURCE_INDEX = N13_SOURCE_PATH_COUNT;
const N13_PLAN_SOURCE_INDEX = N13_PLAN_SOURCE_PATH_COUNT;
const N13_RECEIPT_SOURCE_INDEX = N13_RECEIPT_SOURCE_PATH_COUNT;
const N14_PROVIDER_FOLLOW_UP = `provider-execution-follow-up${"-delivery-follow-up".repeat(14)}`;
const N14_PROVIDER_REVIEW = `review-${N14_PROVIDER_FOLLOW_UP}`;
const N14_SOURCE_PATH_COUNT = N13_RECONCILIATION_SOURCE_PATH_COUNT + 1;
const N14_SOURCE_PLAN_INDEX = N13_PLAN_SOURCE_INDEX;
const N14_SOURCE_RECONCILIATION_INDEX = N13_RECONCILIATION_SOURCE_PATH_COUNT;
const N14_PLAN_SOURCE_PATH_COUNT = N14_SOURCE_PATH_COUNT + 1;
const N14_RECEIPT_SOURCE_PATH_COUNT = N14_PLAN_SOURCE_PATH_COUNT + 1;
const N14_RECONCILIATION_SOURCE_PATH_COUNT = N14_RECEIPT_SOURCE_PATH_COUNT + 1;
const N14_FOLLOW_UP_SOURCE_INDEX = N14_SOURCE_PATH_COUNT;
const N14_PLAN_SOURCE_INDEX = N14_PLAN_SOURCE_PATH_COUNT;
const N14_RECEIPT_SOURCE_INDEX = N14_RECEIPT_SOURCE_PATH_COUNT;
const N15_PROVIDER_FOLLOW_UP = `provider-execution-follow-up${"-delivery-follow-up".repeat(15)}`;
const N15_PROVIDER_REVIEW = `review-${N15_PROVIDER_FOLLOW_UP}`;
const N15_SOURCE_PATH_COUNT = N14_RECONCILIATION_SOURCE_PATH_COUNT + 1;
const N15_SOURCE_PLAN_INDEX = N14_PLAN_SOURCE_INDEX;
const N15_SOURCE_RECONCILIATION_INDEX = N14_RECONCILIATION_SOURCE_PATH_COUNT;
const N15_PLAN_SOURCE_PATH_COUNT = N15_SOURCE_PATH_COUNT + 1;
const N15_RECEIPT_SOURCE_PATH_COUNT = N15_PLAN_SOURCE_PATH_COUNT + 1;
const N15_RECONCILIATION_SOURCE_PATH_COUNT = N15_RECEIPT_SOURCE_PATH_COUNT + 1;
const N15_FOLLOW_UP_SOURCE_INDEX = N15_SOURCE_PATH_COUNT;
const N15_PLAN_SOURCE_INDEX = N15_PLAN_SOURCE_PATH_COUNT;
const N15_RECEIPT_SOURCE_INDEX = N15_RECEIPT_SOURCE_PATH_COUNT;
const N16_PROVIDER_FOLLOW_UP = `provider-execution-follow-up${"-delivery-follow-up".repeat(16)}`;
const N16_PROVIDER_REVIEW = `review-${N16_PROVIDER_FOLLOW_UP}`;
const N16_SOURCE_PATH_COUNT = N15_RECONCILIATION_SOURCE_PATH_COUNT + 1;
const N16_SOURCE_PLAN_INDEX = N15_PLAN_SOURCE_INDEX;
const N16_SOURCE_RECONCILIATION_INDEX = N15_RECONCILIATION_SOURCE_PATH_COUNT;
const N16_PLAN_SOURCE_PATH_COUNT = N16_SOURCE_PATH_COUNT + 1;
const N16_RECEIPT_SOURCE_PATH_COUNT = N16_PLAN_SOURCE_PATH_COUNT + 1;
const N16_RECONCILIATION_SOURCE_PATH_COUNT = N16_RECEIPT_SOURCE_PATH_COUNT + 1;
const N16_FOLLOW_UP_SOURCE_INDEX = N16_SOURCE_PATH_COUNT;
const N16_PLAN_SOURCE_INDEX = N16_PLAN_SOURCE_PATH_COUNT;
const N16_RECEIPT_SOURCE_INDEX = N16_RECEIPT_SOURCE_PATH_COUNT;
const N17_PROVIDER_FOLLOW_UP = `provider-execution-follow-up${"-delivery-follow-up".repeat(17)}`;
const N17_PROVIDER_REVIEW = `review-${N17_PROVIDER_FOLLOW_UP}`;
const N17_SOURCE_PATH_COUNT = N16_RECONCILIATION_SOURCE_PATH_COUNT + 1;
const N17_SOURCE_PLAN_INDEX = N16_PLAN_SOURCE_INDEX;
const N17_SOURCE_RECONCILIATION_INDEX = N16_RECONCILIATION_SOURCE_PATH_COUNT;
const N17_PLAN_SOURCE_PATH_COUNT = N17_SOURCE_PATH_COUNT + 1;
const N17_RECEIPT_SOURCE_PATH_COUNT = N17_PLAN_SOURCE_PATH_COUNT + 1;
const N17_RECONCILIATION_SOURCE_PATH_COUNT = N17_RECEIPT_SOURCE_PATH_COUNT + 1;
const N17_FOLLOW_UP_SOURCE_INDEX = N17_SOURCE_PATH_COUNT;
const N17_PLAN_SOURCE_INDEX = N17_PLAN_SOURCE_PATH_COUNT;
const N17_RECEIPT_SOURCE_INDEX = N17_RECEIPT_SOURCE_PATH_COUNT;

export interface ContentPipelineCliOptions {
  review_gateway?: Pick<LlmGateway, "complete">;
  review_now?: () => Date;
}

export async function runContentPipelineCli(
  argv: readonly string[],
  io: ContentPipelineCliIO = defaultCliIo,
  options: ContentPipelineCliOptions = {}
): Promise<number> {
  const [command, ...args] = argv;

  try {
    if (command === "validate-unit") {
      return await runValidateUnit(args, io);
    }

    if (command === "run-workflow-smoke") {
      return await runWorkflowSmoke(args, io);
    }

    if (command === "run-llm-mock") {
      return await runLlmMock(args, io);
    }

    if (command === "run-llm-review") {
      return await runLlmReview(args, io, options);
    }

    if (command === "render-review-report") {
      return await runRenderReviewReport(args, io);
    }

    if (command === "render-review-repair-request") {
      return await runRenderReviewRepairRequest(args, io);
    }

    if (command === "validate-review-repair-request") {
      return await runValidateReviewRepairRequest(args, io);
    }

    if (command === "render-review-provider-execution-request") {
      return await runRenderReviewProviderExecutionRequest(args, io);
    }

    if (command === "validate-review-provider-execution-request") {
      return await runValidateReviewProviderExecutionRequest(args, io);
    }

    if (command === "decide-review-provider-execution-request") {
      return await runDecideReviewProviderExecutionRequest(args, io);
    }

    if (command === "validate-review-provider-execution-decision") {
      return await runValidateReviewProviderExecutionDecision(args, io);
    }

    if (command === "record-review-provider-execution-attempt") {
      return await runRecordReviewProviderExecutionAttempt(args, io);
    }

    if (command === "validate-review-provider-execution-attempt") {
      return await runValidateReviewProviderExecutionAttempt(args, io);
    }

    if (command === "record-review-provider-execution-receipt") {
      return await runRecordReviewProviderExecutionReceipt(args, io);
    }

    if (command === "validate-review-provider-execution-receipt") {
      return await runValidateReviewProviderExecutionReceipt(args, io);
    }

    if (command === "render-review-provider-execution-reconciliation") {
      return await runRenderReviewProviderExecutionReconciliation(args, io);
    }

    if (command === "validate-review-provider-execution-reconciliation") {
      return await runValidateReviewProviderExecutionReconciliation(args, io);
    }

    if (command === "render-review-provider-execution-follow-up") {
      return await runRenderReviewProviderExecutionFollowUp(args, io);
    }

    if (command === "validate-review-provider-execution-follow-up") {
      return await runValidateReviewProviderExecutionFollowUp(args, io);
    }

    if (command === "render-review-provider-execution-follow-up-plan") {
      return await runRenderReviewProviderExecutionFollowUpPlan(args, io);
    }

    if (command === "validate-review-provider-execution-follow-up-plan") {
      return await runValidateReviewProviderExecutionFollowUpPlan(args, io);
    }

    if (command === "render-review-provider-execution-follow-up-receipt") {
      return await runRenderReviewProviderExecutionFollowUpReceipt(args, io);
    }

    if (command === "validate-review-provider-execution-follow-up-receipt") {
      return await runValidateReviewProviderExecutionFollowUpReceipt(args, io);
    }

    if (command === "render-review-provider-execution-follow-up-reconciliation") {
      return await runRenderReviewProviderExecutionFollowUpReconciliation(args, io);
    }

    if (command === "validate-review-provider-execution-follow-up-reconciliation") {
      return await runValidateReviewProviderExecutionFollowUpReconciliation(args, io);
    }

    if (command === "render-review-provider-execution-follow-up-delivery-follow-up") {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUp(args, io);
    }

    if (command === "validate-review-provider-execution-follow-up-delivery-follow-up") {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUp(args, io);
    }

    if (command === "render-review-provider-execution-follow-up-delivery-follow-up-plan") {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpPlan(args, io);
    }

    if (command === "validate-review-provider-execution-follow-up-delivery-follow-up-plan") {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpPlan(args, io);
    }

    if (command === "render-review-provider-execution-follow-up-delivery-follow-up-receipt") {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpReceipt(args, io);
    }

    if (command === "validate-review-provider-execution-follow-up-delivery-follow-up-receipt") {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpReceipt(args, io);
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUp(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUp(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-plan"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-plan"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-receipt"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-receipt"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
        args,
        io
      );
    }
    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }


    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
        args,
        io
      );
    }
    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
        args,
        io
      );
    }
    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }


    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
        args,
        io
      );
    }
    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
        args,
        io
      );
    }

    if (command === `render-review-${N13_PROVIDER_FOLLOW_UP}`) {
      return await runRenderReviewProviderExecutionFollowUpN13(args, io);
    }

    if (command === `validate-review-${N13_PROVIDER_FOLLOW_UP}`) {
      return await runValidateReviewProviderExecutionFollowUpN13(args, io);
    }

    if (command === `render-review-${N14_PROVIDER_FOLLOW_UP}`) {
      return await runRenderReviewProviderExecutionFollowUpN14(args, io);
    }

    if (command === `validate-review-${N14_PROVIDER_FOLLOW_UP}`) {
      return await runValidateReviewProviderExecutionFollowUpN14(args, io);
    }

    if (command === `render-review-${N15_PROVIDER_FOLLOW_UP}`) {
      return await runRenderReviewProviderExecutionFollowUpN15(args, io);
    }

    if (command === `validate-review-${N15_PROVIDER_FOLLOW_UP}`) {
      return await runValidateReviewProviderExecutionFollowUpN15(args, io);
    }

    if (command === `render-review-${N16_PROVIDER_FOLLOW_UP}`) {
      return await runRenderReviewProviderExecutionFollowUpN16(args, io);
    }

    if (command === `validate-review-${N16_PROVIDER_FOLLOW_UP}`) {
      return await runValidateReviewProviderExecutionFollowUpN16(args, io);
    }

    if (command === `render-review-${N16_PROVIDER_FOLLOW_UP}-plan`) {
      return await runRenderReviewProviderExecutionFollowUpN16Plan(args, io);
    }

    if (command === `validate-review-${N16_PROVIDER_FOLLOW_UP}-plan`) {
      return await runValidateReviewProviderExecutionFollowUpN16Plan(args, io);
    }

    if (command === `render-review-${N16_PROVIDER_FOLLOW_UP}-receipt`) {
      return await runRenderReviewProviderExecutionFollowUpN16Receipt(args, io);
    }

    if (command === `validate-review-${N16_PROVIDER_FOLLOW_UP}-receipt`) {
      return await runValidateReviewProviderExecutionFollowUpN16Receipt(args, io);
    }

    if (command === `render-review-${N16_PROVIDER_FOLLOW_UP}-reconciliation`) {
      return await runRenderReviewProviderExecutionFollowUpN16Reconciliation(args, io);
    }

    if (command === `validate-review-${N16_PROVIDER_FOLLOW_UP}-reconciliation`) {
      return await runValidateReviewProviderExecutionFollowUpN16Reconciliation(args, io);
    }

    if (command === `render-review-${N17_PROVIDER_FOLLOW_UP}`) {
      return await runRenderReviewProviderExecutionFollowUpN17(args, io);
    }

    if (command === `validate-review-${N17_PROVIDER_FOLLOW_UP}`) {
      return await runValidateReviewProviderExecutionFollowUpN17(args, io);
    }

    if (command === `render-review-${N17_PROVIDER_FOLLOW_UP}-plan`) {
      return await runRenderReviewProviderExecutionFollowUpN17Plan(args, io);
    }

    if (command === `validate-review-${N17_PROVIDER_FOLLOW_UP}-plan`) {
      return await runValidateReviewProviderExecutionFollowUpN17Plan(args, io);
    }

    if (command === `render-review-${N17_PROVIDER_FOLLOW_UP}-receipt`) {
      return await runRenderReviewProviderExecutionFollowUpN17Receipt(args, io);
    }

    if (command === `validate-review-${N17_PROVIDER_FOLLOW_UP}-receipt`) {
      return await runValidateReviewProviderExecutionFollowUpN17Receipt(args, io);
    }

    if (command === `render-review-${N17_PROVIDER_FOLLOW_UP}-reconciliation`) {
      return await runRenderReviewProviderExecutionFollowUpN17Reconciliation(args, io);
    }

    if (command === `validate-review-${N17_PROVIDER_FOLLOW_UP}-reconciliation`) {
      return await runValidateReviewProviderExecutionFollowUpN17Reconciliation(args, io);
    }

    if (command === `render-review-${N15_PROVIDER_FOLLOW_UP}-plan`) {
      return await runRenderReviewProviderExecutionFollowUpN15Plan(args, io);
    }

    if (command === `validate-review-${N15_PROVIDER_FOLLOW_UP}-plan`) {
      return await runValidateReviewProviderExecutionFollowUpN15Plan(args, io);
    }

    if (command === `render-review-${N15_PROVIDER_FOLLOW_UP}-receipt`) {
      return await runRenderReviewProviderExecutionFollowUpN15Receipt(args, io);
    }

    if (command === `validate-review-${N15_PROVIDER_FOLLOW_UP}-receipt`) {
      return await runValidateReviewProviderExecutionFollowUpN15Receipt(args, io);
    }

    if (command === `render-review-${N15_PROVIDER_FOLLOW_UP}-reconciliation`) {
      return await runRenderReviewProviderExecutionFollowUpN15Reconciliation(args, io);
    }

    if (command === `validate-review-${N15_PROVIDER_FOLLOW_UP}-reconciliation`) {
      return await runValidateReviewProviderExecutionFollowUpN15Reconciliation(args, io);
    }

    if (command === `render-review-${N14_PROVIDER_FOLLOW_UP}-plan`) {
      return await runRenderReviewProviderExecutionFollowUpN14Plan(args, io);
    }

    if (command === `validate-review-${N14_PROVIDER_FOLLOW_UP}-plan`) {
      return await runValidateReviewProviderExecutionFollowUpN14Plan(args, io);
    }

    if (command === `render-review-${N14_PROVIDER_FOLLOW_UP}-receipt`) {
      return await runRenderReviewProviderExecutionFollowUpN14Receipt(args, io);
    }

    if (command === `validate-review-${N14_PROVIDER_FOLLOW_UP}-receipt`) {
      return await runValidateReviewProviderExecutionFollowUpN14Receipt(args, io);
    }

    if (command === `render-review-${N14_PROVIDER_FOLLOW_UP}-reconciliation`) {
      return await runRenderReviewProviderExecutionFollowUpN14Reconciliation(args, io);
    }

    if (command === `validate-review-${N14_PROVIDER_FOLLOW_UP}-reconciliation`) {
      return await runValidateReviewProviderExecutionFollowUpN14Reconciliation(args, io);
    }

    if (command === `render-review-${N13_PROVIDER_FOLLOW_UP}-plan`) {
      return await runRenderReviewProviderExecutionFollowUpN13Plan(args, io);
    }

    if (command === `validate-review-${N13_PROVIDER_FOLLOW_UP}-plan`) {
      return await runValidateReviewProviderExecutionFollowUpN13Plan(args, io);
    }

    if (command === `render-review-${N13_PROVIDER_FOLLOW_UP}-receipt`) {
      return await runRenderReviewProviderExecutionFollowUpN13Receipt(args, io);
    }

    if (command === `validate-review-${N13_PROVIDER_FOLLOW_UP}-receipt`) {
      return await runValidateReviewProviderExecutionFollowUpN13Receipt(args, io);
    }

    if (command === `render-review-${N13_PROVIDER_FOLLOW_UP}-reconciliation`) {
      return await runRenderReviewProviderExecutionFollowUpN13Reconciliation(args, io);
    }

    if (command === `validate-review-${N13_PROVIDER_FOLLOW_UP}-reconciliation`) {
      return await runValidateReviewProviderExecutionFollowUpN13Reconciliation(args, io);
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }
    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }


    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }


    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
        args,
        io
      );
    }

    if (
      command ===
      "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }

    if (
      command ===
      "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation"
    ) {
      return await runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
        args,
        io
      );
    }
    if (command === "render-review-inbox-item") {
      return await runRenderReviewInboxItem(args, io);
    }

    if (command === "render-review-inbox-delivery-plan") {
      return await runRenderReviewInboxDeliveryPlan(args, io);
    }

    if (command === "validate-review-inbox-delivery-plan") {
      return await runValidateReviewInboxDeliveryPlan(args, io);
    }

    if (command === "render-review-inbox-delivery-receipt") {
      return await runRenderReviewInboxDeliveryReceipt(args, io);
    }

    if (command === "validate-review-inbox-delivery-receipt") {
      return await runValidateReviewInboxDeliveryReceipt(args, io);
    }

    if (command === "render-review-inbox-delivery-reconciliation") {
      return await runRenderReviewInboxDeliveryReconciliation(args, io);
    }

    if (command === "validate-review-inbox-delivery-reconciliation") {
      return await runValidateReviewInboxDeliveryReconciliation(args, io);
    }

    if (command === "render-review-inbox-delivery-follow-up") {
      return await runRenderReviewInboxDeliveryFollowUp(args, io);
    }

    if (command === "validate-review-inbox-delivery-follow-up") {
      return await runValidateReviewInboxDeliveryFollowUp(args, io);
    }

    if (command === "render-review-inbox-delivery-follow-up-plan") {
      return await runRenderReviewInboxDeliveryFollowUpPlan(args, io);
    }

    if (command === "validate-review-inbox-delivery-follow-up-plan") {
      return await runValidateReviewInboxDeliveryFollowUpPlan(args, io);
    }

    if (command === "render-review-inbox-delivery-follow-up-receipt") {
      return await runRenderReviewInboxDeliveryFollowUpReceipt(args, io);
    }

    if (command === "validate-review-inbox-delivery-follow-up-receipt") {
      return await runValidateReviewInboxDeliveryFollowUpReceipt(args, io);
    }

    if (command === "render-review-inbox-delivery-follow-up-reconciliation") {
      return await runRenderReviewInboxDeliveryFollowUpReconciliation(args, io);
    }

    if (command === "validate-review-inbox-delivery-follow-up-reconciliation") {
      return await runValidateReviewInboxDeliveryFollowUpReconciliation(args, io);
    }

    if (command === "approve-review-artifact") {
      return await runApproveReviewArtifact(args, io);
    }

    if (command === "apply-reviewed-patch") {
      return await runApplyReviewedPatch(args, io);
    }

    io.stderr(usage());
    return 1;
  } catch (error) {
    io.stderr(error instanceof Error ? error.message : String(error));
    return 1;
  }
}

async function runValidateUnit(args: readonly string[], io: ContentPipelineCliIO): Promise<number> {
  const unitPath = args[0];
  if (!unitPath) {
    io.stderr("Missing unit yaml path.");
    io.stderr(usage());
    return 1;
  }

  const unit = await loadUnitSpecFromFile(unitPath);
  const semantic = validateUnitSemanticIntegrity(unit);
  io.stdout(
    JSON.stringify(
      {
        ok: semantic.passed,
        command: "validate-unit",
        unit_id: unit.metadata.unit_id,
        title: unit.metadata.title,
        knowledge_nodes: unit.knowledge.nodes.length,
        quality_checklist_pass: unit.quality.checklist_pass,
        semantic_validation: semantic
      },
      null,
      2
    )
  );
  return semantic.passed ? 0 : 1;
}

async function runWorkflowSmoke(args: readonly string[], io: ContentPipelineCliIO): Promise<number> {
  const unitPath = args[0];
  if (!unitPath) {
    io.stderr("Missing unit yaml path.");
    io.stderr(usage());
    return 1;
  }

  const logPath = readFlagValue(args, "--log");
  const unit = await loadUnitSpecFromFile(unitPath);
  const result = runMockUnitWorkflow(unit);

  if (logPath) {
    await writeFile(logPath, `${JSON.stringify(result.logs, null, 2)}\n`, "utf8");
  }

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "run-workflow-smoke",
        unit_id: result.unit.metadata.unit_id,
        completed_agents: result.logs.map((entry) => entry.role),
        log_path: logPath ?? null
      },
      null,
      2
    )
  );
  return 0;
}

async function runLlmMock(args: readonly string[], io: ContentPipelineCliIO): Promise<number> {
  const unitPath = args[0];
  if (!unitPath) {
    io.stderr("Missing unit yaml path.");
    io.stderr(usage());
    return 1;
  }

  const logPath = readFlagValue(args, "--log");
  const unit = await loadUnitSpecFromFile(unitPath);
  const result = await runLlmMockUnitWorkflow(unit);

  if (logPath) {
    await writeFile(logPath, `${JSON.stringify(result.logs, null, 2)}\n`, "utf8");
  }

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "run-llm-mock",
        unit_id: result.unit.metadata.unit_id,
        completed_agents: result.logs.filter((entry) => entry.status === "succeeded").map((entry) => entry.role),
        invocation_count: result.invocations.length,
        log_path: logPath ?? null
      },
      null,
      2
    )
  );
  return 0;
}

async function runLlmReview(
  args: readonly string[],
  io: ContentPipelineCliIO,
  options: ContentPipelineCliOptions
): Promise<number> {
  const unitPath = args[0];
  if (!unitPath) {
    io.stderr("Missing unit yaml path.");
    io.stderr(usage());
    return 1;
  }

  const reviewPath = readFlagValue(args, "--review") ?? readFlagValue(args, "--log");
  if (!reviewPath) {
    io.stderr("Missing --review <review-artifact.json>. Review mode never writes unit.yaml.");
    io.stderr(usage());
    return 1;
  }

  const unit = await loadUnitSpecFromFile(unitPath);
  const rerunArtifactPath = readFlagValue(args, "--from-artifact");
  const explicitRerunFrom = readFlagValue(args, "--rerun-from") as CurriculumAgentRole | undefined;
  const artifact = await runLlmReviewUnitWorkflow(unit, {
    ...(options.review_gateway ? { gateway: options.review_gateway } : {}),
    ...(options.review_now ? { now: options.review_now } : {}),
    ...(rerunArtifactPath
      ? {
          rerun: {
            from_artifact: JSON.parse(await readFile(rerunArtifactPath, "utf8")) as UnitReviewArtifact,
            ...(explicitRerunFrom ? { start_from_role: explicitRerunFrom } : {})
          }
        }
      : {})
  });

  await writeFile(reviewPath, `${JSON.stringify(artifact, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: artifact.status === "ready_for_human_review",
        command: "run-llm-review",
        unit_id: artifact.unit_id,
        status: artifact.status,
        candidate_patch_count: artifact.candidate_patches.length,
        repair_plan_source: artifact.repair_plan?.source ?? null,
        recommended_rerun_from: artifact.repair_plan?.recommended_rerun_from ?? null,
        retry_decision: artifact.retry_policy?.decision ?? null,
        orchestration_action: artifact.orchestration_guidance?.action ?? null,
        orchestration_requires_provider_execution:
          artifact.orchestration_guidance?.requires_provider_execution ?? null,
        orchestration_requires_human_decision: artifact.orchestration_guidance?.requires_human_decision ?? null,
        orchestration_human_queue: artifact.orchestration_guidance?.human_queue ?? null,
        orchestration_automation_step: artifact.orchestration_guidance?.automation_step ?? null,
        orchestration_provider_execution_allowed_without_human:
          artifact.orchestration_guidance?.provider_execution_allowed_without_human ?? null,
        orchestration_primary_human_action: artifact.orchestration_guidance?.primary_human_action ?? null,
        orchestration_inbox_title: artifact.orchestration_guidance?.inbox_title ?? null,
        orchestration_inbox_summary: artifact.orchestration_guidance?.inbox_summary ?? null,
        runtime_event_count: artifact.runtime_projection_summary?.total_events ?? 0,
        runtime_admin_audit_projection_count:
          artifact.runtime_projection_summary?.admin_audit_projection_count ?? 0,
        runtime_student_projection_count: artifact.runtime_projection_summary?.student_projection_count ?? 0,
        runtime_teacher_projection_count: artifact.runtime_projection_summary?.teacher_projection_count ?? 0,
        runtime_guardian_projection_count: artifact.runtime_projection_summary?.guardian_projection_count ?? 0,
        scoped_rerun_from: artifact.rerun_context?.start_from_role ?? null,
        rerun_chain_depth: artifact.rerun_context?.rerun_chain_depth ?? null,
        rerun_root_artifact_generated_at: artifact.rerun_context?.rerun_root_artifact_generated_at ?? null,
        rerun_source_retry_decision: artifact.rerun_context?.source_retry_decision ?? null,
        writeback_performed: artifact.writeback_performed,
        review_path: reviewPath
      },
      null,
      2
    )
  );
  if (artifact.status !== "ready_for_human_review") {
    const firstFailure = artifact.invocation_logs.find((entry) => entry.status === "failed_retryable")?.failure;
    const rerunHint = artifact.repair_plan?.recommended_rerun_from
      ? ` Suggested rerun from: ${artifact.repair_plan.recommended_rerun_from}.`
      : "";
    const retryDecisionHint = artifact.retry_policy?.decision
      ? ` Retry decision: ${artifact.retry_policy.decision}.`
      : "";
    io.stderr(
      `Review artifact blocked. See ${reviewPath}. First failure category: ${firstFailure?.category ?? "unknown"}.${rerunHint}${retryDecisionHint}`
    );
  }
  return artifact.status === "ready_for_human_review" ? 0 : 1;
}

async function runRenderReviewReport(args: readonly string[], io: ContentPipelineCliIO): Promise<number> {
  const artifactPath = args[0];
  if (!artifactPath) {
    io.stderr("Missing review artifact path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-report.md>.");
    io.stderr(usage());
    return 1;
  }

  const artifact = JSON.parse(await readFile(artifactPath, "utf8")) as UnitReviewArtifact;
  const report = renderUnitReviewMarkdownReport(artifact);
  await writeFile(outputPath, report, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "render-review-report",
        unit_id: artifact.unit_id,
        status: artifact.status,
        report_path: outputPath
      },
      null,
      2
    )
  );
  return 0;
}

async function runRenderReviewRepairRequest(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const artifactPath = args[0];
  if (!artifactPath) {
    io.stderr("Missing review artifact path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-repair-request.json>.");
    io.stderr(usage());
    return 1;
  }

  const artifact = JSON.parse(await readFile(artifactPath, "utf8")) as UnitReviewArtifact;
  const sourceValidation = validateUnitReviewRepairRequestSource(artifact);
  if (!sourceValidation.ok) {
    io.stderr(
      [
        "Cannot render repair request from an invalid review artifact source.",
        ...sourceValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }

  const request = buildUnitReviewRepairRequest(artifact);
  await writeFile(outputPath, `${JSON.stringify(request, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "render-review-repair-request",
        repair_action: request.repair_action,
        repair_strategy: request.repair_strategy,
        requested_start_role: request.requested_start_role,
        requested_role_count: request.requested_roles.length,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewRepairRequest(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const artifactPath = args[0];
  const requestPath = args[1];
  if (!artifactPath || !requestPath) {
    io.stderr("Missing review artifact path or repair request path.");
    io.stderr(usage());
    return 1;
  }

  const artifact = JSON.parse(await readFile(artifactPath, "utf8")) as UnitReviewArtifact;
  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const sourceValidation = validateUnitReviewRepairRequestSource(artifact);
  const issues = [
    ...sourceValidation.issues.map((issue) => ({ stage: "repair_request_source", ...issue })),
    ...(sourceValidation.ok
      ? validateUnitReviewRepairRequest(artifact, request).issues.map((issue) => ({
          stage: "repair_request",
          ...issue
        }))
      : [])
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: "validate-review-repair-request",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionRequest(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const artifactPath = args[0];
  if (!artifactPath) {
    io.stderr("Missing review artifact path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-request.json>.");
    io.stderr(usage());
    return 1;
  }

  const artifact = JSON.parse(await readFile(artifactPath, "utf8")) as UnitReviewArtifact;
  const sourceValidation = validateUnitReviewProviderExecutionRequestSource(artifact);
  if (!sourceValidation.ok) {
    io.stderr(
      [
        "Cannot render provider execution request from an invalid review artifact source.",
        ...sourceValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }

  const request = buildUnitReviewProviderExecutionRequest(artifact);
  await writeFile(outputPath, `${JSON.stringify(request, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "render-review-provider-execution-request",
        execution_action: request.execution_action,
        requested_start_role: request.requested_start_role,
        requested_role_count: request.requested_roles.length,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionRequest(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const artifactPath = args[0];
  const requestPath = args[1];
  if (!artifactPath || !requestPath) {
    io.stderr("Missing review artifact path or provider execution request path.");
    io.stderr(usage());
    return 1;
  }

  const artifact = JSON.parse(await readFile(artifactPath, "utf8")) as UnitReviewArtifact;
  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const sourceValidation = validateUnitReviewProviderExecutionRequestSource(artifact);
  const issues = [
    ...sourceValidation.issues.map((issue) => ({ stage: "provider_execution_request_source", ...issue })),
    ...(sourceValidation.ok
      ? validateUnitReviewProviderExecutionRequest(artifact, request).issues.map((issue) => ({
          stage: "provider_execution_request",
          ...issue
        }))
      : [])
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: "validate-review-provider-execution-request",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runDecideReviewProviderExecutionRequest(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  if (!requestPath) {
    io.stderr("Missing provider execution request path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-decision.json>. This command never overwrites the source request.");
    io.stderr(usage());
    return 1;
  }

  const reviewerId = readFlagValue(args, "--reviewer");
  if (!reviewerId || !reviewerId.trim()) {
    io.stderr("Missing --reviewer <reviewer-id>.");
    io.stderr(usage());
    return 1;
  }

  const decisionFlag = readFlagValue(args, "--decision");
  if (decisionFlag !== "approve" && decisionFlag !== "reject") {
    io.stderr("Missing or invalid --decision <approve|reject>.");
    io.stderr(usage());
    return 1;
  }

  const budgetCheckFlag = readFlagValue(args, "--budget-check");
  if (budgetCheckFlag !== "passed" && budgetCheckFlag !== "failed") {
    io.stderr("Missing or invalid --budget-check <passed|failed>.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const sourceValidation = validateUnitReviewProviderExecutionDecisionSource(request);
  if (!sourceValidation.ok) {
    io.stderr(
      [
        "Cannot record provider execution decision from an invalid provider execution request source.",
        ...sourceValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }

  const notes = readFlagValue(args, "--notes");
  const budgetReference = readFlagValue(args, "--budget-reference");
  const decision = recordUnitReviewProviderExecutionDecision(request, {
    reviewer_id: reviewerId,
    decision: decisionFlag === "approve" ? "approved" : "rejected",
    budget_check_status: budgetCheckFlag,
    ...(notes ? { notes } : {}),
    ...(budgetReference ? { budget_reference: budgetReference } : {})
  });
  await writeFile(outputPath, `${JSON.stringify(decision, null, 2)}\n`, {
    encoding: "utf8",
    flag: "wx"
  });

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "decide-review-provider-execution-request",
        unit_id: decision.unit_id,
        decision_status: decision.decision_status,
        execution_permission: decision.execution_permission,
        budget_check_status: decision.budget_check_status,
        reviewer_id: decision.reviewer_id,
        source_request_path: requestPath,
        output_path: outputPath,
        source_overwritten: false
      },
      null,
      2
    )
  );
  return 0;
}

async function runValidateReviewProviderExecutionDecision(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  if (!requestPath || !decisionPath) {
    io.stderr("Missing provider execution request path or provider execution decision path.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const sourceValidation = validateUnitReviewProviderExecutionDecisionSource(request);
  const issues = [
    ...sourceValidation.issues.map((issue) => ({ stage: "provider_execution_decision_source", ...issue })),
    ...validateUnitReviewProviderExecutionDecision(request, decision).issues.map((issue) => ({
      stage: "provider_execution_decision",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: "validate-review-provider-execution-decision",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRecordReviewProviderExecutionAttempt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  if (!requestPath || !decisionPath) {
    io.stderr("Missing provider execution request path or provider execution decision path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-attempt.json>. This command never overwrites the source request or decision.");
    io.stderr(usage());
    return 1;
  }

  const recordedBy = readFlagValue(args, "--recorded-by");
  if (!recordedBy || !recordedBy.trim()) {
    io.stderr("Missing --recorded-by <operator-id>.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const sourceValidation = validateUnitReviewProviderExecutionAttemptSource(request, decision);
  if (!sourceValidation.ok) {
    io.stderr(
      [
        "Cannot record provider execution attempt from an invalid request/decision source chain.",
        ...sourceValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }

  const recordedAt = readFlagValue(args, "--recorded-at");
  const attemptNotes = readFlagValue(args, "--attempt-notes");
  const attempt = recordUnitReviewProviderExecutionAttempt(request, decision, {
    recorded_by: recordedBy,
    ...(recordedAt ? { recorded_at: recordedAt } : {}),
    ...(attemptNotes ? { attempt_notes: attemptNotes } : {})
  });
  await writeFile(outputPath, `${JSON.stringify(attempt, null, 2)}\n`, {
    encoding: "utf8",
    flag: "wx"
  });

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "record-review-provider-execution-attempt",
        unit_id: attempt.unit_id,
        attempt_key: attempt.attempt_key,
        attempt_status: attempt.attempt_status,
        execution_action: attempt.execution_action,
        requested_start_role: attempt.requested_start_role,
        recorded_by: attempt.recorded_by,
        source_request_path: requestPath,
        source_decision_path: decisionPath,
        output_path: outputPath,
        sources_overwritten: false
      },
      null,
      2
    )
  );
  return 0;
}

async function runValidateReviewProviderExecutionAttempt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  if (!requestPath || !decisionPath || !attemptPath) {
    io.stderr("Missing provider execution request path, provider execution decision path, or provider execution attempt path.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const issues = [
    ...validateUnitReviewProviderExecutionAttemptSource(request, decision).issues.map((issue) => ({
      stage: "provider_execution_attempt_source",
      ...issue
    })),
    ...validateUnitReviewProviderExecutionAttempt(request, decision, attempt).issues.map((issue) => ({
      stage: "provider_execution_attempt",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: "validate-review-provider-execution-attempt",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRecordReviewProviderExecutionReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  if (!requestPath || !decisionPath || !attemptPath) {
    io.stderr("Missing provider execution request path, provider execution decision path, or provider execution attempt path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-receipt.json>. This command never overwrites the source request, decision, or attempt.");
    io.stderr(usage());
    return 1;
  }

  const executedBy = readFlagValue(args, "--executed-by");
  if (!executedBy || !executedBy.trim()) {
    io.stderr("Missing --executed-by <operator-id>.");
    io.stderr(usage());
    return 1;
  }

  const receiptStatus = readFlagValue(args, "--receipt-status");
  if (receiptStatus !== "artifact_recorded" && receiptStatus !== "execution_failed") {
    io.stderr("Missing or invalid --receipt-status <artifact_recorded|execution_failed>.");
    io.stderr(usage());
    return 1;
  }

  const actualProviderCallCountFlag = readFlagValue(args, "--actual-provider-call-count");
  if (!actualProviderCallCountFlag || !/^\d+$/.test(actualProviderCallCountFlag)) {
    io.stderr("Missing or invalid --actual-provider-call-count <non-negative-integer>.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceValidation = validateUnitReviewProviderExecutionReceiptSource(request, decision, attempt);
  if (!sourceValidation.ok) {
    io.stderr(
      [
        "Cannot record provider execution receipt from an invalid request/decision/attempt source chain.",
        ...sourceValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }

  const executedAt = readFlagValue(args, "--executed-at");
  const resultArtifactGeneratedAt = readFlagValue(args, "--result-artifact-generated-at");
  const resultArtifactStatus = readFlagValue(args, "--result-artifact-status");
  const failureCode = readFlagValue(args, "--failure-code");
  const failureSummary = readFlagValue(args, "--failure-summary");
  const receipt = recordUnitReviewProviderExecutionReceipt(request, decision, attempt, {
    receipt_status: receiptStatus,
    executed_by: executedBy,
    ...(executedAt ? { executed_at: executedAt } : {}),
    actual_provider_call_count: Number(actualProviderCallCountFlag),
    ...(resultArtifactGeneratedAt ? { result_artifact_generated_at: resultArtifactGeneratedAt } : {}),
    ...(resultArtifactStatus ? { result_artifact_status: resultArtifactStatus as "ready_for_human_review" | "blocked" } : {}),
    ...(failureCode ? { failure_code: failureCode } : {}),
    ...(failureSummary ? { failure_summary: failureSummary } : {})
  });
  await writeFile(outputPath, `${JSON.stringify(receipt, null, 2)}\n`, {
    encoding: "utf8",
    flag: "wx"
  });

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "record-review-provider-execution-receipt",
        unit_id: receipt.unit_id,
        attempt_key: receipt.attempt_key,
        receipt_status: receipt.receipt_status,
        actual_provider_call_count: receipt.actual_provider_call_count,
        executed_by: receipt.executed_by,
        output_path: outputPath,
        sources_overwritten: false
      },
      null,
      2
    )
  );
  return 0;
}

async function runValidateReviewProviderExecutionReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const receiptPath = args[3];
  if (!requestPath || !decisionPath || !attemptPath || !receiptPath) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, or provider execution receipt path.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const issues = [
    ...validateUnitReviewProviderExecutionReceiptSource(request, decision, attempt).issues.map((issue) => ({
      stage: "provider_execution_receipt_source",
      ...issue
    })),
    ...validateUnitReviewProviderExecutionReceipt(request, decision, attempt, receipt).issues.map((issue) => ({
      stage: "provider_execution_receipt",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: "validate-review-provider-execution-receipt",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const receiptPath = args[3];
  if (!requestPath || !decisionPath || !attemptPath || !receiptPath) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, or provider execution receipt path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-reconciliation.json>.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const reconciliation = buildUnitReviewProviderExecutionReconciliation(
    request,
    decision,
    attempt,
    receipt
  );
  await writeFile(outputPath, `${JSON.stringify(reconciliation, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "render-review-provider-execution-reconciliation",
        reconciliation_status: reconciliation.reconciliation_status,
        recommended_follow_up: reconciliation.recommended_follow_up,
        execution_outcome: reconciliation.execution_outcome,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const receiptPath = args[3];
  const reconciliationPath = args[4];
  if (!requestPath || !decisionPath || !attemptPath || !receiptPath || !reconciliationPath) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, or provider execution reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const reconciliation = JSON.parse(await readFile(reconciliationPath, "utf8"));
  const validation = validateUnitReviewProviderExecutionReconciliation(
    request,
    decision,
    attempt,
    receipt,
    reconciliation
  );

  io.stdout(
    JSON.stringify(
      {
        ok: validation.ok,
        command: "validate-review-provider-execution-reconciliation",
        issue_count: validation.issues.length,
        issues: validation.issues
      },
      null,
      2
    )
  );

  return validation.ok ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const receiptPath = args[3];
  const reconciliationPath = args[4];
  if (!requestPath || !decisionPath || !attemptPath || !receiptPath || !reconciliationPath) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, or provider execution reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up.json>.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const reconciliation = JSON.parse(await readFile(reconciliationPath, "utf8"));
  const sourceValidation = validateUnitReviewProviderExecutionFollowUpSource(
    request,
    decision,
    attempt,
    receipt,
    reconciliation
  );
  if (!sourceValidation.ok) {
    io.stderr(
      [
        "Cannot render provider execution follow-up from an invalid reconciliation contract.",
        ...sourceValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }

  const followUp = buildUnitReviewProviderExecutionFollowUp(
    request,
    decision,
    attempt,
    receipt,
    reconciliation
  );
  await writeFile(outputPath, `${JSON.stringify(followUp, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "render-review-provider-execution-follow-up",
        follow_up_state: followUp.follow_up_state,
        follow_up_action: followUp.follow_up_action,
        provider_execution_chain_closed: followUp.provider_execution_chain_closed,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const receiptPath = args[3];
  const reconciliationPath = args[4];
  const followUpPath = args[5];
  if (!requestPath || !decisionPath || !attemptPath || !receiptPath || !reconciliationPath || !followUpPath) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, or provider execution follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const reconciliation = JSON.parse(await readFile(reconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const validation = validateUnitReviewProviderExecutionFollowUp(
    request,
    decision,
    attempt,
    receipt,
    reconciliation,
    followUp
  );

  io.stdout(
    JSON.stringify(
      {
        ok: validation.ok,
        command: "validate-review-provider-execution-follow-up",
        issue_count: validation.issues.length,
        issues: validation.issues
      },
      null,
      2
    )
  );

  return validation.ok ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const receiptPath = args[3];
  const reconciliationPath = args[4];
  const followUpPath = args[5];
  if (!requestPath || !decisionPath || !attemptPath || !receiptPath || !reconciliationPath || !followUpPath) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, or provider execution follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-plan.json>.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const reconciliation = JSON.parse(await readFile(reconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const sourceValidation = validateUnitReviewProviderExecutionFollowUpPlanSource(
    request,
    decision,
    attempt,
    receipt,
    reconciliation,
    followUp
  );
  if (!sourceValidation.ok) {
    io.stderr(
      [
        "Cannot render provider execution follow-up plan from an invalid follow-up contract.",
        ...sourceValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }

  const plan = buildUnitReviewProviderExecutionFollowUpPlan(followUp);
  await writeFile(outputPath, `${JSON.stringify(plan, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "render-review-provider-execution-follow-up-plan",
        delivery_action: plan.delivery_action,
        final_follow_up_item_key: plan.final_follow_up_item_key,
        provider_execution_chain_closed: plan.provider_execution_chain_closed,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const receiptPath = args[3];
  const reconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  if (!requestPath || !decisionPath || !attemptPath || !receiptPath || !reconciliationPath || !followUpPath || !planPath) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, or provider execution follow-up plan path.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const reconciliation = JSON.parse(await readFile(reconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const sourceValidation = validateUnitReviewProviderExecutionFollowUpPlanSource(
    request,
    decision,
    attempt,
    receipt,
    reconciliation,
    followUp
  );
  const planValidation = validateUnitReviewProviderExecutionFollowUpPlan(followUp, plan);
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: "provider_execution_follow_up_source_contract",
      ...issue
    })),
    ...planValidation.issues.map((issue) => ({
      stage: "provider_execution_follow_up_plan",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: "validate-review-provider-execution-follow-up-plan",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, or provider execution follow-up plan path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-receipt.json>.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const sourceValidation = validateUnitReviewProviderExecutionFollowUpReceiptSource(
    request,
    decision,
    attempt,
    sourceReceipt,
    sourceReconciliation,
    followUp,
    plan
  );
  if (!sourceValidation.ok) {
    io.stderr(
      [
        "Cannot render provider execution follow-up receipt from an invalid follow-up delivery plan chain.",
        ...sourceValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }

  const executedAt = readFlagValue(args, "--executed-at") ?? new Date().toISOString();
  const upsertStatus = readOperationStatusFlag(args, "--upsert-status");
  const receipt = buildUnitReviewProviderExecutionFollowUpReceipt(plan, {
    executed_at: executedAt,
    ...(upsertStatus ? { upsert_status: upsertStatus } : {})
  });
  await writeFile(outputPath, `${JSON.stringify(receipt, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "render-review-provider-execution-follow-up-receipt",
        overall_status: receipt.overall_status,
        failed_operation_count: receipt.operations.filter((operation) => operation.status === "failed").length,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const receiptPath = args[7];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !receiptPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, or provider execution follow-up receipt path.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const sourceValidation = validateUnitReviewProviderExecutionFollowUpReceiptSource(
    request,
    decision,
    attempt,
    sourceReceipt,
    sourceReconciliation,
    followUp,
    plan
  );
  const receiptValidation = validateUnitReviewProviderExecutionFollowUpReceipt(plan, receipt);
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: "provider_execution_follow_up_receipt_source_contract",
      ...issue
    })),
    ...receiptValidation.issues.map((issue) => ({
      stage: "provider_execution_follow_up_receipt",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: "validate-review-provider-execution-follow-up-receipt",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const receiptPath = args[7];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !receiptPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, or provider execution follow-up receipt path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-reconciliation.json>.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const sourceValidation = validateUnitReviewProviderExecutionFollowUpReconciliationSource(
    request,
    decision,
    attempt,
    sourceReceipt,
    sourceReconciliation,
    followUp,
    plan,
    receipt
  );
  if (!sourceValidation.ok) {
    io.stderr(
      [
        "Cannot render provider execution follow-up reconciliation from an invalid follow-up delivery chain.",
        ...sourceValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }

  const reconciliation = buildUnitReviewProviderExecutionFollowUpReconciliation(plan, receipt);
  await writeFile(outputPath, `${JSON.stringify(reconciliation, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "render-review-provider-execution-follow-up-reconciliation",
        reconciliation_status: reconciliation.reconciliation_status,
        recommended_follow_up: reconciliation.recommended_follow_up,
        unresolved_operation_count: reconciliation.unresolved_operations.length,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const receiptPath = args[7];
  const reconciliationPath = args[8];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !receiptPath ||
    !reconciliationPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, or provider execution follow-up reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const reconciliation = JSON.parse(await readFile(reconciliationPath, "utf8"));
  const sourceValidation = validateUnitReviewProviderExecutionFollowUpReconciliationSource(
    request,
    decision,
    attempt,
    sourceReceipt,
    sourceReconciliation,
    followUp,
    plan,
    receipt
  );
  const reconciliationValidation = validateUnitReviewProviderExecutionFollowUpReconciliation(
    plan,
    receipt,
    reconciliation
  );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: "provider_execution_follow_up_reconciliation_source_contract",
      ...issue
    })),
    ...reconciliationValidation.issues.map((issue) => ({
      stage: "provider_execution_follow_up_reconciliation",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: "validate-review-provider-execution-follow-up-reconciliation",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, or provider execution follow-up reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up.json>.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const sourceValidation = validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpSource(
    request,
    decision,
    attempt,
    sourceReceipt,
    sourceReconciliation,
    followUp,
    plan,
    followUpReceipt,
    followUpReconciliation
  );
  if (!sourceValidation.ok) {
    io.stderr(
      [
        "Cannot render provider execution follow-up delivery follow-up from an invalid post-delivery follow-up chain.",
        ...sourceValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }

  const followUpDeliveryFollowUp = buildUnitReviewProviderExecutionFollowUpDeliveryFollowUp(
    plan,
    followUpReconciliation
  );
  await writeFile(
    outputPath,
    `${JSON.stringify(followUpDeliveryFollowUp, null, 2)}\n`,
    "utf8"
  );

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "render-review-provider-execution-follow-up-delivery-follow-up",
        follow_up_state: followUpDeliveryFollowUp.follow_up_state,
        follow_up_action: followUpDeliveryFollowUp.follow_up_action,
        follow_up_delivery_chain_closed:
          followUpDeliveryFollowUp.follow_up_delivery_chain_closed,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  const followUpDeliveryFollowUpPath = args[9];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath ||
    !followUpDeliveryFollowUpPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, provider execution follow-up reconciliation path, or provider execution follow-up delivery follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const followUpDeliveryFollowUp = JSON.parse(
    await readFile(followUpDeliveryFollowUpPath, "utf8")
  );
  const sourceValidation = validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpSource(
    request,
    decision,
    attempt,
    sourceReceipt,
    sourceReconciliation,
    followUp,
    plan,
    followUpReceipt,
    followUpReconciliation
  );
  const followUpValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUp(
      plan,
      followUpReconciliation,
      followUpDeliveryFollowUp
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: "provider_execution_follow_up_delivery_follow_up_source_contract",
      ...issue
    })),
    ...followUpValidation.issues.map((issue) => ({
      stage: "provider_execution_follow_up_delivery_follow_up",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: "validate-review-provider-execution-follow-up-delivery-follow-up",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  const followUpDeliveryFollowUpPath = args[9];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath ||
    !followUpDeliveryFollowUpPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, provider execution follow-up reconciliation path, or provider execution follow-up delivery follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-plan.json>.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const followUpDeliveryFollowUp = JSON.parse(
    await readFile(followUpDeliveryFollowUpPath, "utf8")
  );
  const sourceValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpPlanSource(
      request,
      decision,
      attempt,
      sourceReceipt,
      sourceReconciliation,
      followUp,
      plan,
      followUpReceipt,
      followUpReconciliation,
      followUpDeliveryFollowUp
    );
  if (!sourceValidation.ok) {
    io.stderr(
      [
        "Cannot render provider execution follow-up delivery follow-up plan from an invalid post-delivery follow-up chain.",
        ...sourceValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }

  const deliveryFollowUpPlan =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpPlan(
      followUpDeliveryFollowUp
    );
  await writeFile(outputPath, `${JSON.stringify(deliveryFollowUpPlan, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "render-review-provider-execution-follow-up-delivery-follow-up-plan",
        follow_up_state: deliveryFollowUpPlan.follow_up_state,
        delivery_action: deliveryFollowUpPlan.delivery_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  const followUpDeliveryFollowUpPath = args[9];
  const deliveryFollowUpPlanPath = args[10];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath ||
    !followUpDeliveryFollowUpPath ||
    !deliveryFollowUpPlanPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, provider execution follow-up reconciliation path, provider execution follow-up delivery follow-up path, or provider execution follow-up delivery follow-up plan path.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const followUpDeliveryFollowUp = JSON.parse(
    await readFile(followUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpPlanPath, "utf8")
  );
  const sourceValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpPlanSource(
      request,
      decision,
      attempt,
      sourceReceipt,
      sourceReconciliation,
      followUp,
      plan,
      followUpReceipt,
      followUpReconciliation,
      followUpDeliveryFollowUp
    );
  const planValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpPlan(
      followUpDeliveryFollowUp,
      deliveryFollowUpPlan
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: "provider_execution_follow_up_delivery_follow_up_plan_source_contract",
      ...issue
    })),
    ...planValidation.issues.map((issue) => ({
      stage: "provider_execution_follow_up_delivery_follow_up_plan",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: "validate-review-provider-execution-follow-up-delivery-follow-up-plan",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  const followUpDeliveryFollowUpPath = args[9];
  const deliveryFollowUpPlanPath = args[10];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath ||
    !followUpDeliveryFollowUpPath ||
    !deliveryFollowUpPlanPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, provider execution follow-up reconciliation path, provider execution follow-up delivery follow-up path, or provider execution follow-up delivery follow-up plan path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-receipt.json>.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const followUpDeliveryFollowUp = JSON.parse(
    await readFile(followUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpPlanPath, "utf8")
  );
  const sourceValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpReceiptSource(
      request,
      decision,
      attempt,
      sourceReceipt,
      sourceReconciliation,
      followUp,
      plan,
      followUpReceipt,
      followUpReconciliation,
      followUpDeliveryFollowUp,
      deliveryFollowUpPlan
    );
  if (!sourceValidation.ok) {
    io.stderr(
      [
        "Cannot render provider execution follow-up delivery follow-up receipt from an invalid post-delivery follow-up plan chain.",
        ...sourceValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }

  const upsertStatus = readFlagValue(args, "--upsert-status") as UnitReviewInboxDeliveryOperationStatus | undefined;
  const deliveryFollowUpReceipt =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpReceipt(
      deliveryFollowUpPlan,
      {
        executed_at: new Date().toISOString(),
        ...(upsertStatus ? { upsert_status: upsertStatus } : {})
      }
    );
  await writeFile(
    outputPath,
    `${JSON.stringify(deliveryFollowUpReceipt, null, 2)}\n`,
    "utf8"
  );

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "render-review-provider-execution-follow-up-delivery-follow-up-receipt",
        overall_status: deliveryFollowUpReceipt.overall_status,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  const followUpDeliveryFollowUpPath = args[9];
  const deliveryFollowUpPlanPath = args[10];
  const deliveryFollowUpReceiptPath = args[11];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath ||
    !followUpDeliveryFollowUpPath ||
    !deliveryFollowUpPlanPath ||
    !deliveryFollowUpReceiptPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, provider execution follow-up reconciliation path, provider execution follow-up delivery follow-up path, provider execution follow-up delivery follow-up plan path, or provider execution follow-up delivery follow-up receipt path.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const followUpDeliveryFollowUp = JSON.parse(
    await readFile(followUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpReceiptPath, "utf8")
  );
  const sourceValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpReceiptSource(
      request,
      decision,
      attempt,
      sourceReceipt,
      sourceReconciliation,
      followUp,
      plan,
      followUpReceipt,
      followUpReconciliation,
      followUpDeliveryFollowUp,
      deliveryFollowUpPlan
    );
  const receiptValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpReceipt(
      deliveryFollowUpPlan,
      deliveryFollowUpReceipt
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: "provider_execution_follow_up_delivery_follow_up_receipt_source_contract",
      ...issue
    })),
    ...receiptValidation.issues.map((issue) => ({
      stage: "provider_execution_follow_up_delivery_follow_up_receipt",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: "validate-review-provider-execution-follow-up-delivery-follow-up-receipt",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  const followUpDeliveryFollowUpPath = args[9];
  const deliveryFollowUpPlanPath = args[10];
  const deliveryFollowUpReceiptPath = args[11];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath ||
    !followUpDeliveryFollowUpPath ||
    !deliveryFollowUpPlanPath ||
    !deliveryFollowUpReceiptPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, provider execution follow-up reconciliation path, provider execution follow-up delivery follow-up path, provider execution follow-up delivery follow-up plan path, or provider execution follow-up delivery follow-up receipt path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-reconciliation.json>.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const followUpDeliveryFollowUp = JSON.parse(
    await readFile(followUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpReceiptPath, "utf8")
  );
  const sourceValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpReconciliationSource(
      request,
      decision,
      attempt,
      sourceReceipt,
      sourceReconciliation,
      followUp,
      plan,
      followUpReceipt,
      followUpReconciliation,
      followUpDeliveryFollowUp,
      deliveryFollowUpPlan,
      deliveryFollowUpReceipt
    );
  if (!sourceValidation.ok) {
    io.stderr(
      [
        "Cannot render provider execution follow-up delivery follow-up reconciliation from an invalid post-delivery receipt chain.",
        ...sourceValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }

  const deliveryFollowUpReconciliation =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpReconciliation(
      deliveryFollowUpPlan,
      deliveryFollowUpReceipt
    );
  await writeFile(
    outputPath,
    `${JSON.stringify(deliveryFollowUpReconciliation, null, 2)}\n`,
    "utf8"
  );

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-reconciliation",
        reconciliation_status: deliveryFollowUpReconciliation.reconciliation_status,
        recommended_follow_up: deliveryFollowUpReconciliation.recommended_follow_up,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  const followUpDeliveryFollowUpPath = args[9];
  const deliveryFollowUpPlanPath = args[10];
  const deliveryFollowUpReceiptPath = args[11];
  const deliveryFollowUpReconciliationPath = args[12];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath ||
    !followUpDeliveryFollowUpPath ||
    !deliveryFollowUpPlanPath ||
    !deliveryFollowUpReceiptPath ||
    !deliveryFollowUpReconciliationPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, provider execution follow-up reconciliation path, provider execution follow-up delivery follow-up path, provider execution follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up receipt path, or provider execution follow-up delivery follow-up reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const followUpDeliveryFollowUp = JSON.parse(
    await readFile(followUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpReconciliationPath, "utf8")
  );
  const sourceValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpReconciliationSource(
      request,
      decision,
      attempt,
      sourceReceipt,
      sourceReconciliation,
      followUp,
      plan,
      followUpReceipt,
      followUpReconciliation,
      followUpDeliveryFollowUp,
      deliveryFollowUpPlan,
      deliveryFollowUpReceipt
    );
  const reconciliationValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpReconciliation(
      deliveryFollowUpPlan,
      deliveryFollowUpReceipt,
      deliveryFollowUpReconciliation
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_reconciliation_source_contract",
      ...issue
    })),
    ...reconciliationValidation.issues.map((issue) => ({
      stage: "provider_execution_follow_up_delivery_follow_up_reconciliation",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-reconciliation",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  const followUpDeliveryFollowUpPath = args[9];
  const deliveryFollowUpPlanPath = args[10];
  const deliveryFollowUpReceiptPath = args[11];
  const deliveryFollowUpReconciliationPath = args[12];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath ||
    !followUpDeliveryFollowUpPath ||
    !deliveryFollowUpPlanPath ||
    !deliveryFollowUpReceiptPath ||
    !deliveryFollowUpReconciliationPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, provider execution follow-up reconciliation path, provider execution follow-up delivery follow-up path, provider execution follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up receipt path, or provider execution follow-up delivery follow-up reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(
    args,
    "--out"
  );
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up.json>.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const followUpDeliveryFollowUp = JSON.parse(
    await readFile(followUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpReconciliationPath, "utf8")
  );
  const sourceValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpSource(
      request,
      decision,
      attempt,
      sourceReceipt,
      sourceReconciliation,
      followUp,
      plan,
      followUpReceipt,
      followUpReconciliation,
      followUpDeliveryFollowUp,
      deliveryFollowUpPlan,
      deliveryFollowUpReceipt,
      deliveryFollowUpReconciliation
    );

  if (!sourceValidation.ok) {
    io.stdout(
      JSON.stringify(
        {
          ok: false,
          command:
            "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up",
          issue_count: sourceValidation.issues.length,
          issues: sourceValidation.issues
        },
        null,
        2
      )
    );
    return 1;
  }

  const deliveryFollowUpDeliveryFollowUp =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUp(
      deliveryFollowUpPlan,
      deliveryFollowUpReconciliation
    );
  await writeFile(
    outputPath,
    `${JSON.stringify(deliveryFollowUpDeliveryFollowUp, null, 2)}\n`,
    "utf8"
  );

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up",
        follow_up_state: deliveryFollowUpDeliveryFollowUp.follow_up_state,
        follow_up_action: deliveryFollowUpDeliveryFollowUp.follow_up_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  const followUpDeliveryFollowUpPath = args[9];
  const deliveryFollowUpPlanPath = args[10];
  const deliveryFollowUpReceiptPath = args[11];
  const deliveryFollowUpReconciliationPath = args[12];
  const deliveryFollowUpDeliveryFollowUpPath = args[13];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath ||
    !followUpDeliveryFollowUpPath ||
    !deliveryFollowUpPlanPath ||
    !deliveryFollowUpReceiptPath ||
    !deliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, provider execution follow-up reconciliation path, provider execution follow-up delivery follow-up path, provider execution follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up reconciliation path, or provider execution follow-up delivery follow-up delivery follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const followUpDeliveryFollowUp = JSON.parse(
    await readFile(followUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const sourceValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpSource(
      request,
      decision,
      attempt,
      sourceReceipt,
      sourceReconciliation,
      followUp,
      plan,
      followUpReceipt,
      followUpReconciliation,
      followUpDeliveryFollowUp,
      deliveryFollowUpPlan,
      deliveryFollowUpReceipt,
      deliveryFollowUpReconciliation
    );
  const followUpValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUp(
      deliveryFollowUpPlan,
      deliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUp
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_source_contract",
      ...issue
    })),
    ...followUpValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  const followUpDeliveryFollowUpPath = args[9];
  const deliveryFollowUpPlanPath = args[10];
  const deliveryFollowUpReceiptPath = args[11];
  const deliveryFollowUpReconciliationPath = args[12];
  const deliveryFollowUpDeliveryFollowUpPath = args[13];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath ||
    !followUpDeliveryFollowUpPath ||
    !deliveryFollowUpPlanPath ||
    !deliveryFollowUpReceiptPath ||
    !deliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, provider execution follow-up reconciliation path, provider execution follow-up delivery follow-up path, provider execution follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up reconciliation path, or provider execution follow-up delivery follow-up delivery follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-plan.json>.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const followUpDeliveryFollowUp = JSON.parse(
    await readFile(followUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const sourceValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource(
      request,
      decision,
      attempt,
      sourceReceipt,
      sourceReconciliation,
      followUp,
      plan,
      followUpReceipt,
      followUpReconciliation,
      followUpDeliveryFollowUp,
      deliveryFollowUpPlan,
      deliveryFollowUpReceipt,
      deliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUp
    );
  if (!sourceValidation.ok) {
    io.stderr(
      [
        "Cannot render provider execution follow-up delivery follow-up delivery follow-up plan from an invalid post-delivery routing chain.",
        ...sourceValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }

  const deliveryFollowUpDeliveryFollowUpPlan =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
      deliveryFollowUpDeliveryFollowUp
    );
  await writeFile(
    outputPath,
    `${JSON.stringify(deliveryFollowUpDeliveryFollowUpPlan, null, 2)}\n`,
    "utf8"
  );

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-plan",
        follow_up_state: deliveryFollowUpDeliveryFollowUpPlan.follow_up_state,
        delivery_action: deliveryFollowUpDeliveryFollowUpPlan.delivery_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  const followUpDeliveryFollowUpPath = args[9];
  const deliveryFollowUpPlanPath = args[10];
  const deliveryFollowUpReceiptPath = args[11];
  const deliveryFollowUpReconciliationPath = args[12];
  const deliveryFollowUpDeliveryFollowUpPath = args[13];
  const deliveryFollowUpDeliveryFollowUpPlanPath = args[14];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath ||
    !followUpDeliveryFollowUpPath ||
    !deliveryFollowUpPlanPath ||
    !deliveryFollowUpReceiptPath ||
    !deliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpPath ||
    !deliveryFollowUpDeliveryFollowUpPlanPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, provider execution follow-up reconciliation path, provider execution follow-up delivery follow-up path, provider execution follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up reconciliation path, provider execution follow-up delivery follow-up delivery follow-up path, or provider execution follow-up delivery follow-up delivery follow-up plan path.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const followUpDeliveryFollowUp = JSON.parse(
    await readFile(followUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPlanPath, "utf8")
  );
  const sourceValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource(
      request,
      decision,
      attempt,
      sourceReceipt,
      sourceReconciliation,
      followUp,
      plan,
      followUpReceipt,
      followUpReconciliation,
      followUpDeliveryFollowUp,
      deliveryFollowUpPlan,
      deliveryFollowUpReceipt,
      deliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUp
    );
  const planValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
      deliveryFollowUpDeliveryFollowUp,
      deliveryFollowUpDeliveryFollowUpPlan
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_plan_source_contract",
      ...issue
    })),
    ...planValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_plan",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-plan",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  const followUpDeliveryFollowUpPath = args[9];
  const deliveryFollowUpPlanPath = args[10];
  const deliveryFollowUpReceiptPath = args[11];
  const deliveryFollowUpReconciliationPath = args[12];
  const deliveryFollowUpDeliveryFollowUpPath = args[13];
  const deliveryFollowUpDeliveryFollowUpPlanPath = args[14];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath ||
    !followUpDeliveryFollowUpPath ||
    !deliveryFollowUpPlanPath ||
    !deliveryFollowUpReceiptPath ||
    !deliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpPath ||
    !deliveryFollowUpDeliveryFollowUpPlanPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, provider execution follow-up reconciliation path, provider execution follow-up delivery follow-up path, provider execution follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up reconciliation path, provider execution follow-up delivery follow-up delivery follow-up path, or provider execution follow-up delivery follow-up delivery follow-up plan path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const followUpDeliveryFollowUp = JSON.parse(
    await readFile(followUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPlanPath, "utf8")
  );
  const sourceValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource(
      request,
      decision,
      attempt,
      sourceReceipt,
      sourceReconciliation,
      followUp,
      plan,
      followUpReceipt,
      followUpReconciliation,
      followUpDeliveryFollowUp,
      deliveryFollowUpPlan,
      deliveryFollowUpReceipt,
      deliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUp,
      deliveryFollowUpDeliveryFollowUpPlan
    );
  if (!sourceValidation.ok) {
    io.stderr(
      [
        "Cannot render provider execution follow-up delivery follow-up delivery follow-up receipt from an invalid post-delivery follow-up plan chain.",
        ...sourceValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }

  const upsertStatus = readOperationStatusFlag(args, "--upsert-status");
  const deliveryFollowUpDeliveryFollowUpReceipt =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
      deliveryFollowUpDeliveryFollowUpPlan,
      {
        executed_at: new Date().toISOString(),
        ...(upsertStatus ? { upsert_status: upsertStatus } : {})
      }
    );
  await writeFile(
    outputPath,
    `${JSON.stringify(deliveryFollowUpDeliveryFollowUpReceipt, null, 2)}\n`,
    "utf8"
  );

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-receipt",
        overall_status: deliveryFollowUpDeliveryFollowUpReceipt.overall_status,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  const followUpDeliveryFollowUpPath = args[9];
  const deliveryFollowUpPlanPath = args[10];
  const deliveryFollowUpReceiptPath = args[11];
  const deliveryFollowUpReconciliationPath = args[12];
  const deliveryFollowUpDeliveryFollowUpPath = args[13];
  const deliveryFollowUpDeliveryFollowUpPlanPath = args[14];
  const deliveryFollowUpDeliveryFollowUpReceiptPath = args[15];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath ||
    !followUpDeliveryFollowUpPath ||
    !deliveryFollowUpPlanPath ||
    !deliveryFollowUpReceiptPath ||
    !deliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpPath ||
    !deliveryFollowUpDeliveryFollowUpPlanPath ||
    !deliveryFollowUpDeliveryFollowUpReceiptPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, provider execution follow-up reconciliation path, provider execution follow-up delivery follow-up path, provider execution follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up reconciliation path, provider execution follow-up delivery follow-up delivery follow-up path, provider execution follow-up delivery follow-up delivery follow-up plan path, or provider execution follow-up delivery follow-up delivery follow-up receipt path.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const followUpDeliveryFollowUp = JSON.parse(
    await readFile(followUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpReceiptPath, "utf8")
  );
  const sourceValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource(
      request,
      decision,
      attempt,
      sourceReceipt,
      sourceReconciliation,
      followUp,
      plan,
      followUpReceipt,
      followUpReconciliation,
      followUpDeliveryFollowUp,
      deliveryFollowUpPlan,
      deliveryFollowUpReceipt,
      deliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUp,
      deliveryFollowUpDeliveryFollowUpPlan
    );
  const receiptValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
      deliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpReceipt
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_receipt_source_contract",
      ...issue
    })),
    ...receiptValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_receipt",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-receipt",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  const followUpDeliveryFollowUpPath = args[9];
  const deliveryFollowUpPlanPath = args[10];
  const deliveryFollowUpReceiptPath = args[11];
  const deliveryFollowUpReconciliationPath = args[12];
  const deliveryFollowUpDeliveryFollowUpPath = args[13];
  const deliveryFollowUpDeliveryFollowUpPlanPath = args[14];
  const deliveryFollowUpDeliveryFollowUpReceiptPath = args[15];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath ||
    !followUpDeliveryFollowUpPath ||
    !deliveryFollowUpPlanPath ||
    !deliveryFollowUpReceiptPath ||
    !deliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpPath ||
    !deliveryFollowUpDeliveryFollowUpPlanPath ||
    !deliveryFollowUpDeliveryFollowUpReceiptPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, provider execution follow-up reconciliation path, provider execution follow-up delivery follow-up path, provider execution follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up reconciliation path, provider execution follow-up delivery follow-up delivery follow-up path, provider execution follow-up delivery follow-up delivery follow-up plan path, or provider execution follow-up delivery follow-up delivery follow-up receipt path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const followUpDeliveryFollowUp = JSON.parse(
    await readFile(followUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpReceiptPath, "utf8")
  );
  const sourceValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource(
      request,
      decision,
      attempt,
      sourceReceipt,
      sourceReconciliation,
      followUp,
      plan,
      followUpReceipt,
      followUpReconciliation,
      followUpDeliveryFollowUp,
      deliveryFollowUpPlan,
      deliveryFollowUpReceipt,
      deliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUp,
      deliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpReceipt
    );
  if (!sourceValidation.ok) {
    io.stderr(
      [
        "Cannot render provider execution follow-up delivery follow-up delivery follow-up reconciliation from an invalid post-delivery receipt chain.",
        ...sourceValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }

  const deliveryFollowUpDeliveryFollowUpReconciliation =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
      deliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpReceipt
    );
  await writeFile(
    outputPath,
    `${JSON.stringify(deliveryFollowUpDeliveryFollowUpReconciliation, null, 2)}\n`,
    "utf8"
  );

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-reconciliation",
        reconciliation_status:
          deliveryFollowUpDeliveryFollowUpReconciliation.reconciliation_status,
        recommended_follow_up:
          deliveryFollowUpDeliveryFollowUpReconciliation.recommended_follow_up,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  const followUpDeliveryFollowUpPath = args[9];
  const deliveryFollowUpPlanPath = args[10];
  const deliveryFollowUpReceiptPath = args[11];
  const deliveryFollowUpReconciliationPath = args[12];
  const deliveryFollowUpDeliveryFollowUpPath = args[13];
  const deliveryFollowUpDeliveryFollowUpPlanPath = args[14];
  const deliveryFollowUpDeliveryFollowUpReceiptPath = args[15];
  const deliveryFollowUpDeliveryFollowUpReconciliationPath = args[16];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath ||
    !followUpDeliveryFollowUpPath ||
    !deliveryFollowUpPlanPath ||
    !deliveryFollowUpReceiptPath ||
    !deliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpPath ||
    !deliveryFollowUpDeliveryFollowUpPlanPath ||
    !deliveryFollowUpDeliveryFollowUpReceiptPath ||
    !deliveryFollowUpDeliveryFollowUpReconciliationPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, provider execution follow-up reconciliation path, provider execution follow-up delivery follow-up path, provider execution follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up reconciliation path, provider execution follow-up delivery follow-up delivery follow-up path, provider execution follow-up delivery follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up delivery follow-up receipt path, or provider execution follow-up delivery follow-up delivery follow-up reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const followUpDeliveryFollowUp = JSON.parse(
    await readFile(followUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpReconciliationPath, "utf8")
  );
  const sourceValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource(
      request,
      decision,
      attempt,
      sourceReceipt,
      sourceReconciliation,
      followUp,
      plan,
      followUpReceipt,
      followUpReconciliation,
      followUpDeliveryFollowUp,
      deliveryFollowUpPlan,
      deliveryFollowUpReceipt,
      deliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUp,
      deliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpReceipt
    );
  const reconciliationValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
      deliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpReceipt,
      deliveryFollowUpDeliveryFollowUpReconciliation
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_reconciliation_source_contract",
      ...issue
    })),
    ...reconciliationValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_reconciliation",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-reconciliation",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  const followUpDeliveryFollowUpPath = args[9];
  const deliveryFollowUpPlanPath = args[10];
  const deliveryFollowUpReceiptPath = args[11];
  const deliveryFollowUpReconciliationPath = args[12];
  const deliveryFollowUpDeliveryFollowUpPath = args[13];
  const deliveryFollowUpDeliveryFollowUpPlanPath = args[14];
  const deliveryFollowUpDeliveryFollowUpReceiptPath = args[15];
  const deliveryFollowUpDeliveryFollowUpReconciliationPath = args[16];
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath = args[17];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath ||
    !followUpDeliveryFollowUpPath ||
    !deliveryFollowUpPlanPath ||
    !deliveryFollowUpReceiptPath ||
    !deliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpPath ||
    !deliveryFollowUpDeliveryFollowUpPlanPath ||
    !deliveryFollowUpDeliveryFollowUpReceiptPath ||
    !deliveryFollowUpDeliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, provider execution follow-up reconciliation path, provider execution follow-up delivery follow-up path, provider execution follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up reconciliation path, provider execution follow-up delivery follow-up delivery follow-up path, provider execution follow-up delivery follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up delivery follow-up reconciliation path, or provider execution follow-up delivery follow-up delivery follow-up delivery follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const followUpDeliveryFollowUp = JSON.parse(
    await readFile(followUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const sourceValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource(
      request,
      decision,
      attempt,
      sourceReceipt,
      sourceReconciliation,
      followUp,
      plan,
      followUpReceipt,
      followUpReconciliation,
      followUpDeliveryFollowUp,
      deliveryFollowUpPlan,
      deliveryFollowUpReceipt,
      deliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUp,
      deliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpReceipt,
      deliveryFollowUpDeliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUp
    );
  if (!sourceValidation.ok) {
    io.stderr(
      [
        "Cannot render provider execution follow-up delivery follow-up delivery follow-up plan from an invalid post-delivery routing chain.",
        ...sourceValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }

  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUp
    );
  await writeFile(
    outputPath,
    `${JSON.stringify(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan, null, 2)}\n`,
    "utf8"
  );

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan",
        follow_up_state: deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan.follow_up_state,
        delivery_action: deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan.delivery_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  const followUpDeliveryFollowUpPath = args[9];
  const deliveryFollowUpPlanPath = args[10];
  const deliveryFollowUpReceiptPath = args[11];
  const deliveryFollowUpReconciliationPath = args[12];
  const deliveryFollowUpDeliveryFollowUpPath = args[13];
  const deliveryFollowUpDeliveryFollowUpPlanPath = args[14];
  const deliveryFollowUpDeliveryFollowUpReceiptPath = args[15];
  const deliveryFollowUpDeliveryFollowUpReconciliationPath = args[16];
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath = args[17];
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanPath = args[18];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath ||
    !followUpDeliveryFollowUpPath ||
    !deliveryFollowUpPlanPath ||
    !deliveryFollowUpReceiptPath ||
    !deliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpPath ||
    !deliveryFollowUpDeliveryFollowUpPlanPath ||
    !deliveryFollowUpDeliveryFollowUpReceiptPath ||
    !deliveryFollowUpDeliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath ||
    !deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, provider execution follow-up reconciliation path, provider execution follow-up delivery follow-up path, provider execution follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up reconciliation path, provider execution follow-up delivery follow-up delivery follow-up path, provider execution follow-up delivery follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up delivery follow-up reconciliation path, provider execution follow-up delivery follow-up delivery follow-up delivery follow-up path, or provider execution follow-up delivery follow-up delivery follow-up delivery follow-up plan path.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const followUpDeliveryFollowUp = JSON.parse(
    await readFile(followUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanPath, "utf8")
  );
  const sourceValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource(
      request,
      decision,
      attempt,
      sourceReceipt,
      sourceReconciliation,
      followUp,
      plan,
      followUpReceipt,
      followUpReconciliation,
      followUpDeliveryFollowUp,
      deliveryFollowUpPlan,
      deliveryFollowUpReceipt,
      deliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUp,
      deliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpReceipt,
      deliveryFollowUpDeliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUp
    );
  const planValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_plan_source_contract",
      ...issue
    })),
    ...planValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_plan",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  const followUpDeliveryFollowUpPath = args[9];
  const deliveryFollowUpPlanPath = args[10];
  const deliveryFollowUpReceiptPath = args[11];
  const deliveryFollowUpReconciliationPath = args[12];
  const deliveryFollowUpDeliveryFollowUpPath = args[13];
  const deliveryFollowUpDeliveryFollowUpPlanPath = args[14];
  const deliveryFollowUpDeliveryFollowUpReceiptPath = args[15];
  const deliveryFollowUpDeliveryFollowUpReconciliationPath = args[16];
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath = args[17];
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanPath = args[18];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath ||
    !followUpDeliveryFollowUpPath ||
    !deliveryFollowUpPlanPath ||
    !deliveryFollowUpReceiptPath ||
    !deliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpPath ||
    !deliveryFollowUpDeliveryFollowUpPlanPath ||
    !deliveryFollowUpDeliveryFollowUpReceiptPath ||
    !deliveryFollowUpDeliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath ||
    !deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, provider execution follow-up reconciliation path, provider execution follow-up delivery follow-up path, provider execution follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up reconciliation path, provider execution follow-up delivery follow-up delivery follow-up path, provider execution follow-up delivery follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up delivery follow-up reconciliation path, provider execution follow-up delivery follow-up delivery follow-up delivery follow-up path, or provider execution follow-up delivery follow-up delivery follow-up delivery follow-up plan path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const followUpDeliveryFollowUp = JSON.parse(
    await readFile(followUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanPath, "utf8")
  );
  const sourceValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource(
      request,
      decision,
      attempt,
      sourceReceipt,
      sourceReconciliation,
      followUp,
      plan,
      followUpReceipt,
      followUpReconciliation,
      followUpDeliveryFollowUp,
      deliveryFollowUpPlan,
      deliveryFollowUpReceipt,
      deliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUp,
      deliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpReceipt,
      deliveryFollowUpDeliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan
    );
  if (!sourceValidation.ok) {
    io.stderr(
      [
        "Cannot render provider execution follow-up delivery follow-up delivery follow-up receipt from an invalid post-delivery follow-up plan chain.",
        ...sourceValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }

  const upsertStatus = readOperationStatusFlag(args, "--upsert-status");
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
      {
        executed_at: new Date().toISOString(),
        ...(upsertStatus ? { upsert_status: upsertStatus } : {})
      }
    );
  await writeFile(
    outputPath,
    `${JSON.stringify(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt, null, 2)}\n`,
    "utf8"
  );

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt",
        overall_status: deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt.overall_status,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  const followUpDeliveryFollowUpPath = args[9];
  const deliveryFollowUpPlanPath = args[10];
  const deliveryFollowUpReceiptPath = args[11];
  const deliveryFollowUpReconciliationPath = args[12];
  const deliveryFollowUpDeliveryFollowUpPath = args[13];
  const deliveryFollowUpDeliveryFollowUpPlanPath = args[14];
  const deliveryFollowUpDeliveryFollowUpReceiptPath = args[15];
  const deliveryFollowUpDeliveryFollowUpReconciliationPath = args[16];
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath = args[17];
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanPath = args[18];
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptPath = args[19];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath ||
    !followUpDeliveryFollowUpPath ||
    !deliveryFollowUpPlanPath ||
    !deliveryFollowUpReceiptPath ||
    !deliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpPath ||
    !deliveryFollowUpDeliveryFollowUpPlanPath ||
    !deliveryFollowUpDeliveryFollowUpReceiptPath ||
    !deliveryFollowUpDeliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath ||
    !deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanPath ||
    !deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, provider execution follow-up reconciliation path, provider execution follow-up delivery follow-up path, provider execution follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up reconciliation path, provider execution follow-up delivery follow-up delivery follow-up path, provider execution follow-up delivery follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up delivery follow-up reconciliation path, provider execution follow-up delivery follow-up delivery follow-up delivery follow-up path, provider execution follow-up delivery follow-up delivery follow-up delivery follow-up plan path, or provider execution follow-up delivery follow-up delivery follow-up delivery follow-up receipt path.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const followUpDeliveryFollowUp = JSON.parse(
    await readFile(followUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptPath, "utf8")
  );
  const sourceValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource(
      request,
      decision,
      attempt,
      sourceReceipt,
      sourceReconciliation,
      followUp,
      plan,
      followUpReceipt,
      followUpReconciliation,
      followUpDeliveryFollowUp,
      deliveryFollowUpPlan,
      deliveryFollowUpReceipt,
      deliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUp,
      deliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpReceipt,
      deliveryFollowUpDeliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan
    );
  const receiptValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_receipt_source_contract",
      ...issue
    })),
    ...receiptValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_receipt",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  const followUpDeliveryFollowUpPath = args[9];
  const deliveryFollowUpPlanPath = args[10];
  const deliveryFollowUpReceiptPath = args[11];
  const deliveryFollowUpReconciliationPath = args[12];
  const deliveryFollowUpDeliveryFollowUpPath = args[13];
  const deliveryFollowUpDeliveryFollowUpPlanPath = args[14];
  const deliveryFollowUpDeliveryFollowUpReceiptPath = args[15];
  const deliveryFollowUpDeliveryFollowUpReconciliationPath = args[16];
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath = args[17];
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanPath = args[18];
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptPath = args[19];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath ||
    !followUpDeliveryFollowUpPath ||
    !deliveryFollowUpPlanPath ||
    !deliveryFollowUpReceiptPath ||
    !deliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpPath ||
    !deliveryFollowUpDeliveryFollowUpPlanPath ||
    !deliveryFollowUpDeliveryFollowUpReceiptPath ||
    !deliveryFollowUpDeliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath ||
    !deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanPath ||
    !deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, provider execution follow-up reconciliation path, provider execution follow-up delivery follow-up path, provider execution follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up reconciliation path, provider execution follow-up delivery follow-up delivery follow-up path, provider execution follow-up delivery follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up delivery follow-up reconciliation path, provider execution follow-up delivery follow-up delivery follow-up delivery follow-up path, provider execution follow-up delivery follow-up delivery follow-up delivery follow-up plan path, or provider execution follow-up delivery follow-up delivery follow-up delivery follow-up receipt path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const followUpDeliveryFollowUp = JSON.parse(
    await readFile(followUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptPath, "utf8")
  );
  const sourceValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource(
      request,
      decision,
      attempt,
      sourceReceipt,
      sourceReconciliation,
      followUp,
      plan,
      followUpReceipt,
      followUpReconciliation,
      followUpDeliveryFollowUp,
      deliveryFollowUpPlan,
      deliveryFollowUpReceipt,
      deliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUp,
      deliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpReceipt,
      deliveryFollowUpDeliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt
    );
  if (!sourceValidation.ok) {
    io.stderr(
      [
        "Cannot render provider execution follow-up delivery follow-up delivery follow-up reconciliation from an invalid post-delivery receipt chain.",
        ...sourceValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }

  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt
    );
  await writeFile(
    outputPath,
    `${JSON.stringify(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation, null, 2)}\n`,
    "utf8"
  );

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation",
        reconciliation_status:
          deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation.reconciliation_status,
        recommended_follow_up:
          deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation.recommended_follow_up,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  const followUpDeliveryFollowUpPath = args[9];
  const deliveryFollowUpPlanPath = args[10];
  const deliveryFollowUpReceiptPath = args[11];
  const deliveryFollowUpReconciliationPath = args[12];
  const deliveryFollowUpDeliveryFollowUpPath = args[13];
  const deliveryFollowUpDeliveryFollowUpPlanPath = args[14];
  const deliveryFollowUpDeliveryFollowUpReceiptPath = args[15];
  const deliveryFollowUpDeliveryFollowUpReconciliationPath = args[16];
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath = args[17];
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanPath = args[18];
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptPath = args[19];
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationPath = args[20];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath ||
    !followUpDeliveryFollowUpPath ||
    !deliveryFollowUpPlanPath ||
    !deliveryFollowUpReceiptPath ||
    !deliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpPath ||
    !deliveryFollowUpDeliveryFollowUpPlanPath ||
    !deliveryFollowUpDeliveryFollowUpReceiptPath ||
    !deliveryFollowUpDeliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath ||
    !deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanPath ||
    !deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptPath ||
    !deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, provider execution follow-up reconciliation path, provider execution follow-up delivery follow-up path, provider execution follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up reconciliation path, provider execution follow-up delivery follow-up delivery follow-up path, provider execution follow-up delivery follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up delivery follow-up reconciliation path, provider execution follow-up delivery follow-up delivery follow-up delivery follow-up path, provider execution follow-up delivery follow-up delivery follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up delivery follow-up delivery follow-up receipt path, or provider execution follow-up delivery follow-up delivery follow-up delivery follow-up reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const followUpDeliveryFollowUp = JSON.parse(
    await readFile(followUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationPath, "utf8")
  );
  const sourceValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource(
      request,
      decision,
      attempt,
      sourceReceipt,
      sourceReconciliation,
      followUp,
      plan,
      followUpReceipt,
      followUpReconciliation,
      followUpDeliveryFollowUp,
      deliveryFollowUpPlan,
      deliveryFollowUpReceipt,
      deliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUp,
      deliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpReceipt,
      deliveryFollowUpDeliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt
    );
  const reconciliationValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_reconciliation_source_contract",
      ...issue
    })),
    ...reconciliationValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_reconciliation",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}


async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  const followUpDeliveryFollowUpPath = args[9];
  const deliveryFollowUpPlanPath = args[10];
  const deliveryFollowUpReceiptPath = args[11];
  const deliveryFollowUpReconciliationPath = args[12];
  const deliveryFollowUpDeliveryFollowUpPath = args[13];
  const deliveryFollowUpDeliveryFollowUpPlanPath = args[14];
  const deliveryFollowUpDeliveryFollowUpReceiptPath = args[15];
  const deliveryFollowUpDeliveryFollowUpReconciliationPath = args[16];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath ||
    !followUpDeliveryFollowUpPath ||
    !deliveryFollowUpPlanPath ||
    !deliveryFollowUpReceiptPath ||
    !deliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpPath ||
    !deliveryFollowUpDeliveryFollowUpPlanPath ||
    !deliveryFollowUpDeliveryFollowUpReceiptPath ||
    !deliveryFollowUpDeliveryFollowUpReconciliationPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, provider execution follow-up reconciliation path, provider execution follow-up delivery follow-up path, provider execution follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up reconciliation path, provider execution follow-up delivery follow-up delivery follow-up path, provider execution follow-up delivery follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up delivery follow-up receipt path, or provider execution follow-up delivery follow-up delivery follow-up reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(
    args,
    "--out"
  );
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const followUpDeliveryFollowUp = JSON.parse(
    await readFile(followUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpReconciliationPath, "utf8")
  );
  const sourceValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource(
      request,
      decision,
      attempt,
      sourceReceipt,
      sourceReconciliation,
      followUp,
      plan,
      followUpReceipt,
      followUpReconciliation,
      followUpDeliveryFollowUp,
      deliveryFollowUpPlan,
      deliveryFollowUpReceipt,
      deliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUp,
      deliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpReceipt,
      deliveryFollowUpDeliveryFollowUpReconciliation
    );
  if (!sourceValidation.ok) {
    io.stderr(
      [
        "Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up from an invalid downstream provider executor chain.",
        ...sourceValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }

  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUp =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
      deliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpReconciliation
    );
  await writeFile(
    outputPath,
    `${JSON.stringify(deliveryFollowUpDeliveryFollowUpDeliveryFollowUp, null, 2)}\n`,
    "utf8"
  );

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up",
        follow_up_state: deliveryFollowUpDeliveryFollowUpDeliveryFollowUp.follow_up_state,
        follow_up_action: deliveryFollowUpDeliveryFollowUpDeliveryFollowUp.follow_up_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  const followUpDeliveryFollowUpPath = args[9];
  const deliveryFollowUpPlanPath = args[10];
  const deliveryFollowUpReceiptPath = args[11];
  const deliveryFollowUpReconciliationPath = args[12];
  const deliveryFollowUpDeliveryFollowUpPath = args[13];
  const deliveryFollowUpDeliveryFollowUpPlanPath = args[14];
  const deliveryFollowUpDeliveryFollowUpReceiptPath = args[15];
  const deliveryFollowUpDeliveryFollowUpReconciliationPath = args[16];
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath = args[17];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath ||
    !followUpDeliveryFollowUpPath ||
    !deliveryFollowUpPlanPath ||
    !deliveryFollowUpReceiptPath ||
    !deliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpPath ||
    !deliveryFollowUpDeliveryFollowUpPlanPath ||
    !deliveryFollowUpDeliveryFollowUpReceiptPath ||
    !deliveryFollowUpDeliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, provider execution follow-up reconciliation path, provider execution follow-up delivery follow-up path, provider execution follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up reconciliation path, provider execution follow-up delivery follow-up delivery follow-up path, provider execution follow-up delivery follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up delivery follow-up reconciliation path, or provider execution follow-up delivery follow-up delivery follow-up delivery follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const followUpDeliveryFollowUp = JSON.parse(
    await readFile(followUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const sourceValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource(
      request,
      decision,
      attempt,
      sourceReceipt,
      sourceReconciliation,
      followUp,
      plan,
      followUpReceipt,
      followUpReconciliation,
      followUpDeliveryFollowUp,
      deliveryFollowUpPlan,
      deliveryFollowUpReceipt,
      deliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUp,
      deliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpReceipt,
      deliveryFollowUpDeliveryFollowUpReconciliation
    );
  const followUpValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
      deliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUp
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_source_contract",
      ...issue
    })),
    ...followUpValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  const followUpDeliveryFollowUpPath = args[9];
  const deliveryFollowUpPlanPath = args[10];
  const deliveryFollowUpReceiptPath = args[11];
  const deliveryFollowUpReconciliationPath = args[12];
  const deliveryFollowUpDeliveryFollowUpPath = args[13];
  const deliveryFollowUpDeliveryFollowUpPlanPath = args[14];
  const deliveryFollowUpDeliveryFollowUpReceiptPath = args[15];
  const deliveryFollowUpDeliveryFollowUpReconciliationPath = args[16];
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath = args[17];
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanPath = args[18];
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptPath = args[19];
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationPath = args[20];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath ||
    !followUpDeliveryFollowUpPath ||
    !deliveryFollowUpPlanPath ||
    !deliveryFollowUpReceiptPath ||
    !deliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpPath ||
    !deliveryFollowUpDeliveryFollowUpPlanPath ||
    !deliveryFollowUpDeliveryFollowUpReceiptPath ||
    !deliveryFollowUpDeliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath ||
    !deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanPath ||
    !deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptPath ||
    !deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, provider execution follow-up reconciliation path, provider execution follow-up delivery follow-up path, provider execution follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up reconciliation path, provider execution follow-up delivery follow-up delivery follow-up path, provider execution follow-up delivery follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up delivery follow-up reconciliation path, provider execution follow-up delivery follow-up delivery follow-up delivery follow-up path, provider execution follow-up delivery follow-up delivery follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up delivery follow-up delivery follow-up receipt path, or provider execution follow-up delivery follow-up delivery follow-up delivery follow-up reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const followUpDeliveryFollowUp = JSON.parse(
    await readFile(followUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationPath, "utf8")
  );
  const sourceValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource(
      request,
      decision,
      attempt,
      sourceReceipt,
      sourceReconciliation,
      followUp,
      plan,
      followUpReceipt,
      followUpReconciliation,
      followUpDeliveryFollowUp,
      deliveryFollowUpPlan,
      deliveryFollowUpReceipt,
      deliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUp,
      deliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpReceipt,
      deliveryFollowUpDeliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation
    );
  if (!sourceValidation.ok) {
    io.stderr(
      [
        "Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up from an invalid downstream provider post-delivery executor chain.",
        ...sourceValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }

  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation
    );
  await writeFile(
    outputPath,
    `${JSON.stringify(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp, null, 2)}\n`,
    "utf8"
  );

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up",
        follow_up_state:
          deliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp.follow_up_state,
        follow_up_action:
          deliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp.follow_up_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const requestPath = args[0];
  const decisionPath = args[1];
  const attemptPath = args[2];
  const sourceReceiptPath = args[3];
  const sourceReconciliationPath = args[4];
  const followUpPath = args[5];
  const planPath = args[6];
  const followUpReceiptPath = args[7];
  const followUpReconciliationPath = args[8];
  const followUpDeliveryFollowUpPath = args[9];
  const deliveryFollowUpPlanPath = args[10];
  const deliveryFollowUpReceiptPath = args[11];
  const deliveryFollowUpReconciliationPath = args[12];
  const deliveryFollowUpDeliveryFollowUpPath = args[13];
  const deliveryFollowUpDeliveryFollowUpPlanPath = args[14];
  const deliveryFollowUpDeliveryFollowUpReceiptPath = args[15];
  const deliveryFollowUpDeliveryFollowUpReconciliationPath = args[16];
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath = args[17];
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanPath = args[18];
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptPath = args[19];
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationPath = args[20];
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath = args[21];
  if (
    !requestPath ||
    !decisionPath ||
    !attemptPath ||
    !sourceReceiptPath ||
    !sourceReconciliationPath ||
    !followUpPath ||
    !planPath ||
    !followUpReceiptPath ||
    !followUpReconciliationPath ||
    !followUpDeliveryFollowUpPath ||
    !deliveryFollowUpPlanPath ||
    !deliveryFollowUpReceiptPath ||
    !deliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpPath ||
    !deliveryFollowUpDeliveryFollowUpPlanPath ||
    !deliveryFollowUpDeliveryFollowUpReceiptPath ||
    !deliveryFollowUpDeliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath ||
    !deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanPath ||
    !deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptPath ||
    !deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationPath ||
    !deliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath
  ) {
    io.stderr("Missing provider execution request path, provider execution decision path, provider execution attempt path, provider execution receipt path, provider execution reconciliation path, provider execution follow-up path, provider execution follow-up plan path, provider execution follow-up receipt path, provider execution follow-up reconciliation path, provider execution follow-up delivery follow-up path, provider execution follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up reconciliation path, provider execution follow-up delivery follow-up delivery follow-up path, provider execution follow-up delivery follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up delivery follow-up reconciliation path, provider execution follow-up delivery follow-up delivery follow-up delivery follow-up path, provider execution follow-up delivery follow-up delivery follow-up delivery follow-up plan path, provider execution follow-up delivery follow-up delivery follow-up delivery follow-up receipt path, provider execution follow-up delivery follow-up delivery follow-up delivery follow-up reconciliation path, or provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const request = JSON.parse(await readFile(requestPath, "utf8"));
  const decision = JSON.parse(await readFile(decisionPath, "utf8"));
  const attempt = JSON.parse(await readFile(attemptPath, "utf8"));
  const sourceReceipt = JSON.parse(await readFile(sourceReceiptPath, "utf8"));
  const sourceReconciliation = JSON.parse(await readFile(sourceReconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const followUpReceipt = JSON.parse(await readFile(followUpReceiptPath, "utf8"));
  const followUpReconciliation = JSON.parse(
    await readFile(followUpReconciliationPath, "utf8")
  );
  const followUpDeliveryFollowUp = JSON.parse(
    await readFile(followUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationPath, "utf8")
  );
  const deliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp = JSON.parse(
    await readFile(deliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPath, "utf8")
  );
  const sourceValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource(
      request,
      decision,
      attempt,
      sourceReceipt,
      sourceReconciliation,
      followUp,
      plan,
      followUpReceipt,
      followUpReconciliation,
      followUpDeliveryFollowUp,
      deliveryFollowUpPlan,
      deliveryFollowUpReceipt,
      deliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUp,
      deliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpReceipt,
      deliveryFollowUpDeliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUp,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation
    );
  const followUpValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation,
      deliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_source_contract",
      ...issue
    })),
    ...followUpValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 22);
  if (!sourcePaths) {
    io.stderr("Missing source chain paths for provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up plan.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource,
      source
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up plan from an invalid routing chain.", sourceValidation.issues));
    return 1;
  }

  const followUp = source[21];
  const plan =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
      followUp
    );
  await writeFile(outputPath, `${JSON.stringify(plan, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan",
        follow_up_state: plan.follow_up_state,
        delivery_action: plan.delivery_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 22);
  const planPath = args[22];
  if (!sourcePaths || !planPath) {
    io.stderr("Missing source chain paths or provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up plan path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource,
      source
    );
  const planValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
      source[21],
      plan
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_plan_source_contract",
      ...issue
    })),
    ...planValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_plan",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 22);
  const planPath = args[22];
  if (!sourcePaths || !planPath) {
    io.stderr("Missing source chain paths or provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up plan path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource,
      [...source, plan]
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up receipt from an invalid plan source chain.", sourceValidation.issues));
    return 1;
  }

  const upsertStatus = readOperationStatusFlag(args, "--upsert-status");
  const receipt =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
      plan,
      {
        executed_at: readFlagValue(args, "--executed-at") ?? new Date().toISOString(),
        ...(upsertStatus ? { upsert_status: upsertStatus } : {})
      }
    );
  await writeFile(outputPath, `${JSON.stringify(receipt, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt",
        overall_status: receipt.overall_status,
        operation_count: receipt.operations.length,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 22);
  const planPath = args[22];
  const receiptPath = args[23];
  if (!sourcePaths || !planPath || !receiptPath) {
    io.stderr("Missing source chain paths, provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource,
      [...source, plan]
    );
  const receiptValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
      plan,
      receipt
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_receipt_source_contract",
      ...issue
    })),
    ...receiptValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_receipt",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 22);
  const planPath = args[22];
  const receiptPath = args[23];
  if (!sourcePaths || !planPath || !receiptPath) {
    io.stderr("Missing source chain paths, provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource,
      [...source, plan, receipt]
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up reconciliation from an invalid receipt source chain.", sourceValidation.issues));
    return 1;
  }

  const reconciliation =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
      plan,
      receipt
    );
  await writeFile(outputPath, `${JSON.stringify(reconciliation, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation",
        reconciliation_status: reconciliation.reconciliation_status,
        recommended_follow_up: reconciliation.recommended_follow_up,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 22);
  const planPath = args[22];
  const receiptPath = args[23];
  const reconciliationPath = args[24];
  if (!sourcePaths || !planPath || !receiptPath || !reconciliationPath) {
    io.stderr("Missing source chain paths, provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up plan path, receipt path, or reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const reconciliation = JSON.parse(await readFile(reconciliationPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource,
      [...source, plan, receipt]
    );
  const reconciliationValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
      plan,
      receipt,
      reconciliation
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_reconciliation_source_contract",
      ...issue
    })),
    ...reconciliationValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_reconciliation",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 25);
  if (!sourcePaths) {
    io.stderr("Missing latest provider post-delivery executor source chain paths.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource,
    sourceObjects
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up from an invalid latest downstream provider post-delivery executor chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const followUp =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
      sourceObjects[22],
      sourceObjects[24]
    );
  await writeFile(outputPath, `${JSON.stringify(followUp, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up",
        follow_up_state: followUp.follow_up_state,
        follow_up_action: followUp.follow_up_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, 26);
  if (!paths) {
    io.stderr("Missing latest provider post-delivery executor source chain path or follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, 25));
  const followUp = JSON.parse(await readFile(paths[25]!, "utf8"));
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource,
    sourceObjects
  );
  const followUpValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
      sourceObjects[22],
      sourceObjects[24],
      followUp
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_source_contract",
      ...issue
    })),
    ...followUpValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 26);
  if (!sourcePaths) {
    io.stderr("Missing latest provider post-delivery routing source chain paths.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource,
      source
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up plan from an invalid routing chain.", sourceValidation.issues));
    return 1;
  }

  const followUp = source[25];
  const plan =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
      followUp
    );
  await writeFile(outputPath, `${JSON.stringify(plan, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan",
        follow_up_state: plan.follow_up_state,
        delivery_action: plan.delivery_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 26);
  const planPath = args[26];
  if (!sourcePaths || !planPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths or plan path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource,
      source
    );
  const planValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
      source[25],
      plan
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_plan_source_contract",
      ...issue
    })),
    ...planValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_plan",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 26);
  const planPath = args[26];
  if (!sourcePaths || !planPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths or plan path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource,
      [...source, plan]
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up receipt from an invalid plan source chain.", sourceValidation.issues));
    return 1;
  }

  const upsertStatus = readOperationStatusFlag(args, "--upsert-status");
  const receipt =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
      plan,
      {
        executed_at: readFlagValue(args, "--executed-at") ?? new Date().toISOString(),
        ...(upsertStatus ? { upsert_status: upsertStatus } : {})
      }
    );
  await writeFile(outputPath, `${JSON.stringify(receipt, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt",
        overall_status: receipt.overall_status,
        operation_count: receipt.operations.length,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 26);
  const planPath = args[26];
  const receiptPath = args[27];
  if (!sourcePaths || !planPath || !receiptPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource,
      [...source, plan]
    );
  const receiptValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
      plan,
      receipt
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_receipt_source_contract",
      ...issue
    })),
    ...receiptValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_receipt",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 26);
  const planPath = args[26];
  const receiptPath = args[27];
  if (!sourcePaths || !planPath || !receiptPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource,
      [...source, plan, receipt]
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up reconciliation from an invalid receipt source chain.", sourceValidation.issues));
    return 1;
  }

  const reconciliation =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
      plan,
      receipt
    );
  await writeFile(outputPath, `${JSON.stringify(reconciliation, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation",
        reconciliation_status: reconciliation.reconciliation_status,
        recommended_follow_up: reconciliation.recommended_follow_up,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 26);
  const planPath = args[26];
  const receiptPath = args[27];
  const reconciliationPath = args[28];
  if (!sourcePaths || !planPath || !receiptPath || !reconciliationPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths, plan path, receipt path, or reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const reconciliation = JSON.parse(await readFile(reconciliationPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource,
      [...source, plan, receipt]
    );
  const reconciliationValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
      plan,
      receipt,
      reconciliation
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_reconciliation_source_contract",
      ...issue
    })),
    ...reconciliationValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_reconciliation",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 29);
  if (!sourcePaths) {
    io.stderr("Missing latest downstream provider post-delivery executor-ready trio source chain paths.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource,
    sourceObjects
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render latest downstream provider post-delivery closure/follow-up routing from an invalid executor-ready trio source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const followUp =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
      sourceObjects[26],
      sourceObjects[28]
    );
  await writeFile(outputPath, `${JSON.stringify(followUp, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up",
        follow_up_state: followUp.follow_up_state,
        follow_up_action: followUp.follow_up_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, 30);
  if (!paths) {
    io.stderr("Missing latest downstream provider post-delivery executor-ready trio source chain path or follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, 29));
  const followUp = JSON.parse(await readFile(paths[29]!, "utf8"));
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource,
    sourceObjects
  );
  const followUpValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
      sourceObjects[26],
      sourceObjects[28],
      followUp
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_source_contract",
      ...issue
    })),
    ...followUpValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 30);
  if (!sourcePaths) {
    io.stderr("Missing latest provider post-delivery routing source chain paths.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource,
      source
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up plan from an invalid routing chain.", sourceValidation.issues));
    return 1;
  }

  const followUp = source[29];
  const plan =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
      followUp
    );
  await writeFile(outputPath, `${JSON.stringify(plan, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan",
        follow_up_state: plan.follow_up_state,
        delivery_action: plan.delivery_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 30);
  const planPath = args[30];
  if (!sourcePaths || !planPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths or plan path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource,
      source
    );
  const planValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
      source[29],
      plan
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_plan_source_contract",
      ...issue
    })),
    ...planValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_plan",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 30);
  const planPath = args[30];
  if (!sourcePaths || !planPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths or plan path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource,
      [...source, plan]
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up receipt from an invalid plan source chain.", sourceValidation.issues));
    return 1;
  }

  const upsertStatus = readOperationStatusFlag(args, "--upsert-status");
  const receipt =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
      plan,
      {
        executed_at: readFlagValue(args, "--executed-at") ?? new Date().toISOString(),
        ...(upsertStatus ? { upsert_status: upsertStatus } : {})
      }
    );
  await writeFile(outputPath, `${JSON.stringify(receipt, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt",
        overall_status: receipt.overall_status,
        operation_count: receipt.operations.length,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 30);
  const planPath = args[30];
  const receiptPath = args[31];
  if (!sourcePaths || !planPath || !receiptPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource,
      [...source, plan]
    );
  const receiptValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
      plan,
      receipt
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_receipt_source_contract",
      ...issue
    })),
    ...receiptValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_receipt",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 30);
  const planPath = args[30];
  const receiptPath = args[31];
  if (!sourcePaths || !planPath || !receiptPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource,
      [...source, plan, receipt]
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up reconciliation from an invalid receipt source chain.", sourceValidation.issues));
    return 1;
  }

  const reconciliation =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
      plan,
      receipt
    );
  await writeFile(outputPath, `${JSON.stringify(reconciliation, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation",
        reconciliation_status: reconciliation.reconciliation_status,
        recommended_follow_up: reconciliation.recommended_follow_up,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 30);
  const planPath = args[30];
  const receiptPath = args[31];
  const reconciliationPath = args[32];
  if (!sourcePaths || !planPath || !receiptPath || !reconciliationPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths, plan path, receipt path, or reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const reconciliation = JSON.parse(await readFile(reconciliationPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource,
      [...source, plan, receipt]
    );
  const reconciliationValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
      plan,
      receipt,
      reconciliation
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_reconciliation_source_contract",
      ...issue
    })),
    ...reconciliationValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_reconciliation",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 33);
  if (!sourcePaths) {
    io.stderr("Missing latest downstream provider post-delivery executor-ready trio source chain paths.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource,
    sourceObjects
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render latest downstream provider post-delivery closure/follow-up routing from an invalid executor-ready trio source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const followUp =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
      sourceObjects[30],
      sourceObjects[32]
    );
  await writeFile(outputPath, `${JSON.stringify(followUp, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up",
        follow_up_state: followUp.follow_up_state,
        follow_up_action: followUp.follow_up_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, 34);
  if (!paths) {
    io.stderr("Missing latest downstream provider post-delivery executor-ready trio source chain path or follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, 33));
  const followUp = JSON.parse(await readFile(paths[33]!, "utf8"));
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource,
    sourceObjects
  );
  const followUpValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
      sourceObjects[30],
      sourceObjects[32],
      followUp
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_source_contract",
      ...issue
    })),
    ...followUpValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}


async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 37);
  if (!sourcePaths) {
    io.stderr("Missing newest downstream provider post-delivery executor-ready trio source chain paths.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource,
    sourceObjects
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render newest downstream provider post-delivery closure/follow-up routing from an invalid executor-ready trio source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const followUp =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
      sourceObjects[34],
      sourceObjects[36]
    );
  await writeFile(outputPath, `${JSON.stringify(followUp, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up",
        follow_up_state: followUp.follow_up_state,
        follow_up_action: followUp.follow_up_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, 38);
  if (!paths) {
    io.stderr("Missing newest downstream provider post-delivery executor-ready trio source chain path or follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, 37));
  const followUp = JSON.parse(await readFile(paths[37]!, "utf8"));
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource,
    sourceObjects
  );
  const followUpValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
      sourceObjects[34],
      sourceObjects[36],
      followUp
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_source_contract",
      ...issue
    })),
    ...followUpValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}


async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 34);
  if (!sourcePaths) {
    io.stderr("Missing latest provider post-delivery routing source chain paths.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource,
      source
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up plan from an invalid routing chain.", sourceValidation.issues));
    return 1;
  }

  const followUp = source[33];
  const plan =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
      followUp
    );
  await writeFile(outputPath, `${JSON.stringify(plan, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan",
        follow_up_state: plan.follow_up_state,
        delivery_action: plan.delivery_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 34);
  const planPath = args[34];
  if (!sourcePaths || !planPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths or plan path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource,
      source
    );
  const planValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
      source[33],
      plan
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_plan_source_contract",
      ...issue
    })),
    ...planValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_plan",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 34);
  const planPath = args[34];
  if (!sourcePaths || !planPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths or plan path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource,
      [...source, plan]
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up receipt from an invalid plan source chain.", sourceValidation.issues));
    return 1;
  }

  const upsertStatus = readOperationStatusFlag(args, "--upsert-status");
  const receipt =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
      plan,
      {
        executed_at: readFlagValue(args, "--executed-at") ?? new Date().toISOString(),
        ...(upsertStatus ? { upsert_status: upsertStatus } : {})
      }
    );
  await writeFile(outputPath, `${JSON.stringify(receipt, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt",
        overall_status: receipt.overall_status,
        operation_count: receipt.operations.length,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 34);
  const planPath = args[34];
  const receiptPath = args[35];
  if (!sourcePaths || !planPath || !receiptPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource,
      [...source, plan]
    );
  const receiptValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
      plan,
      receipt
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_receipt_source_contract",
      ...issue
    })),
    ...receiptValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_receipt",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 34);
  const planPath = args[34];
  const receiptPath = args[35];
  if (!sourcePaths || !planPath || !receiptPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource,
      [...source, plan, receipt]
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up reconciliation from an invalid receipt source chain.", sourceValidation.issues));
    return 1;
  }

  const reconciliation =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
      plan,
      receipt
    );
  await writeFile(outputPath, `${JSON.stringify(reconciliation, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation",
        reconciliation_status: reconciliation.reconciliation_status,
        recommended_follow_up: reconciliation.recommended_follow_up,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 34);
  const planPath = args[34];
  const receiptPath = args[35];
  const reconciliationPath = args[36];
  if (!sourcePaths || !planPath || !receiptPath || !reconciliationPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths, plan path, receipt path, or reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const reconciliation = JSON.parse(await readFile(reconciliationPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource,
      [...source, plan, receipt]
    );
  const reconciliationValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
      plan,
      receipt,
      reconciliation
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_reconciliation_source_contract",
      ...issue
    })),
    ...reconciliationValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_reconciliation",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}


async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 41);
  if (!sourcePaths) {
    io.stderr("Missing newest downstream provider post-delivery executor-ready trio source chain paths.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource,
    sourceObjects
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render newest downstream provider post-delivery closure/follow-up routing from an invalid executor-ready trio source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const followUp =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
      sourceObjects[38],
      sourceObjects[40]
    );
  await writeFile(outputPath, `${JSON.stringify(followUp, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up",
        follow_up_state: followUp.follow_up_state,
        follow_up_action: followUp.follow_up_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, 42);
  if (!paths) {
    io.stderr("Missing newest downstream provider post-delivery executor-ready trio source chain path or follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, 41));
  const followUp = JSON.parse(await readFile(paths[41]!, "utf8"));
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource,
    sourceObjects
  );
  const followUpValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
      sourceObjects[38],
      sourceObjects[40],
      followUp
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_source_contract",
      ...issue
    })),
    ...followUpValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}


async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 45);
  if (!sourcePaths) {
    io.stderr("Missing newest downstream provider post-delivery executor-ready trio source chain paths.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource,
    sourceObjects
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render newest downstream provider post-delivery closure/follow-up routing from an invalid executor-ready trio source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const followUp =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
      sourceObjects[42],
      sourceObjects[44]
    );
  await writeFile(outputPath, `${JSON.stringify(followUp, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up",
        follow_up_state: followUp.follow_up_state,
        follow_up_action: followUp.follow_up_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, 46);
  if (!paths) {
    io.stderr("Missing newest downstream provider post-delivery executor-ready trio source chain path or follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, 45));
  const followUp = JSON.parse(await readFile(paths[45]!, "utf8"));
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource,
    sourceObjects
  );
  const followUpValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
      sourceObjects[42],
      sourceObjects[44],
      followUp
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_source_contract",
      ...issue
    })),
    ...followUpValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}


async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 38);
  if (!sourcePaths) {
    io.stderr("Missing latest provider post-delivery routing source chain paths.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource,
      source
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up plan from an invalid routing chain.", sourceValidation.issues));
    return 1;
  }

  const followUp = source[37];
  const plan =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
      followUp
    );
  await writeFile(outputPath, `${JSON.stringify(plan, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan",
        follow_up_state: plan.follow_up_state,
        delivery_action: plan.delivery_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 38);
  const planPath = args[38];
  if (!sourcePaths || !planPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths or plan path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource,
      source
    );
  const planValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
      source[37],
      plan
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_plan_source_contract",
      ...issue
    })),
    ...planValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_plan",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 38);
  const planPath = args[38];
  if (!sourcePaths || !planPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths or plan path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource,
      [...source, plan]
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up receipt from an invalid plan source chain.", sourceValidation.issues));
    return 1;
  }

  const upsertStatus = readOperationStatusFlag(args, "--upsert-status");
  const receipt =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
      plan,
      {
        executed_at: readFlagValue(args, "--executed-at") ?? new Date().toISOString(),
        ...(upsertStatus ? { upsert_status: upsertStatus } : {})
      }
    );
  await writeFile(outputPath, `${JSON.stringify(receipt, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt",
        overall_status: receipt.overall_status,
        operation_count: receipt.operations.length,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 38);
  const planPath = args[38];
  const receiptPath = args[39];
  if (!sourcePaths || !planPath || !receiptPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource,
      [...source, plan]
    );
  const receiptValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
      plan,
      receipt
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_receipt_source_contract",
      ...issue
    })),
    ...receiptValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_receipt",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 38);
  const planPath = args[38];
  const receiptPath = args[39];
  if (!sourcePaths || !planPath || !receiptPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource,
      [...source, plan, receipt]
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up reconciliation from an invalid receipt source chain.", sourceValidation.issues));
    return 1;
  }

  const reconciliation =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
      plan,
      receipt
    );
  await writeFile(outputPath, `${JSON.stringify(reconciliation, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation",
        reconciliation_status: reconciliation.reconciliation_status,
        recommended_follow_up: reconciliation.recommended_follow_up,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 38);
  const planPath = args[38];
  const receiptPath = args[39];
  const reconciliationPath = args[40];
  if (!sourcePaths || !planPath || !receiptPath || !reconciliationPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths, plan path, receipt path, or reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const reconciliation = JSON.parse(await readFile(reconciliationPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource,
      [...source, plan, receipt]
    );
  const reconciliationValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
      plan,
      receipt,
      reconciliation
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_reconciliation_source_contract",
      ...issue
    })),
    ...reconciliationValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_reconciliation",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}


async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 42);
  if (!sourcePaths) {
    io.stderr("Missing latest provider post-delivery routing source chain paths.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource,
      source
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up plan from an invalid routing chain.", sourceValidation.issues));
    return 1;
  }

  const followUp = source[41];
  const plan =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
      followUp
    );
  await writeFile(outputPath, `${JSON.stringify(plan, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan",
        follow_up_state: plan.follow_up_state,
        delivery_action: plan.delivery_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 42);
  const planPath = args[42];
  if (!sourcePaths || !planPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths or plan path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource,
      source
    );
  const planValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
      source[41],
      plan
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_plan_source_contract",
      ...issue
    })),
    ...planValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_plan",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 42);
  const planPath = args[42];
  if (!sourcePaths || !planPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths or plan path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource,
      [...source, plan]
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up receipt from an invalid plan source chain.", sourceValidation.issues));
    return 1;
  }

  const upsertStatus = readOperationStatusFlag(args, "--upsert-status");
  const receipt =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
      plan,
      {
        executed_at: readFlagValue(args, "--executed-at") ?? new Date().toISOString(),
        ...(upsertStatus ? { upsert_status: upsertStatus } : {})
      }
    );
  await writeFile(outputPath, `${JSON.stringify(receipt, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt",
        overall_status: receipt.overall_status,
        operation_count: receipt.operations.length,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 42);
  const planPath = args[42];
  const receiptPath = args[43];
  if (!sourcePaths || !planPath || !receiptPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource,
      [...source, plan]
    );
  const receiptValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
      plan,
      receipt
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_receipt_source_contract",
      ...issue
    })),
    ...receiptValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_receipt",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 42);
  const planPath = args[42];
  const receiptPath = args[43];
  if (!sourcePaths || !planPath || !receiptPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource,
      [...source, plan, receipt]
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up reconciliation from an invalid receipt source chain.", sourceValidation.issues));
    return 1;
  }

  const reconciliation =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
      plan,
      receipt
    );
  await writeFile(outputPath, `${JSON.stringify(reconciliation, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation",
        reconciliation_status: reconciliation.reconciliation_status,
        recommended_follow_up: reconciliation.recommended_follow_up,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 42);
  const planPath = args[42];
  const receiptPath = args[43];
  const reconciliationPath = args[44];
  if (!sourcePaths || !planPath || !receiptPath || !reconciliationPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths, plan path, receipt path, or reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const reconciliation = JSON.parse(await readFile(reconciliationPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource,
      [...source, plan, receipt]
    );
  const reconciliationValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
      plan,
      receipt,
      reconciliation
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_reconciliation_source_contract",
      ...issue
    })),
    ...reconciliationValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_reconciliation",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}


async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 49);
  if (!sourcePaths) {
    io.stderr("Missing newest downstream provider post-delivery executor-ready trio source chain paths.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource,
    sourceObjects
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render newest downstream provider post-delivery closure/follow-up routing from an invalid executor-ready trio source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const followUp =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
      sourceObjects[46],
      sourceObjects[48]
    );
  await writeFile(outputPath, `${JSON.stringify(followUp, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up",
        follow_up_state: followUp.follow_up_state,
        follow_up_action: followUp.follow_up_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, 50);
  if (!paths) {
    io.stderr("Missing newest downstream provider post-delivery executor-ready trio source chain path or follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, 49));
  const followUp = JSON.parse(await readFile(paths[49]!, "utf8"));
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource,
    sourceObjects
  );
  const followUpValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
      sourceObjects[46],
      sourceObjects[48],
      followUp
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_source_contract",
      ...issue
    })),
    ...followUpValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}


async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 46);
  if (!sourcePaths) {
    io.stderr("Missing latest provider post-delivery routing source chain paths.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource,
      source
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up plan from an invalid routing chain.", sourceValidation.issues));
    return 1;
  }

  const followUp = source[45];
  const plan =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
      followUp
    );
  await writeFile(outputPath, `${JSON.stringify(plan, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan",
        follow_up_state: plan.follow_up_state,
        delivery_action: plan.delivery_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 46);
  const planPath = args[46];
  if (!sourcePaths || !planPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths or plan path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource,
      source
    );
  const planValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
      source[45],
      plan
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_plan_source_contract",
      ...issue
    })),
    ...planValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_plan",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 46);
  const planPath = args[46];
  if (!sourcePaths || !planPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths or plan path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource,
      [...source, plan]
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up receipt from an invalid plan source chain.", sourceValidation.issues));
    return 1;
  }

  const upsertStatus = readOperationStatusFlag(args, "--upsert-status");
  const receipt =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
      plan,
      {
        executed_at: readFlagValue(args, "--executed-at") ?? new Date().toISOString(),
        ...(upsertStatus ? { upsert_status: upsertStatus } : {})
      }
    );
  await writeFile(outputPath, `${JSON.stringify(receipt, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt",
        overall_status: receipt.overall_status,
        operation_count: receipt.operations.length,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 46);
  const planPath = args[46];
  const receiptPath = args[47];
  if (!sourcePaths || !planPath || !receiptPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource,
      [...source, plan]
    );
  const receiptValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
      plan,
      receipt
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_receipt_source_contract",
      ...issue
    })),
    ...receiptValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_receipt",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 46);
  const planPath = args[46];
  const receiptPath = args[47];
  if (!sourcePaths || !planPath || !receiptPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource,
      [...source, plan, receipt]
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up reconciliation from an invalid receipt source chain.", sourceValidation.issues));
    return 1;
  }

  const reconciliation =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
      plan,
      receipt
    );
  await writeFile(outputPath, `${JSON.stringify(reconciliation, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation",
        reconciliation_status: reconciliation.reconciliation_status,
        recommended_follow_up: reconciliation.recommended_follow_up,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 46);
  const planPath = args[46];
  const receiptPath = args[47];
  const reconciliationPath = args[48];
  if (!sourcePaths || !planPath || !receiptPath || !reconciliationPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths, plan path, receipt path, or reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const reconciliation = JSON.parse(await readFile(reconciliationPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource,
      [...source, plan, receipt]
    );
  const reconciliationValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
      plan,
      receipt,
      reconciliation
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_reconciliation_source_contract",
      ...issue
    })),
    ...reconciliationValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_reconciliation",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}



async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 53);
  if (!sourcePaths) {
    io.stderr("Missing newest downstream provider post-delivery executor-ready trio source chain paths.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource,
    sourceObjects
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render newest downstream provider post-delivery closure/follow-up routing from an invalid executor-ready trio source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const followUp =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
      sourceObjects[50],
      sourceObjects[52]
    );
  await writeFile(outputPath, `${JSON.stringify(followUp, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up",
        follow_up_state: followUp.follow_up_state,
        follow_up_action: followUp.follow_up_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, 54);
  if (!paths) {
    io.stderr("Missing newest downstream provider post-delivery executor-ready trio source chain path or follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, 53));
  const followUp = JSON.parse(await readFile(paths[53]!, "utf8"));
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpSource,
    sourceObjects
  );
  const followUpValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUp(
      sourceObjects[50],
      sourceObjects[52],
      followUp
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_source_contract",
      ...issue
    })),
    ...followUpValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}


async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 54);
  if (!sourcePaths) {
    io.stderr("Missing latest provider post-delivery routing source chain paths.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource,
      source
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up plan from an invalid routing chain.", sourceValidation.issues));
    return 1;
  }

  const followUp = source[53];
  const plan =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
      followUp
    );
  await writeFile(outputPath, `${JSON.stringify(plan, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan",
        follow_up_state: plan.follow_up_state,
        delivery_action: plan.delivery_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 54);
  const planPath = args[54];
  if (!sourcePaths || !planPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths or plan path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource,
      source
    );
  const planValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
      source[53],
      plan
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_plan_source_contract",
      ...issue
    })),
    ...planValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_plan",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 54);
  const planPath = args[54];
  if (!sourcePaths || !planPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths or plan path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource,
      [...source, plan]
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up receipt from an invalid plan source chain.", sourceValidation.issues));
    return 1;
  }

  const upsertStatus = readOperationStatusFlag(args, "--upsert-status");
  const receipt =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
      plan,
      {
        executed_at: readFlagValue(args, "--executed-at") ?? new Date().toISOString(),
        ...(upsertStatus ? { upsert_status: upsertStatus } : {})
      }
    );
  await writeFile(outputPath, `${JSON.stringify(receipt, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt",
        overall_status: receipt.overall_status,
        operation_count: receipt.operations.length,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 54);
  const planPath = args[54];
  const receiptPath = args[55];
  if (!sourcePaths || !planPath || !receiptPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource,
      [...source, plan]
    );
  const receiptValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
      plan,
      receipt
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_receipt_source_contract",
      ...issue
    })),
    ...receiptValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_receipt",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 54);
  const planPath = args[54];
  const receiptPath = args[55];
  if (!sourcePaths || !planPath || !receiptPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource,
      [...source, plan, receipt]
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up reconciliation from an invalid receipt source chain.", sourceValidation.issues));
    return 1;
  }

  const reconciliation =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
      plan,
      receipt
    );
  await writeFile(outputPath, `${JSON.stringify(reconciliation, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation",
        reconciliation_status: reconciliation.reconciliation_status,
        recommended_follow_up: reconciliation.recommended_follow_up,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 54);
  const planPath = args[54];
  const receiptPath = args[55];
  const reconciliationPath = args[56];
  if (!sourcePaths || !planPath || !receiptPath || !reconciliationPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths, plan path, receipt path, or reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const reconciliation = JSON.parse(await readFile(reconciliationPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource,
      [...source, plan, receipt]
    );
  const reconciliationValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
      plan,
      receipt,
      reconciliation
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_reconciliation_source_contract",
      ...issue
    })),
    ...reconciliationValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_reconciliation",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpN13(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, N13_SOURCE_PATH_COUNT);
  if (!sourcePaths) {
    io.stderr("Missing newest downstream provider post-delivery executor-ready trio source chain paths.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr(`Missing --out <${N13_PROVIDER_REVIEW}.json>.`);
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpN13Source,
    sourceObjects
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render newest downstream provider post-delivery closure/follow-up routing from an invalid executor-ready trio source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const followUp = buildUnitReviewProviderExecutionFollowUpN13(
    sourceObjects[N13_SOURCE_PLAN_INDEX],
    sourceObjects[N13_SOURCE_RECONCILIATION_INDEX]
  );
  await writeFile(outputPath, `${JSON.stringify(followUp, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: `render-review-${N13_PROVIDER_FOLLOW_UP}`,
        follow_up_state: followUp.follow_up_state,
        follow_up_action: followUp.follow_up_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpN13(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, N13_SOURCE_PATH_COUNT + 1);
  if (!paths) {
    io.stderr("Missing newest downstream provider post-delivery executor-ready trio source chain path or follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, N13_SOURCE_PATH_COUNT));
  const followUp = JSON.parse(await readFile(paths[N13_SOURCE_PATH_COUNT]!, "utf8"));
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpN13Source,
    sourceObjects
  );
  const followUpValidation = validateUnitReviewProviderExecutionFollowUpN13(
    sourceObjects[N13_SOURCE_PLAN_INDEX],
    sourceObjects[N13_SOURCE_RECONCILIATION_INDEX],
    followUp
  );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: `${N13_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_source_contract`,
      ...issue
    })),
    ...followUpValidation.issues.map((issue) => ({
      stage: N13_PROVIDER_FOLLOW_UP.replaceAll("-", "_"),
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: `validate-review-${N13_PROVIDER_FOLLOW_UP}`,
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpN14(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, N14_SOURCE_PATH_COUNT);
  if (!sourcePaths) {
    io.stderr("Missing newest N13 executor-ready trio source chain paths.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr(`Missing --out <${N14_PROVIDER_REVIEW}.json>.`);
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpN14Source,
    sourceObjects
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render N14 provider post-delivery closure/follow-up routing from an invalid N13 executor-ready trio source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const followUp = buildUnitReviewProviderExecutionFollowUpN14(
    sourceObjects[N14_SOURCE_PLAN_INDEX],
    sourceObjects[N14_SOURCE_RECONCILIATION_INDEX]
  );
  await writeFile(outputPath, `${JSON.stringify(followUp, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: `render-review-${N14_PROVIDER_FOLLOW_UP}`,
        follow_up_state: followUp.follow_up_state,
        follow_up_action: followUp.follow_up_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpN14(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, N14_SOURCE_PATH_COUNT + 1);
  if (!paths) {
    io.stderr("Missing newest N13 executor-ready trio source chain path or N14 follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, N14_SOURCE_PATH_COUNT));
  const followUp = JSON.parse(await readFile(paths[N14_SOURCE_PATH_COUNT]!, "utf8"));
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpN14Source,
    sourceObjects
  );
  const followUpValidation = validateUnitReviewProviderExecutionFollowUpN14(
    sourceObjects[N14_SOURCE_PLAN_INDEX],
    sourceObjects[N14_SOURCE_RECONCILIATION_INDEX],
    followUp
  );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: `${N14_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_source_contract`,
      ...issue
    })),
    ...followUpValidation.issues.map((issue) => ({
      stage: N14_PROVIDER_FOLLOW_UP.replaceAll("-", "_"),
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: `validate-review-${N14_PROVIDER_FOLLOW_UP}`,
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpN15(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, N15_SOURCE_PATH_COUNT);
  if (!sourcePaths) {
    io.stderr("Missing newest N14 executor-ready trio source chain paths.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr(`Missing --out <${N15_PROVIDER_REVIEW}.json>.`);
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidatorSafely(
    validateUnitReviewProviderExecutionFollowUpN15Source,
    sourceObjects,
    "source_validation_exception"
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render N15 provider post-delivery closure/follow-up routing from an invalid N14 executor-ready trio source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const followUp = buildUnitReviewProviderExecutionFollowUpN15(
    sourceObjects[N15_SOURCE_PLAN_INDEX],
    sourceObjects[N15_SOURCE_RECONCILIATION_INDEX]
  );
  await writeFile(outputPath, `${JSON.stringify(followUp, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: `render-review-${N15_PROVIDER_FOLLOW_UP}`,
        follow_up_state: followUp.follow_up_state,
        follow_up_action: followUp.follow_up_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpN15(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, N15_SOURCE_PATH_COUNT + 1);
  if (!paths) {
    io.stderr("Missing newest N14 executor-ready trio source chain path or N15 follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, N15_SOURCE_PATH_COUNT));
  const followUp = JSON.parse(await readFile(paths[N15_SOURCE_PATH_COUNT]!, "utf8"));
  const sourceValidation = callFixedArityValidatorSafely(
    validateUnitReviewProviderExecutionFollowUpN15Source,
    sourceObjects,
    "source_validation_exception"
  );
  const followUpValidation = sourceValidation.ok
    ? validateUnitReviewProviderExecutionFollowUpN15(
        sourceObjects[N15_SOURCE_PLAN_INDEX],
        sourceObjects[N15_SOURCE_RECONCILIATION_INDEX],
        followUp
      )
    : {
        ok: false,
        issues: [] as ReturnType<typeof validateUnitReviewProviderExecutionFollowUpN15>["issues"],
      };
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: `${N15_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_source_contract`,
      ...issue
    })),
    ...followUpValidation.issues.map((issue) => ({
      stage: N15_PROVIDER_FOLLOW_UP.replaceAll("-", "_"),
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: `validate-review-${N15_PROVIDER_FOLLOW_UP}`,
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpN16(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, N16_SOURCE_PATH_COUNT);
  if (!sourcePaths) {
    io.stderr("Missing newest N15 executor-ready trio source chain paths.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr(`Missing --out <${N16_PROVIDER_REVIEW}.json>.`);
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidatorSafely(
    validateUnitReviewProviderExecutionFollowUpN16Source,
    sourceObjects,
    "source_validation_exception"
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render N16 provider post-delivery closure/follow-up routing from an invalid N15 executor-ready trio source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const followUp = buildUnitReviewProviderExecutionFollowUpN16(
    sourceObjects[N16_SOURCE_PLAN_INDEX],
    sourceObjects[N16_SOURCE_RECONCILIATION_INDEX]
  );
  await writeFile(outputPath, `${JSON.stringify(followUp, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: `render-review-${N16_PROVIDER_FOLLOW_UP}`,
        follow_up_state: followUp.follow_up_state,
        follow_up_action: followUp.follow_up_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpN16(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, N16_SOURCE_PATH_COUNT + 1);
  if (!paths) {
    io.stderr("Missing newest N15 executor-ready trio source chain path or N16 follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, N16_SOURCE_PATH_COUNT));
  const followUp = JSON.parse(await readFile(paths[N16_SOURCE_PATH_COUNT]!, "utf8"));
  const sourceValidation = callFixedArityValidatorSafely(
    validateUnitReviewProviderExecutionFollowUpN16Source,
    sourceObjects,
    "source_validation_exception"
  );
  const followUpValidation = sourceValidation.ok
    ? validateUnitReviewProviderExecutionFollowUpN16(
        sourceObjects[N16_SOURCE_PLAN_INDEX],
        sourceObjects[N16_SOURCE_RECONCILIATION_INDEX],
        followUp
      )
    : {
        ok: false,
        issues: [] as ReturnType<typeof validateUnitReviewProviderExecutionFollowUpN16>["issues"],
      };
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: `${N16_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_source_contract`,
      ...issue
    })),
    ...followUpValidation.issues.map((issue) => ({
      stage: N16_PROVIDER_FOLLOW_UP.replaceAll("-", "_"),
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: `validate-review-${N16_PROVIDER_FOLLOW_UP}`,
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}


async function runRenderReviewProviderExecutionFollowUpN17(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, N17_SOURCE_PATH_COUNT);
  if (!sourcePaths) {
    io.stderr("Missing newest N16 executor-ready trio source chain paths.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr(`Missing --out <${N17_PROVIDER_REVIEW}.json>.`);
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidatorSafely(
    validateUnitReviewProviderExecutionFollowUpN17Source,
    sourceObjects,
    "source_validation_exception"
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render N17 provider post-delivery closure/follow-up routing from an invalid N16 executor-ready trio source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const followUp = buildUnitReviewProviderExecutionFollowUpN17(
    sourceObjects[N17_SOURCE_PLAN_INDEX],
    sourceObjects[N17_SOURCE_RECONCILIATION_INDEX]
  );
  await writeFile(outputPath, `${JSON.stringify(followUp, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: `render-review-${N17_PROVIDER_FOLLOW_UP}`,
        follow_up_state: followUp.follow_up_state,
        follow_up_action: followUp.follow_up_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpN17(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, N17_SOURCE_PATH_COUNT + 1);
  if (!paths) {
    io.stderr("Missing newest N16 executor-ready trio source chain path or N17 follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, N17_SOURCE_PATH_COUNT));
  const followUp = JSON.parse(await readFile(paths[N17_SOURCE_PATH_COUNT]!, "utf8"));
  const sourceValidation = callFixedArityValidatorSafely(
    validateUnitReviewProviderExecutionFollowUpN17Source,
    sourceObjects,
    "source_validation_exception"
  );
  const followUpValidation = sourceValidation.ok
    ? callFixedArityValidatorSafely(
        validateUnitReviewProviderExecutionFollowUpN17,
        [
          sourceObjects[N17_SOURCE_PLAN_INDEX],
          sourceObjects[N17_SOURCE_RECONCILIATION_INDEX],
          followUp,
        ],
        "follow_up_validation_exception"
      )
    : {
        ok: false,
        issues: [] as ReturnType<typeof validateUnitReviewProviderExecutionFollowUpN17>["issues"],
      };
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: `${N17_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_source_contract`,
      ...issue
    })),
    ...followUpValidation.issues.map((issue) => ({
      stage: N17_PROVIDER_FOLLOW_UP.replaceAll("-", "_"),
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: `validate-review-${N17_PROVIDER_FOLLOW_UP}`,
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpN13Plan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, N13_PLAN_SOURCE_PATH_COUNT);
  if (!sourcePaths) {
    io.stderr("Missing newest downstream provider post-delivery routing source chain paths and follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr(`Missing --out <${N13_PROVIDER_REVIEW}-plan.json>.`);
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpN13PlanSource,
    sourceObjects
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render newest downstream provider post-delivery follow-up delivery plan from an invalid routing source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const plan = buildUnitReviewProviderExecutionFollowUpN13Plan(
    sourceObjects[N13_FOLLOW_UP_SOURCE_INDEX]
  );
  await writeFile(outputPath, `${JSON.stringify(plan, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: `render-review-${N13_PROVIDER_FOLLOW_UP}-plan`,
        follow_up_state: plan.follow_up_state,
        delivery_action: plan.delivery_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpN13Plan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, N13_PLAN_SOURCE_PATH_COUNT + 1);
  if (!paths) {
    io.stderr("Missing newest downstream provider post-delivery routing source chain, follow-up path, or plan path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, N13_PLAN_SOURCE_PATH_COUNT));
  const plan = JSON.parse(await readFile(paths[N13_PLAN_SOURCE_INDEX]!, "utf8"));
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpN13PlanSource,
    sourceObjects
  );
  const planValidation = validateUnitReviewProviderExecutionFollowUpN13Plan(
    sourceObjects[N13_FOLLOW_UP_SOURCE_INDEX],
    plan
  );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: `${N13_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_plan_source_contract`,
      ...issue
    })),
    ...planValidation.issues.map((issue) => ({
      stage: `${N13_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_plan`,
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: `validate-review-${N13_PROVIDER_FOLLOW_UP}-plan`,
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpN13Receipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, N13_RECEIPT_SOURCE_PATH_COUNT);
  if (!sourcePaths) {
    io.stderr("Missing newest downstream provider post-delivery routing source chain, follow-up path, or plan path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr(`Missing --out <${N13_PROVIDER_REVIEW}-receipt.json>.`);
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpN13ReceiptSource,
    sourceObjects
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render newest downstream provider post-delivery follow-up receipt from an invalid plan source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const upsertStatus = readOperationStatusFlag(args, "--upsert-status");
  const receipt = buildUnitReviewProviderExecutionFollowUpN13Receipt(
    sourceObjects[N13_PLAN_SOURCE_INDEX],
    {
      executed_at: readFlagValue(args, "--executed-at") ?? new Date().toISOString(),
      ...(upsertStatus ? { upsert_status: upsertStatus } : {})
    }
  );
  await writeFile(outputPath, `${JSON.stringify(receipt, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: `render-review-${N13_PROVIDER_FOLLOW_UP}-receipt`,
        overall_status: receipt.overall_status,
        operation_count: receipt.operations.length,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpN13Receipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, N13_RECEIPT_SOURCE_PATH_COUNT + 1);
  if (!paths) {
    io.stderr("Missing newest downstream provider post-delivery routing source chain, follow-up path, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, N13_RECEIPT_SOURCE_PATH_COUNT));
  const plan = sourceObjects[N13_PLAN_SOURCE_INDEX];
  const receipt = JSON.parse(await readFile(paths[N13_RECEIPT_SOURCE_INDEX]!, "utf8"));
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpN13ReceiptSource,
    sourceObjects
  );
  const receiptValidation = validateUnitReviewProviderExecutionFollowUpN13Receipt(plan, receipt);
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: `${N13_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_receipt_source_contract`,
      ...issue
    })),
    ...receiptValidation.issues.map((issue) => ({
      stage: `${N13_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_receipt`,
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: `validate-review-${N13_PROVIDER_FOLLOW_UP}-receipt`,
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpN13Reconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, N13_RECONCILIATION_SOURCE_PATH_COUNT);
  if (!sourcePaths) {
    io.stderr("Missing newest downstream provider post-delivery routing source chain, follow-up path, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr(`Missing --out <${N13_PROVIDER_REVIEW}-reconciliation.json>.`);
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpN13ReconciliationSource,
    sourceObjects
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render newest downstream provider post-delivery follow-up reconciliation from an invalid receipt source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const reconciliation = buildUnitReviewProviderExecutionFollowUpN13Reconciliation(
    sourceObjects[N13_PLAN_SOURCE_INDEX],
    sourceObjects[N13_RECEIPT_SOURCE_INDEX]
  );
  await writeFile(outputPath, `${JSON.stringify(reconciliation, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: `render-review-${N13_PROVIDER_FOLLOW_UP}-reconciliation`,
        reconciliation_status: reconciliation.reconciliation_status,
        recommended_follow_up: reconciliation.recommended_follow_up,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpN13Reconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, N13_RECONCILIATION_SOURCE_PATH_COUNT + 1);
  if (!paths) {
    io.stderr("Missing newest downstream provider post-delivery routing source chain, follow-up path, plan path, receipt path, or reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, N13_RECONCILIATION_SOURCE_PATH_COUNT));
  const plan = sourceObjects[N13_PLAN_SOURCE_INDEX];
  const receipt = sourceObjects[N13_RECEIPT_SOURCE_INDEX];
  const reconciliation = JSON.parse(
    await readFile(paths[N13_RECONCILIATION_SOURCE_PATH_COUNT]!, "utf8")
  );
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpN13ReconciliationSource,
    sourceObjects
  );
  const reconciliationValidation = validateUnitReviewProviderExecutionFollowUpN13Reconciliation(
    plan,
    receipt,
    reconciliation
  );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: `${N13_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_reconciliation_source_contract`,
      ...issue
    })),
    ...reconciliationValidation.issues.map((issue) => ({
      stage: `${N13_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_reconciliation`,
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: `validate-review-${N13_PROVIDER_FOLLOW_UP}-reconciliation`,
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}


async function runRenderReviewProviderExecutionFollowUpN14Plan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, N14_PLAN_SOURCE_PATH_COUNT);
  if (!sourcePaths) {
    io.stderr("Missing newest downstream provider post-delivery routing source chain paths and follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr(`Missing --out <${N14_PROVIDER_REVIEW}-plan.json>.`);
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpN14PlanSource,
    sourceObjects
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render newest downstream provider post-delivery follow-up delivery plan from an invalid routing source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const plan = buildUnitReviewProviderExecutionFollowUpN14Plan(
    sourceObjects[N14_FOLLOW_UP_SOURCE_INDEX]
  );
  await writeFile(outputPath, `${JSON.stringify(plan, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: `render-review-${N14_PROVIDER_FOLLOW_UP}-plan`,
        follow_up_state: plan.follow_up_state,
        delivery_action: plan.delivery_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpN14Plan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, N14_PLAN_SOURCE_PATH_COUNT + 1);
  if (!paths) {
    io.stderr("Missing newest downstream provider post-delivery routing source chain, follow-up path, or plan path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, N14_PLAN_SOURCE_PATH_COUNT));
  const plan = JSON.parse(await readFile(paths[N14_PLAN_SOURCE_INDEX]!, "utf8"));
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpN14PlanSource,
    sourceObjects
  );
  const planValidation = validateUnitReviewProviderExecutionFollowUpN14Plan(
    sourceObjects[N14_FOLLOW_UP_SOURCE_INDEX],
    plan
  );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: `${N14_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_plan_source_contract`,
      ...issue
    })),
    ...planValidation.issues.map((issue) => ({
      stage: `${N14_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_plan`,
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: `validate-review-${N14_PROVIDER_FOLLOW_UP}-plan`,
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpN14Receipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, N14_RECEIPT_SOURCE_PATH_COUNT);
  if (!sourcePaths) {
    io.stderr("Missing newest downstream provider post-delivery routing source chain, follow-up path, or plan path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr(`Missing --out <${N14_PROVIDER_REVIEW}-receipt.json>.`);
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpN14ReceiptSource,
    sourceObjects
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render newest downstream provider post-delivery follow-up receipt from an invalid plan source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const upsertStatus = readOperationStatusFlag(args, "--upsert-status");
  const receipt = buildUnitReviewProviderExecutionFollowUpN14Receipt(
    sourceObjects[N14_PLAN_SOURCE_INDEX],
    {
      executed_at: readFlagValue(args, "--executed-at") ?? new Date().toISOString(),
      ...(upsertStatus ? { upsert_status: upsertStatus } : {})
    }
  );
  await writeFile(outputPath, `${JSON.stringify(receipt, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: `render-review-${N14_PROVIDER_FOLLOW_UP}-receipt`,
        overall_status: receipt.overall_status,
        operation_count: receipt.operations.length,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpN14Receipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, N14_RECEIPT_SOURCE_PATH_COUNT + 1);
  if (!paths) {
    io.stderr("Missing newest downstream provider post-delivery routing source chain, follow-up path, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, N14_RECEIPT_SOURCE_PATH_COUNT));
  const plan = sourceObjects[N14_PLAN_SOURCE_INDEX];
  const receipt = JSON.parse(await readFile(paths[N14_RECEIPT_SOURCE_INDEX]!, "utf8"));
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpN14ReceiptSource,
    sourceObjects
  );
  const receiptValidation = validateUnitReviewProviderExecutionFollowUpN14Receipt(plan, receipt);
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: `${N14_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_receipt_source_contract`,
      ...issue
    })),
    ...receiptValidation.issues.map((issue) => ({
      stage: `${N14_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_receipt`,
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: `validate-review-${N14_PROVIDER_FOLLOW_UP}-receipt`,
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpN14Reconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, N14_RECONCILIATION_SOURCE_PATH_COUNT);
  if (!sourcePaths) {
    io.stderr("Missing newest downstream provider post-delivery routing source chain, follow-up path, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr(`Missing --out <${N14_PROVIDER_REVIEW}-reconciliation.json>.`);
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpN14ReconciliationSource,
    sourceObjects
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render newest downstream provider post-delivery follow-up reconciliation from an invalid receipt source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const reconciliation = buildUnitReviewProviderExecutionFollowUpN14Reconciliation(
    sourceObjects[N14_PLAN_SOURCE_INDEX],
    sourceObjects[N14_RECEIPT_SOURCE_INDEX]
  );
  await writeFile(outputPath, `${JSON.stringify(reconciliation, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: `render-review-${N14_PROVIDER_FOLLOW_UP}-reconciliation`,
        reconciliation_status: reconciliation.reconciliation_status,
        recommended_follow_up: reconciliation.recommended_follow_up,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpN14Reconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, N14_RECONCILIATION_SOURCE_PATH_COUNT + 1);
  if (!paths) {
    io.stderr("Missing newest downstream provider post-delivery routing source chain, follow-up path, plan path, receipt path, or reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, N14_RECONCILIATION_SOURCE_PATH_COUNT));
  const plan = sourceObjects[N14_PLAN_SOURCE_INDEX];
  const receipt = sourceObjects[N14_RECEIPT_SOURCE_INDEX];
  const reconciliation = JSON.parse(
    await readFile(paths[N14_RECONCILIATION_SOURCE_PATH_COUNT]!, "utf8")
  );
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpN14ReconciliationSource,
    sourceObjects
  );
  const reconciliationValidation = validateUnitReviewProviderExecutionFollowUpN14Reconciliation(
    plan,
    receipt,
    reconciliation
  );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: `${N14_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_reconciliation_source_contract`,
      ...issue
    })),
    ...reconciliationValidation.issues.map((issue) => ({
      stage: `${N14_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_reconciliation`,
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: `validate-review-${N14_PROVIDER_FOLLOW_UP}-reconciliation`,
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}




async function runRenderReviewProviderExecutionFollowUpN15Plan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, N15_PLAN_SOURCE_PATH_COUNT);
  if (!sourcePaths) {
    io.stderr("Missing newest downstream provider post-delivery routing source chain paths and follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr(`Missing --out <${N15_PROVIDER_REVIEW}-plan.json>.`);
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpN15PlanSource,
    sourceObjects
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render newest downstream provider post-delivery follow-up delivery plan from an invalid routing source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const plan = buildUnitReviewProviderExecutionFollowUpN15Plan(
    sourceObjects[N15_FOLLOW_UP_SOURCE_INDEX]
  );
  await writeFile(outputPath, `${JSON.stringify(plan, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: `render-review-${N15_PROVIDER_FOLLOW_UP}-plan`,
        follow_up_state: plan.follow_up_state,
        delivery_action: plan.delivery_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpN15Plan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, N15_PLAN_SOURCE_PATH_COUNT + 1);
  if (!paths) {
    io.stderr("Missing newest downstream provider post-delivery routing source chain, follow-up path, or plan path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, N15_PLAN_SOURCE_PATH_COUNT));
  const plan = JSON.parse(await readFile(paths[N15_PLAN_SOURCE_INDEX]!, "utf8"));
  const sourceValidation = callFixedArityValidatorSafely(
    validateUnitReviewProviderExecutionFollowUpN15PlanSource,
    sourceObjects,
    "source_validation_exception"
  );
  const planIssues = sourceValidation.ok
    ? validateUnitReviewProviderExecutionFollowUpN15Plan(
        sourceObjects[N15_FOLLOW_UP_SOURCE_INDEX],
        plan
      ).issues
    : [];
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: `${N15_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_plan_source_contract`,
      ...issue
    })),
    ...planIssues.map((issue) => ({
      stage: `${N15_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_plan`,
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: `validate-review-${N15_PROVIDER_FOLLOW_UP}-plan`,
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpN15Receipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, N15_RECEIPT_SOURCE_PATH_COUNT);
  if (!sourcePaths) {
    io.stderr("Missing newest downstream provider post-delivery routing source chain, follow-up path, or plan path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr(`Missing --out <${N15_PROVIDER_REVIEW}-receipt.json>.`);
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpN15ReceiptSource,
    sourceObjects
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render newest downstream provider post-delivery follow-up receipt from an invalid plan source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const upsertStatus = readOperationStatusFlag(args, "--upsert-status");
  const receipt = buildUnitReviewProviderExecutionFollowUpN15Receipt(
    sourceObjects[N15_PLAN_SOURCE_INDEX],
    {
      executed_at: readFlagValue(args, "--executed-at") ?? new Date().toISOString(),
      ...(upsertStatus ? { upsert_status: upsertStatus } : {})
    }
  );
  await writeFile(outputPath, `${JSON.stringify(receipt, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: `render-review-${N15_PROVIDER_FOLLOW_UP}-receipt`,
        overall_status: receipt.overall_status,
        operation_count: receipt.operations.length,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpN15Receipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, N15_RECEIPT_SOURCE_PATH_COUNT + 1);
  if (!paths) {
    io.stderr("Missing newest downstream provider post-delivery routing source chain, follow-up path, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, N15_RECEIPT_SOURCE_PATH_COUNT));
  const plan = sourceObjects[N15_PLAN_SOURCE_INDEX];
  const receipt = JSON.parse(await readFile(paths[N15_RECEIPT_SOURCE_INDEX]!, "utf8"));
  const sourceValidation = callFixedArityValidatorSafely(
    validateUnitReviewProviderExecutionFollowUpN15ReceiptSource,
    sourceObjects,
    "source_validation_exception"
  );
  const receiptIssues = sourceValidation.ok
    ? validateUnitReviewProviderExecutionFollowUpN15Receipt(plan, receipt).issues
    : [];
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: `${N15_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_receipt_source_contract`,
      ...issue
    })),
    ...receiptIssues.map((issue) => ({
      stage: `${N15_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_receipt`,
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: `validate-review-${N15_PROVIDER_FOLLOW_UP}-receipt`,
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpN15Reconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, N15_RECONCILIATION_SOURCE_PATH_COUNT);
  if (!sourcePaths) {
    io.stderr("Missing newest downstream provider post-delivery routing source chain, follow-up path, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr(`Missing --out <${N15_PROVIDER_REVIEW}-reconciliation.json>.`);
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidator(
    validateUnitReviewProviderExecutionFollowUpN15ReconciliationSource,
    sourceObjects
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render newest downstream provider post-delivery follow-up reconciliation from an invalid receipt source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const reconciliation = buildUnitReviewProviderExecutionFollowUpN15Reconciliation(
    sourceObjects[N15_PLAN_SOURCE_INDEX],
    sourceObjects[N15_RECEIPT_SOURCE_INDEX]
  );
  await writeFile(outputPath, `${JSON.stringify(reconciliation, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: `render-review-${N15_PROVIDER_FOLLOW_UP}-reconciliation`,
        reconciliation_status: reconciliation.reconciliation_status,
        recommended_follow_up: reconciliation.recommended_follow_up,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpN15Reconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, N15_RECONCILIATION_SOURCE_PATH_COUNT + 1);
  if (!paths) {
    io.stderr("Missing newest downstream provider post-delivery routing source chain, follow-up path, plan path, receipt path, or reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, N15_RECONCILIATION_SOURCE_PATH_COUNT));
  const plan = sourceObjects[N15_PLAN_SOURCE_INDEX];
  const receipt = sourceObjects[N15_RECEIPT_SOURCE_INDEX];
  const reconciliation = JSON.parse(
    await readFile(paths[N15_RECONCILIATION_SOURCE_PATH_COUNT]!, "utf8")
  );
  const sourceValidation = callFixedArityValidatorSafely(
    validateUnitReviewProviderExecutionFollowUpN15ReconciliationSource,
    sourceObjects,
    "source_validation_exception"
  );
  const reconciliationIssues = sourceValidation.ok
    ? validateUnitReviewProviderExecutionFollowUpN15Reconciliation(
        plan,
        receipt,
        reconciliation
      ).issues
    : [];
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: `${N15_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_reconciliation_source_contract`,
      ...issue
    })),
    ...reconciliationIssues.map((issue) => ({
      stage: `${N15_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_reconciliation`,
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: `validate-review-${N15_PROVIDER_FOLLOW_UP}-reconciliation`,
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}






async function runRenderReviewProviderExecutionFollowUpN16Plan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, N16_PLAN_SOURCE_PATH_COUNT);
  if (!sourcePaths) {
    io.stderr("Missing newest N16 downstream provider post-delivery routing source chain paths and follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr(`Missing --out <${N16_PROVIDER_REVIEW}-plan.json>.`);
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidatorSafely(
    validateUnitReviewProviderExecutionFollowUpN16PlanSource,
    sourceObjects,
    "source_validation_exception"
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render newest N16 downstream provider post-delivery follow-up delivery plan from an invalid routing source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const plan = buildUnitReviewProviderExecutionFollowUpN16Plan(
    sourceObjects[N16_FOLLOW_UP_SOURCE_INDEX]
  );
  await writeFile(outputPath, `${JSON.stringify(plan, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: `render-review-${N16_PROVIDER_FOLLOW_UP}-plan`,
        follow_up_state: plan.follow_up_state,
        delivery_action: plan.delivery_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpN16Plan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, N16_PLAN_SOURCE_PATH_COUNT + 1);
  if (!paths) {
    io.stderr("Missing newest N16 downstream provider post-delivery routing source chain, follow-up path, or plan path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, N16_PLAN_SOURCE_PATH_COUNT));
  const plan = JSON.parse(await readFile(paths[N16_PLAN_SOURCE_INDEX]!, "utf8"));
  const sourceValidation = callFixedArityValidatorSafely(
    validateUnitReviewProviderExecutionFollowUpN16PlanSource,
    sourceObjects,
    "source_validation_exception"
  );
  const planIssues = sourceValidation.ok
    ? validateUnitReviewProviderExecutionFollowUpN16Plan(
        sourceObjects[N16_FOLLOW_UP_SOURCE_INDEX],
        plan
      ).issues
    : [];
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: `${N16_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_plan_source_contract`,
      ...issue
    })),
    ...planIssues.map((issue) => ({
      stage: `${N16_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_plan`,
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: `validate-review-${N16_PROVIDER_FOLLOW_UP}-plan`,
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpN16Receipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, N16_RECEIPT_SOURCE_PATH_COUNT);
  if (!sourcePaths) {
    io.stderr("Missing newest N16 downstream provider post-delivery routing source chain, follow-up path, or plan path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr(`Missing --out <${N16_PROVIDER_REVIEW}-receipt.json>.`);
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidatorSafely(
    validateUnitReviewProviderExecutionFollowUpN16ReceiptSource,
    sourceObjects,
    "source_validation_exception"
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render newest N16 downstream provider post-delivery follow-up receipt from an invalid plan source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const upsertStatus = readOperationStatusFlag(args, "--upsert-status");
  const receipt = buildUnitReviewProviderExecutionFollowUpN16Receipt(
    sourceObjects[N16_PLAN_SOURCE_INDEX],
    {
      executed_at: readFlagValue(args, "--executed-at") ?? new Date().toISOString(),
      ...(upsertStatus ? { upsert_status: upsertStatus } : {})
    }
  );
  await writeFile(outputPath, `${JSON.stringify(receipt, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: `render-review-${N16_PROVIDER_FOLLOW_UP}-receipt`,
        overall_status: receipt.overall_status,
        operation_count: receipt.operations.length,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpN16Receipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, N16_RECEIPT_SOURCE_PATH_COUNT + 1);
  if (!paths) {
    io.stderr("Missing newest N16 downstream provider post-delivery routing source chain, follow-up path, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, N16_RECEIPT_SOURCE_PATH_COUNT));
  const plan = sourceObjects[N16_PLAN_SOURCE_INDEX];
  const receipt = JSON.parse(await readFile(paths[N16_RECEIPT_SOURCE_INDEX]!, "utf8"));
  const sourceValidation = callFixedArityValidatorSafely(
    validateUnitReviewProviderExecutionFollowUpN16ReceiptSource,
    sourceObjects,
    "source_validation_exception"
  );
  const receiptIssues = sourceValidation.ok
    ? validateUnitReviewProviderExecutionFollowUpN16Receipt(plan, receipt).issues
    : [];
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: `${N16_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_receipt_source_contract`,
      ...issue
    })),
    ...receiptIssues.map((issue) => ({
      stage: `${N16_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_receipt`,
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: `validate-review-${N16_PROVIDER_FOLLOW_UP}-receipt`,
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpN16Reconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, N16_RECONCILIATION_SOURCE_PATH_COUNT);
  if (!sourcePaths) {
    io.stderr("Missing newest N16 downstream provider post-delivery routing source chain, follow-up path, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr(`Missing --out <${N16_PROVIDER_REVIEW}-reconciliation.json>.`);
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidatorSafely(
    validateUnitReviewProviderExecutionFollowUpN16ReconciliationSource,
    sourceObjects,
    "source_validation_exception"
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render newest N16 downstream provider post-delivery follow-up reconciliation from an invalid receipt source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const reconciliation = buildUnitReviewProviderExecutionFollowUpN16Reconciliation(
    sourceObjects[N16_PLAN_SOURCE_INDEX],
    sourceObjects[N16_RECEIPT_SOURCE_INDEX]
  );
  await writeFile(outputPath, `${JSON.stringify(reconciliation, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: `render-review-${N16_PROVIDER_FOLLOW_UP}-reconciliation`,
        reconciliation_status: reconciliation.reconciliation_status,
        recommended_follow_up: reconciliation.recommended_follow_up,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpN16Reconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, N16_RECONCILIATION_SOURCE_PATH_COUNT + 1);
  if (!paths) {
    io.stderr("Missing newest N16 downstream provider post-delivery routing source chain, follow-up path, plan path, receipt path, or reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, N16_RECONCILIATION_SOURCE_PATH_COUNT));
  const plan = sourceObjects[N16_PLAN_SOURCE_INDEX];
  const receipt = sourceObjects[N16_RECEIPT_SOURCE_INDEX];
  const reconciliation = JSON.parse(
    await readFile(paths[N16_RECONCILIATION_SOURCE_PATH_COUNT]!, "utf8")
  );
  const sourceValidation = callFixedArityValidatorSafely(
    validateUnitReviewProviderExecutionFollowUpN16ReconciliationSource,
    sourceObjects,
    "source_validation_exception"
  );
  const reconciliationIssues = sourceValidation.ok
    ? validateUnitReviewProviderExecutionFollowUpN16Reconciliation(
        plan,
        receipt,
        reconciliation
      ).issues
    : [];
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: `${N16_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_reconciliation_source_contract`,
      ...issue
    })),
    ...reconciliationIssues.map((issue) => ({
      stage: `${N16_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_reconciliation`,
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: `validate-review-${N16_PROVIDER_FOLLOW_UP}-reconciliation`,
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpN17Plan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, N17_PLAN_SOURCE_PATH_COUNT);
  if (!sourcePaths) {
    io.stderr("Missing newest N17 downstream provider post-delivery routing source chain paths and follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr(`Missing --out <${N17_PROVIDER_REVIEW}-plan.json>.`);
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidatorSafely(
    validateUnitReviewProviderExecutionFollowUpN17PlanSource,
    sourceObjects,
    "source_validation_exception"
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render newest N17 downstream provider post-delivery follow-up delivery plan from an invalid routing source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const plan = buildUnitReviewProviderExecutionFollowUpN17Plan(
    sourceObjects[N17_FOLLOW_UP_SOURCE_INDEX]
  );
  await writeFile(outputPath, `${JSON.stringify(plan, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: `render-review-${N17_PROVIDER_FOLLOW_UP}-plan`,
        follow_up_state: plan.follow_up_state,
        delivery_action: plan.delivery_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpN17Plan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, N17_PLAN_SOURCE_PATH_COUNT + 1);
  if (!paths) {
    io.stderr("Missing newest N17 downstream provider post-delivery routing source chain, follow-up path, or plan path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, N17_PLAN_SOURCE_PATH_COUNT));
  const plan = JSON.parse(await readFile(paths[N17_PLAN_SOURCE_INDEX]!, "utf8"));
  const sourceValidation = callFixedArityValidatorSafely(
    validateUnitReviewProviderExecutionFollowUpN17PlanSource,
    sourceObjects,
    "source_validation_exception"
  );
  const planValidation = sourceValidation.ok
    ? callFixedArityValidatorSafely(
        validateUnitReviewProviderExecutionFollowUpN17Plan,
        [sourceObjects[N17_FOLLOW_UP_SOURCE_INDEX], plan],
        "plan_validation_exception"
      )
    : { ok: false, issues: [] };
  const planIssues = planValidation.issues;
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: `${N17_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_plan_source_contract`,
      ...issue
    })),
    ...planIssues.map((issue) => ({
      stage: `${N17_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_plan`,
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: `validate-review-${N17_PROVIDER_FOLLOW_UP}-plan`,
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpN17Receipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, N17_RECEIPT_SOURCE_PATH_COUNT);
  if (!sourcePaths) {
    io.stderr("Missing newest N17 downstream provider post-delivery routing source chain, follow-up path, or plan path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr(`Missing --out <${N17_PROVIDER_REVIEW}-receipt.json>.`);
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidatorSafely(
    validateUnitReviewProviderExecutionFollowUpN17ReceiptSource,
    sourceObjects,
    "source_validation_exception"
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render newest N17 downstream provider post-delivery follow-up receipt from an invalid plan source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const upsertStatus = readOperationStatusFlag(args, "--upsert-status");
  const receipt = buildUnitReviewProviderExecutionFollowUpN17Receipt(
    sourceObjects[N17_PLAN_SOURCE_INDEX],
    {
      executed_at: readFlagValue(args, "--executed-at") ?? new Date().toISOString(),
      ...(upsertStatus ? { upsert_status: upsertStatus } : {})
    }
  );
  await writeFile(outputPath, `${JSON.stringify(receipt, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: `render-review-${N17_PROVIDER_FOLLOW_UP}-receipt`,
        overall_status: receipt.overall_status,
        operation_count: receipt.operations.length,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpN17Receipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, N17_RECEIPT_SOURCE_PATH_COUNT + 1);
  if (!paths) {
    io.stderr("Missing newest N17 downstream provider post-delivery routing source chain, follow-up path, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, N17_RECEIPT_SOURCE_PATH_COUNT));
  const plan = sourceObjects[N17_PLAN_SOURCE_INDEX];
  const receipt = JSON.parse(await readFile(paths[N17_RECEIPT_SOURCE_INDEX]!, "utf8"));
  const sourceValidation = callFixedArityValidatorSafely(
    validateUnitReviewProviderExecutionFollowUpN17ReceiptSource,
    sourceObjects,
    "source_validation_exception"
  );
  const receiptValidation = sourceValidation.ok
    ? callFixedArityValidatorSafely(
        validateUnitReviewProviderExecutionFollowUpN17Receipt,
        [plan, receipt],
        "receipt_validation_exception"
      )
    : { ok: false, issues: [] };
  const receiptIssues = receiptValidation.issues;
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: `${N17_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_receipt_source_contract`,
      ...issue
    })),
    ...receiptIssues.map((issue) => ({
      stage: `${N17_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_receipt`,
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: `validate-review-${N17_PROVIDER_FOLLOW_UP}-receipt`,
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpN17Reconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, N17_RECONCILIATION_SOURCE_PATH_COUNT);
  if (!sourcePaths) {
    io.stderr("Missing newest N17 downstream provider post-delivery routing source chain, follow-up path, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr(`Missing --out <${N17_PROVIDER_REVIEW}-reconciliation.json>.`);
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(sourcePaths);
  const sourceValidation = callFixedArityValidatorSafely(
    validateUnitReviewProviderExecutionFollowUpN17ReconciliationSource,
    sourceObjects,
    "source_validation_exception"
  );
  if (!sourceValidation.ok) {
    io.stderr(
      formatValidationFailure(
        "Cannot render newest N17 downstream provider post-delivery follow-up reconciliation from an invalid receipt source chain.",
        sourceValidation.issues
      )
    );
    return 1;
  }

  const reconciliation = buildUnitReviewProviderExecutionFollowUpN17Reconciliation(
    sourceObjects[N17_PLAN_SOURCE_INDEX],
    sourceObjects[N17_RECEIPT_SOURCE_INDEX]
  );
  await writeFile(outputPath, `${JSON.stringify(reconciliation, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: `render-review-${N17_PROVIDER_FOLLOW_UP}-reconciliation`,
        reconciliation_status: reconciliation.reconciliation_status,
        recommended_follow_up: reconciliation.recommended_follow_up,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpN17Reconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const paths = readRequiredPathSlice(args, N17_RECONCILIATION_SOURCE_PATH_COUNT + 1);
  if (!paths) {
    io.stderr("Missing newest N17 downstream provider post-delivery routing source chain, follow-up path, plan path, receipt path, or reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const sourceObjects = await readJsonPathSlice(paths.slice(0, N17_RECONCILIATION_SOURCE_PATH_COUNT));
  const plan = sourceObjects[N17_PLAN_SOURCE_INDEX];
  const receipt = sourceObjects[N17_RECEIPT_SOURCE_INDEX];
  const reconciliation = JSON.parse(
    await readFile(paths[N17_RECONCILIATION_SOURCE_PATH_COUNT]!, "utf8")
  );
  const sourceValidation = callFixedArityValidatorSafely(
    validateUnitReviewProviderExecutionFollowUpN17ReconciliationSource,
    sourceObjects,
    "source_validation_exception"
  );
  const reconciliationValidation = sourceValidation.ok
    ? callFixedArityValidatorSafely(
        validateUnitReviewProviderExecutionFollowUpN17Reconciliation,
        [plan, receipt, reconciliation],
        "reconciliation_validation_exception"
      )
    : { ok: false, issues: [] };
  const reconciliationIssues = reconciliationValidation.issues;
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: `${N17_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_reconciliation_source_contract`,
      ...issue
    })),
    ...reconciliationIssues.map((issue) => ({
      stage: `${N17_PROVIDER_FOLLOW_UP.replaceAll("-", "_")}_reconciliation`,
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: `validate-review-${N17_PROVIDER_FOLLOW_UP}-reconciliation`,
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 50);
  if (!sourcePaths) {
    io.stderr("Missing latest provider post-delivery routing source chain paths.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource,
      source
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up plan from an invalid routing chain.", sourceValidation.issues));
    return 1;
  }

  const followUp = source[49];
  const plan =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
      followUp
    );
  await writeFile(outputPath, `${JSON.stringify(plan, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan",
        follow_up_state: plan.follow_up_state,
        delivery_action: plan.delivery_action,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 50);
  const planPath = args[50];
  if (!sourcePaths || !planPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths or plan path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlanSource,
      source
    );
  const planValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpPlan(
      source[49],
      plan
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_plan_source_contract",
      ...issue
    })),
    ...planValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_plan",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 50);
  const planPath = args[50];
  if (!sourcePaths || !planPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths or plan path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource,
      [...source, plan]
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up receipt from an invalid plan source chain.", sourceValidation.issues));
    return 1;
  }

  const upsertStatus = readOperationStatusFlag(args, "--upsert-status");
  const receipt =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
      plan,
      {
        executed_at: readFlagValue(args, "--executed-at") ?? new Date().toISOString(),
        ...(upsertStatus ? { upsert_status: upsertStatus } : {})
      }
    );
  await writeFile(outputPath, `${JSON.stringify(receipt, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt",
        overall_status: receipt.overall_status,
        operation_count: receipt.operations.length,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 50);
  const planPath = args[50];
  const receiptPath = args[51];
  if (!sourcePaths || !planPath || !receiptPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceiptSource,
      [...source, plan]
    );
  const receiptValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReceipt(
      plan,
      receipt
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_receipt_source_contract",
      ...issue
    })),
    ...receiptValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_receipt",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 50);
  const planPath = args[50];
  const receiptPath = args[51];
  if (!sourcePaths || !planPath || !receiptPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths, plan path, or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource,
      [...source, plan, receipt]
    );
  if (!sourceValidation.ok) {
    io.stderr(formatValidationFailure("Cannot render provider execution follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up delivery follow-up reconciliation from an invalid receipt source chain.", sourceValidation.issues));
    return 1;
  }

  const reconciliation =
    buildUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
      plan,
      receipt
    );
  await writeFile(outputPath, `${JSON.stringify(reconciliation, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command:
          "render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation",
        reconciliation_status: reconciliation.reconciliation_status,
        recommended_follow_up: reconciliation.recommended_follow_up,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const sourcePaths = readRequiredPathSlice(args, 50);
  const planPath = args[50];
  const receiptPath = args[51];
  const reconciliationPath = args[52];
  if (!sourcePaths || !planPath || !receiptPath || !reconciliationPath) {
    io.stderr("Missing latest provider post-delivery routing source chain paths, plan path, receipt path, or reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const source = await readJsonPathSlice(sourcePaths);
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const reconciliation = JSON.parse(await readFile(reconciliationPath, "utf8"));
  const sourceValidation =
    callFixedArityValidator(
      validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliationSource,
      [...source, plan, receipt]
    );
  const reconciliationValidation =
    validateUnitReviewProviderExecutionFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpDeliveryFollowUpReconciliation(
      plan,
      receipt,
      reconciliation
    );
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_reconciliation_source_contract",
      ...issue
    })),
    ...reconciliationValidation.issues.map((issue) => ({
      stage:
        "provider_execution_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_delivery_follow_up_reconciliation",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command:
          "validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}



function readRequiredPathSlice(args: readonly string[], count: number): string[] | null {
  const paths = args.slice(0, count);
  return paths.length === count && paths.every((path) => Boolean(path)) ? paths : null;
}

async function readJsonPathSlice(paths: readonly string[]): Promise<any[]> {
  return await Promise.all(paths.map(async (path) => JSON.parse(await readFile(path, "utf8"))));
}

function callFixedArityValidator<TResult>(
  validator: (...args: any[]) => TResult,
  args: readonly any[]
): TResult {
  return validator(...args);
}

function callFixedArityValidatorSafely(
  validator: (...args: any[]) => { ok: boolean; issues: readonly { code: string; message: string }[] },
  args: readonly any[],
  issueCode: string
): { ok: boolean; issues: readonly { code: string; message: string }[] } {
  try {
    return validator(...args);
  } catch {
    return {
      ok: false,
      issues: [
        {
          code: issueCode,
          message: "Validation failed before contract checks could complete.",
        },
      ],
    };
  }
}

function formatValidationFailure(
  headline: string,
  issues: readonly { code: string; message: string }[]
): string {
  return [headline, ...issues.map((issue) => `- ${issue.code}: ${issue.message}`)].join("\n");
}

async function runRenderReviewInboxItem(args: readonly string[], io: ContentPipelineCliIO): Promise<number> {
  const artifactPath = args[0];
  if (!artifactPath) {
    io.stderr("Missing review artifact path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-inbox-item.json>.");
    io.stderr(usage());
    return 1;
  }

  const artifact = JSON.parse(await readFile(artifactPath, "utf8")) as UnitReviewArtifact;
  const handoff = buildUnitReviewInboxHandoff(artifact);
  await writeFile(outputPath, `${JSON.stringify(handoff, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "render-review-inbox-item",
        unit_id: handoff.unit_id,
        human_queue: handoff.human_queue,
        primary_human_action: handoff.primary_human_action,
        delivery_action: handoff.delivery_action,
        predecessor_item_key: handoff.predecessor_item_key,
        item_key: handoff.item_key,
        output_path: outputPath
      },
      null,
      2
    )
  );
  return 0;
}

async function runRenderReviewInboxDeliveryPlan(args: readonly string[], io: ContentPipelineCliIO): Promise<number> {
  const artifactPath = args[0];
  if (!artifactPath) {
    io.stderr("Missing review artifact path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-inbox-delivery-plan.json>.");
    io.stderr(usage());
    return 1;
  }

  const artifact = JSON.parse(await readFile(artifactPath, "utf8")) as UnitReviewArtifact;
  const handoff = buildUnitReviewInboxHandoff(artifact);
  const deliveryPlan = buildUnitReviewInboxDeliveryPlan(handoff);
  await writeFile(outputPath, `${JSON.stringify(deliveryPlan, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "render-review-inbox-delivery-plan",
        unit_id: artifact.unit_id,
        delivery_action: deliveryPlan.delivery_action,
        upsert_operation_key: deliveryPlan.upsert.operation_key,
        close_count: deliveryPlan.close.length,
        final_active_item_key: deliveryPlan.final_active_item_key,
        output_path: outputPath
      },
      null,
      2
    )
  );
  return 0;
}

async function runValidateReviewInboxDeliveryPlan(args: readonly string[], io: ContentPipelineCliIO): Promise<number> {
  const planPath = args[0];
  if (!planPath) {
    io.stderr("Missing inbox delivery plan path.");
    io.stderr(usage());
    return 1;
  }

  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const validation = validateUnitReviewInboxDeliveryPlan(plan);

  io.stdout(
    JSON.stringify(
      {
        ok: validation.ok,
        command: "validate-review-inbox-delivery-plan",
        issue_count: validation.issues.length,
        issues: validation.issues
      },
      null,
      2
    )
  );

  return validation.ok ? 0 : 1;
}

async function runRenderReviewInboxDeliveryReceipt(args: readonly string[], io: ContentPipelineCliIO): Promise<number> {
  const planPath = args[0];
  if (!planPath) {
    io.stderr("Missing inbox delivery plan path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-inbox-delivery-receipt.json>.");
    io.stderr(usage());
    return 1;
  }

  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const upsertStatus = readOperationStatusFlag(args, "--upsert-status");
  const closeStatus = readOperationStatusFlag(args, "--close-status");
  const executedAt = readFlagValue(args, "--executed-at") ?? new Date().toISOString();
  const receipt = buildUnitReviewInboxDeliveryReceipt(plan, {
    executed_at: executedAt,
    operation_statuses: {
      [plan.upsert.operation_key]: upsertStatus,
      ...Object.fromEntries(
        plan.close.map((closeItem: { operation_key: string }) => [closeItem.operation_key, closeStatus])
      )
    }
  });
  await writeFile(outputPath, `${JSON.stringify(receipt, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "render-review-inbox-delivery-receipt",
        overall_status: receipt.overall_status,
        failed_operation_count: receipt.operations.filter((operation) => operation.status === "failed").length,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewInboxDeliveryReceipt(args: readonly string[], io: ContentPipelineCliIO): Promise<number> {
  const planPath = args[0];
  const receiptPath = args[1];
  if (!planPath || !receiptPath) {
    io.stderr("Missing inbox delivery plan path or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const validation = validateUnitReviewInboxDeliveryReceipt(plan, receipt);

  io.stdout(
    JSON.stringify(
      {
        ok: validation.ok,
        command: "validate-review-inbox-delivery-receipt",
        issue_count: validation.issues.length,
        issues: validation.issues
      },
      null,
      2
    )
  );

  return validation.ok ? 0 : 1;
}

async function runRenderReviewInboxDeliveryReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const planPath = args[0];
  const receiptPath = args[1];
  if (!planPath || !receiptPath) {
    io.stderr("Missing inbox delivery plan path or receipt path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-inbox-delivery-reconciliation.json>.");
    io.stderr(usage());
    return 1;
  }

  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const reconciliation = buildUnitReviewInboxDeliveryReconciliation(plan, receipt);
  await writeFile(outputPath, `${JSON.stringify(reconciliation, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "render-review-inbox-delivery-reconciliation",
        reconciliation_status: reconciliation.reconciliation_status,
        recommended_follow_up: reconciliation.recommended_follow_up,
        unresolved_operation_count: reconciliation.unresolved_operations.length,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewInboxDeliveryReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const planPath = args[0];
  const receiptPath = args[1];
  const reconciliationPath = args[2];
  if (!planPath || !receiptPath || !reconciliationPath) {
    io.stderr("Missing inbox delivery plan path, receipt path, or reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const reconciliation = JSON.parse(await readFile(reconciliationPath, "utf8"));
  const validation = validateUnitReviewInboxDeliveryReconciliation(plan, receipt, reconciliation);

  io.stdout(
    JSON.stringify(
      {
        ok: validation.ok,
        command: "validate-review-inbox-delivery-reconciliation",
        issue_count: validation.issues.length,
        issues: validation.issues
      },
      null,
      2
    )
  );

  return validation.ok ? 0 : 1;
}

async function runRenderReviewInboxDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const planPath = args[0];
  const reconciliationPath = args[1];
  if (!planPath || !reconciliationPath) {
    io.stderr("Missing inbox delivery plan path or reconciliation path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-inbox-delivery-follow-up.json>.");
    io.stderr(usage());
    return 1;
  }

  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const reconciliation = JSON.parse(await readFile(reconciliationPath, "utf8"));
  const reconciliationValidation = validateUnitReviewInboxDeliveryFollowUpSource(plan, reconciliation);
  if (!reconciliationValidation.ok) {
    io.stderr(
      [
        "Cannot render inbox delivery follow-up from an invalid reconciliation contract.",
        ...reconciliationValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }
  const followUp = buildUnitReviewInboxDeliveryFollowUp(plan, reconciliation);
  await writeFile(outputPath, `${JSON.stringify(followUp, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "render-review-inbox-delivery-follow-up",
        follow_up_state: followUp.follow_up_state,
        follow_up_action: followUp.follow_up_action,
        delivery_chain_closed: followUp.delivery_chain_closed,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewInboxDeliveryFollowUp(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const planPath = args[0];
  const reconciliationPath = args[1];
  const followUpPath = args[2];
  if (!planPath || !reconciliationPath || !followUpPath) {
    io.stderr("Missing inbox delivery plan path, reconciliation path, or follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const reconciliation = JSON.parse(await readFile(reconciliationPath, "utf8"));
  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const validation = validateUnitReviewInboxDeliveryFollowUp(plan, reconciliation, followUp);

  io.stdout(
    JSON.stringify(
      {
        ok: validation.ok,
        command: "validate-review-inbox-delivery-follow-up",
        issue_count: validation.issues.length,
        issues: validation.issues
      },
      null,
      2
    )
  );

  return validation.ok ? 0 : 1;
}

async function runRenderReviewInboxDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const followUpPath = args[0];
  if (!followUpPath) {
    io.stderr("Missing inbox delivery follow-up path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-inbox-delivery-follow-up-plan.json>.");
    io.stderr(usage());
    return 1;
  }

  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const sourceValidation = validateUnitReviewInboxDeliveryFollowUpSourceContract(followUp);
  if (!sourceValidation.ok) {
    io.stderr(
      [
        "Cannot render inbox delivery follow-up plan from an invalid follow-up contract.",
        ...sourceValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }
  const plan = buildUnitReviewInboxDeliveryFollowUpPlan(followUp);
  await writeFile(outputPath, `${JSON.stringify(plan, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "render-review-inbox-delivery-follow-up-plan",
        delivery_action: plan.delivery_action,
        final_follow_up_item_key: plan.final_follow_up_item_key,
        preserved_active_item_key: plan.preserved_active_item.item_key,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewInboxDeliveryFollowUpPlan(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const followUpPath = args[0];
  const planPath = args[1];
  if (!followUpPath || !planPath) {
    io.stderr("Missing inbox delivery follow-up path or follow-up delivery plan path.");
    io.stderr(usage());
    return 1;
  }

  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const sourceValidation = validateUnitReviewInboxDeliveryFollowUpSourceContract(followUp);
  const planValidation = validateUnitReviewInboxDeliveryFollowUpPlan(followUp, plan);
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: "follow_up_source_contract",
      ...issue
    })),
    ...planValidation.issues.map((issue) => ({
      stage: "follow_up_plan",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: "validate-review-inbox-delivery-follow-up-plan",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewInboxDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const followUpPath = args[0];
  const planPath = args[1];
  if (!followUpPath || !planPath) {
    io.stderr("Missing inbox delivery follow-up path or follow-up delivery plan path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-inbox-delivery-follow-up-receipt.json>.");
    io.stderr(usage());
    return 1;
  }

  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const sourceValidation = validateUnitReviewInboxDeliveryFollowUpSourceContract(followUp);
  if (!sourceValidation.ok) {
    io.stderr(
      [
        "Cannot render inbox delivery follow-up receipt from an invalid follow-up contract.",
        ...sourceValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }

  const planValidation = validateUnitReviewInboxDeliveryFollowUpPlan(followUp, plan);
  if (!planValidation.ok) {
    io.stderr(
      [
        "Cannot render inbox delivery follow-up receipt from an invalid follow-up delivery plan.",
        ...planValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }

  const upsertStatus = readOperationStatusFlag(args, "--upsert-status");
  const executedAt = readFlagValue(args, "--executed-at") ?? new Date().toISOString();
  const receipt = buildUnitReviewInboxDeliveryFollowUpReceipt(plan, {
    executed_at: executedAt,
    ...(upsertStatus ? { upsert_status: upsertStatus } : {})
  });
  await writeFile(outputPath, `${JSON.stringify(receipt, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "render-review-inbox-delivery-follow-up-receipt",
        overall_status: receipt.overall_status,
        operation_count: receipt.operations.length,
        failed_operation_count: receipt.operations.filter((operation) => operation.status === "failed").length,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewInboxDeliveryFollowUpReceipt(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const followUpPath = args[0];
  const planPath = args[1];
  const receiptPath = args[2];
  if (!followUpPath || !planPath || !receiptPath) {
    io.stderr("Missing inbox delivery follow-up path, follow-up delivery plan path, or follow-up receipt path.");
    io.stderr(usage());
    return 1;
  }

  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const sourceValidation = validateUnitReviewInboxDeliveryFollowUpSourceContract(followUp);
  const planValidation = validateUnitReviewInboxDeliveryFollowUpPlan(followUp, plan);
  const receiptValidation =
    sourceValidation.ok && planValidation.ok
      ? validateUnitReviewInboxDeliveryFollowUpReceipt(plan, receipt)
      : { ok: false, issues: [] };
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: "follow_up_source_contract",
      ...issue
    })),
    ...planValidation.issues.map((issue) => ({
      stage: "follow_up_plan",
      ...issue
    })),
    ...receiptValidation.issues.map((issue) => ({
      stage: "follow_up_receipt",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: "validate-review-inbox-delivery-follow-up-receipt",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runRenderReviewInboxDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const followUpPath = args[0];
  const planPath = args[1];
  const receiptPath = args[2];
  if (!followUpPath || !planPath || !receiptPath) {
    io.stderr("Missing inbox delivery follow-up path, follow-up delivery plan path, or follow-up receipt path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <review-inbox-delivery-follow-up-reconciliation.json>.");
    io.stderr(usage());
    return 1;
  }

  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const sourceValidation = validateUnitReviewInboxDeliveryFollowUpSourceContract(followUp);
  if (!sourceValidation.ok) {
    io.stderr(
      [
        "Cannot render inbox delivery follow-up reconciliation from an invalid follow-up contract.",
        ...sourceValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }

  const planValidation = validateUnitReviewInboxDeliveryFollowUpPlan(followUp, plan);
  if (!planValidation.ok) {
    io.stderr(
      [
        "Cannot render inbox delivery follow-up reconciliation from an invalid follow-up delivery plan.",
        ...planValidation.issues.map((issue) => `- ${issue.code}: ${issue.message}`)
      ].join("\n")
    );
    return 1;
  }

  const reconciliation = buildUnitReviewInboxDeliveryFollowUpReconciliation(plan, receipt);
  await writeFile(outputPath, `${JSON.stringify(reconciliation, null, 2)}\n`, "utf8");

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "render-review-inbox-delivery-follow-up-reconciliation",
        reconciliation_status: reconciliation.reconciliation_status,
        recommended_follow_up: reconciliation.recommended_follow_up,
        unresolved_operation_count: reconciliation.unresolved_operations.length,
        output_path: outputPath
      },
      null,
      2
    )
  );

  return 0;
}

async function runValidateReviewInboxDeliveryFollowUpReconciliation(
  args: readonly string[],
  io: ContentPipelineCliIO
): Promise<number> {
  const followUpPath = args[0];
  const planPath = args[1];
  const receiptPath = args[2];
  const reconciliationPath = args[3];
  if (!followUpPath || !planPath || !receiptPath || !reconciliationPath) {
    io.stderr(
      "Missing inbox delivery follow-up path, follow-up delivery plan path, follow-up receipt path, or follow-up reconciliation path."
    );
    io.stderr(usage());
    return 1;
  }

  const followUp = JSON.parse(await readFile(followUpPath, "utf8"));
  const plan = JSON.parse(await readFile(planPath, "utf8"));
  const receipt = JSON.parse(await readFile(receiptPath, "utf8"));
  const reconciliation = JSON.parse(await readFile(reconciliationPath, "utf8"));
  const sourceValidation = validateUnitReviewInboxDeliveryFollowUpSourceContract(followUp);
  const planValidation = validateUnitReviewInboxDeliveryFollowUpPlan(followUp, plan);
  const reconciliationValidation =
    sourceValidation.ok && planValidation.ok
      ? validateUnitReviewInboxDeliveryFollowUpReconciliation(plan, receipt, reconciliation)
      : { ok: false, issues: [] };
  const issues = [
    ...sourceValidation.issues.map((issue) => ({
      stage: "follow_up_source_contract",
      ...issue
    })),
    ...planValidation.issues.map((issue) => ({
      stage: "follow_up_plan",
      ...issue
    })),
    ...reconciliationValidation.issues.map((issue) => ({
      stage: "follow_up_reconciliation",
      ...issue
    }))
  ];

  io.stdout(
    JSON.stringify(
      {
        ok: issues.length === 0,
        command: "validate-review-inbox-delivery-follow-up-reconciliation",
        issue_count: issues.length,
        issues
      },
      null,
      2
    )
  );

  return issues.length === 0 ? 0 : 1;
}

async function runApproveReviewArtifact(args: readonly string[], io: ContentPipelineCliIO): Promise<number> {
  const artifactPath = args[0];
  if (!artifactPath) {
    io.stderr("Missing review artifact path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <approved-review-artifact.json>. This command never overwrites the source artifact.");
    io.stderr(usage());
    return 1;
  }

  const reviewerId = readFlagValue(args, "--reviewer");
  if (!reviewerId) {
    io.stderr("Missing --reviewer <reviewer-id>.");
    io.stderr(usage());
    return 1;
  }

  const artifact = JSON.parse(await readFile(artifactPath, "utf8")) as UnitReviewArtifact;
  const notes = readFlagValue(args, "--notes");
  const approvedArtifact = approveReviewArtifact(artifact, {
    reviewer_id: reviewerId,
    ...(notes ? { notes } : {})
  });
  await writeFile(outputPath, `${JSON.stringify(approvedArtifact, null, 2)}\n`, {
    encoding: "utf8",
    flag: "wx"
  });

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "approve-review-artifact",
        unit_id: approvedArtifact.unit_id,
        approval_status: approvedArtifact.approval?.status,
        reviewer_id: approvedArtifact.approval?.reviewer_id,
        source_artifact_path: artifactPath,
        output_path: outputPath,
        source_overwritten: false
      },
      null,
      2
    )
  );
  return 0;
}

async function runApplyReviewedPatch(args: readonly string[], io: ContentPipelineCliIO): Promise<number> {
  const unitPath = args[0];
  const artifactPath = args[1];
  if (!unitPath || !artifactPath) {
    io.stderr("Missing unit yaml path or review artifact path.");
    io.stderr(usage());
    return 1;
  }

  const outputPath = readFlagValue(args, "--out");
  if (!outputPath) {
    io.stderr("Missing --out <candidate-unit.yaml>. This command never overwrites the source unit.");
    io.stderr(usage());
    return 1;
  }

  if (!args.includes("--confirm-reviewed")) {
    io.stderr("Missing --confirm-reviewed. Apply requires an approved artifact and explicit confirmation.");
    io.stderr(usage());
    return 1;
  }

  const unit = await loadUnitSpecFromFile(unitPath);
  const artifact = JSON.parse(await readFile(artifactPath, "utf8")) as UnitReviewArtifact;
  const result = applyReviewedPatchesToUnit(unit, artifact);
  const semantic = validateUnitSemanticIntegrity(result.unit);
  if (!semantic.passed) {
    throw new Error(
      [
        "Candidate unit failed semantic validation.",
        ...semantic.issues
          .filter((issue) => issue.severity === "error")
          .map((issue) => `${issue.code} at ${issue.path}: ${issue.message}`)
      ].join("\n")
    );
  }

  await writeFile(outputPath, serializeUnitSpecToYaml(result.unit), {
    encoding: "utf8",
    flag: "wx"
  });

  io.stdout(
    JSON.stringify(
      {
        ok: true,
        command: "apply-reviewed-patch",
        unit_id: result.unit.metadata.unit_id,
        applied_patch_count: result.applied_patch_count,
        source_unit_path: unitPath,
        review_artifact_path: artifactPath,
        output_path: outputPath,
        source_overwritten: false
      },
      null,
      2
    )
  );
  return 0;
}

function readFlagValue(args: readonly string[], flag: string): string | undefined {
  const index = args.indexOf(flag);
  if (index === -1) {
    return undefined;
  }

  return args[index + 1];
}

function readOperationStatusFlag(
  args: readonly string[],
  flag: string
): UnitReviewInboxDeliveryOperationStatus | undefined {
  const value = readFlagValue(args, flag);
  if (!value) {
    return undefined;
  }

  if (value === "applied" || value === "already_applied" || value === "failed") {
    return value;
  }

  throw new Error(`${flag} must be one of: applied, already_applied, failed.`);
}

function usage(): string {
  const n12ProviderFollowUp = `provider-execution-follow-up${"-delivery-follow-up".repeat(12)}`;
  const n12ProviderReview = `review-${n12ProviderFollowUp}`;

  return [
    "Usage:",
    "  content-pipeline validate-unit <unit.yaml>",
    "  content-pipeline run-workflow-smoke <unit.yaml> [--log <workflow-log.json>]",
    "  content-pipeline run-llm-mock <unit.yaml> [--log <invocation-log.json>]",
    "  content-pipeline run-llm-review <unit.yaml> --review <review-artifact.json> [--from-artifact <prior-review-artifact.json>] [--rerun-from <role>]",
    "  content-pipeline render-review-report <review-artifact.json> --out <review-report.md>",
    "  content-pipeline render-review-repair-request <review-artifact.json> --out <review-repair-request.json>",
    "  content-pipeline validate-review-repair-request <review-artifact.json> <review-repair-request.json>",
    "  content-pipeline render-review-provider-execution-request <review-artifact.json> --out <review-provider-execution-request.json>",
    "  content-pipeline validate-review-provider-execution-request <review-artifact.json> <review-provider-execution-request.json>",
    "  content-pipeline decide-review-provider-execution-request <review-provider-execution-request.json> --reviewer <reviewer-id> --decision <approve|reject> --budget-check <passed|failed> --out <review-provider-execution-decision.json> [--notes <notes>] [--budget-reference <reference>]",
    "  content-pipeline validate-review-provider-execution-decision <review-provider-execution-request.json> <review-provider-execution-decision.json>",
    "  content-pipeline record-review-provider-execution-attempt <review-provider-execution-request.json> <review-provider-execution-decision.json> --recorded-by <operator-id> --out <review-provider-execution-attempt.json> [--recorded-at <timestamp>] [--attempt-notes <notes>]",
    "  content-pipeline validate-review-provider-execution-attempt <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json>",
    "  content-pipeline record-review-provider-execution-receipt <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> --executed-by <operator-id> --receipt-status <artifact_recorded|execution_failed> --actual-provider-call-count <non-negative-integer> --out <review-provider-execution-receipt.json> [--executed-at <timestamp>] [--result-artifact-generated-at <timestamp>] [--result-artifact-status <ready_for_human_review|blocked>] [--failure-code <code>] [--failure-summary <summary>]",
    "  content-pipeline validate-review-provider-execution-receipt <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json>",
    "  content-pipeline render-review-provider-execution-reconciliation <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> --out <review-provider-execution-reconciliation.json>",
    "  content-pipeline validate-review-provider-execution-reconciliation <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json>",
    "  content-pipeline render-review-provider-execution-follow-up <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> --out <review-provider-execution-follow-up.json>",
    "  content-pipeline validate-review-provider-execution-follow-up <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json>",
    "  content-pipeline render-review-provider-execution-follow-up-plan <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> --out <review-provider-execution-follow-up-plan.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-plan <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json>",
    "  content-pipeline render-review-provider-execution-follow-up-receipt <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> [--upsert-status <applied|already_applied|failed>] --out <review-provider-execution-follow-up-receipt.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-receipt <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json>",
    "  content-pipeline render-review-provider-execution-follow-up-reconciliation <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> --out <review-provider-execution-follow-up-reconciliation.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-reconciliation <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> --out <review-provider-execution-follow-up-delivery-follow-up.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-plan <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up.json> --out <review-provider-execution-follow-up-delivery-follow-up-plan.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-plan <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-plan.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-receipt <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-plan.json> [--upsert-status <applied|already_applied|failed>] --out <review-provider-execution-follow-up-delivery-follow-up-receipt.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-receipt <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-receipt.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-reconciliation <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-receipt.json> --out <review-provider-execution-follow-up-delivery-follow-up-reconciliation.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-reconciliation <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-reconciliation.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-reconciliation.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-plan <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-plan.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-plan <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-plan.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-receipt <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-plan.json> [--upsert-status <applied|already_applied|failed>] --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-receipt <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-reconciliation <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-reconciliation <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> [--upsert-status <applied|already_applied|failed>] --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up <review-provider-execution-request.json> <review-provider-execution-decision.json> <review-provider-execution-attempt.json> <review-provider-execution-receipt.json> <review-provider-execution-reconciliation.json> <review-provider-execution-follow-up.json> <review-provider-execution-follow-up-plan.json> <review-provider-execution-follow-up-receipt.json> <review-provider-execution-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> [--upsert-status <applied|already_applied|failed>] --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> [--upsert-status <applied|already_applied|failed>] --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> [--upsert-status <applied|already_applied|failed>] --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> [--upsert-status <applied|already_applied|failed>] --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> [--upsert-status <applied|already_applied|failed>] --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> [--upsert-status <applied|already_applied|failed>] --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up <newest-provider-post-delivery-routing-source-chain...> <n11-follow-up-plan.json> <n11-follow-up-receipt.json> <n11-follow-up-reconciliation.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up <newest-provider-post-delivery-routing-source-chain...> <n11-follow-up-plan.json> <n11-follow-up-receipt.json> <n11-follow-up-reconciliation.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json>",
    `  content-pipeline render-review-${n12ProviderFollowUp}-plan <latest-provider-post-delivery-routing-source-chain...> <${n12ProviderReview}.json> --out <${n12ProviderReview}-plan.json>`,
    `  content-pipeline validate-review-${n12ProviderFollowUp}-plan <latest-provider-post-delivery-routing-source-chain...> <${n12ProviderReview}.json> <${n12ProviderReview}-plan.json>`,
    `  content-pipeline render-review-${n12ProviderFollowUp}-receipt <latest-provider-post-delivery-routing-source-chain...> <${n12ProviderReview}.json> <${n12ProviderReview}-plan.json> [--upsert-status <applied|already_applied|failed>] --out <${n12ProviderReview}-receipt.json>`,
    `  content-pipeline validate-review-${n12ProviderFollowUp}-receipt <latest-provider-post-delivery-routing-source-chain...> <${n12ProviderReview}.json> <${n12ProviderReview}-plan.json> <${n12ProviderReview}-receipt.json>`,
    `  content-pipeline render-review-${n12ProviderFollowUp}-reconciliation <latest-provider-post-delivery-routing-source-chain...> <${n12ProviderReview}.json> <${n12ProviderReview}-plan.json> <${n12ProviderReview}-receipt.json> --out <${n12ProviderReview}-reconciliation.json>`,
    `  content-pipeline validate-review-${n12ProviderFollowUp}-reconciliation <latest-provider-post-delivery-routing-source-chain...> <${n12ProviderReview}.json> <${n12ProviderReview}-plan.json> <${n12ProviderReview}-receipt.json> <${n12ProviderReview}-reconciliation.json>`,
    `  content-pipeline render-review-${N13_PROVIDER_FOLLOW_UP} <latest-provider-post-delivery-routing-source-chain...> <${n12ProviderReview}-plan.json> <${n12ProviderReview}-receipt.json> <${n12ProviderReview}-reconciliation.json> --out <${N13_PROVIDER_REVIEW}.json>`,
    `  content-pipeline validate-review-${N13_PROVIDER_FOLLOW_UP} <latest-provider-post-delivery-routing-source-chain...> <${n12ProviderReview}-plan.json> <${n12ProviderReview}-receipt.json> <${n12ProviderReview}-reconciliation.json> <${N13_PROVIDER_REVIEW}.json>`,
    `  content-pipeline render-review-${N13_PROVIDER_FOLLOW_UP}-plan <latest-provider-post-delivery-routing-source-chain...> <${n12ProviderReview}-plan.json> <${n12ProviderReview}-receipt.json> <${n12ProviderReview}-reconciliation.json> <${N13_PROVIDER_REVIEW}.json> --out <${N13_PROVIDER_REVIEW}-plan.json>`,
    `  content-pipeline validate-review-${N13_PROVIDER_FOLLOW_UP}-plan <latest-provider-post-delivery-routing-source-chain...> <${n12ProviderReview}-plan.json> <${n12ProviderReview}-receipt.json> <${n12ProviderReview}-reconciliation.json> <${N13_PROVIDER_REVIEW}.json> <${N13_PROVIDER_REVIEW}-plan.json>`,
    `  content-pipeline render-review-${N13_PROVIDER_FOLLOW_UP}-receipt <latest-provider-post-delivery-routing-source-chain...> <${n12ProviderReview}-plan.json> <${n12ProviderReview}-receipt.json> <${n12ProviderReview}-reconciliation.json> <${N13_PROVIDER_REVIEW}.json> <${N13_PROVIDER_REVIEW}-plan.json> [--upsert-status <applied|already_applied|failed>] --out <${N13_PROVIDER_REVIEW}-receipt.json>`,
    `  content-pipeline validate-review-${N13_PROVIDER_FOLLOW_UP}-receipt <latest-provider-post-delivery-routing-source-chain...> <${n12ProviderReview}-plan.json> <${n12ProviderReview}-receipt.json> <${n12ProviderReview}-reconciliation.json> <${N13_PROVIDER_REVIEW}.json> <${N13_PROVIDER_REVIEW}-plan.json> <${N13_PROVIDER_REVIEW}-receipt.json>`,
    `  content-pipeline render-review-${N13_PROVIDER_FOLLOW_UP}-reconciliation <latest-provider-post-delivery-routing-source-chain...> <${n12ProviderReview}-plan.json> <${n12ProviderReview}-receipt.json> <${n12ProviderReview}-reconciliation.json> <${N13_PROVIDER_REVIEW}.json> <${N13_PROVIDER_REVIEW}-plan.json> <${N13_PROVIDER_REVIEW}-receipt.json> --out <${N13_PROVIDER_REVIEW}-reconciliation.json>`,
    `  content-pipeline validate-review-${N13_PROVIDER_FOLLOW_UP}-reconciliation <latest-provider-post-delivery-routing-source-chain...> <${n12ProviderReview}-plan.json> <${n12ProviderReview}-receipt.json> <${n12ProviderReview}-reconciliation.json> <${N13_PROVIDER_REVIEW}.json> <${N13_PROVIDER_REVIEW}-plan.json> <${N13_PROVIDER_REVIEW}-receipt.json> <${N13_PROVIDER_REVIEW}-reconciliation.json>`,
    `  content-pipeline render-review-${N14_PROVIDER_FOLLOW_UP} <latest-provider-post-delivery-routing-source-chain...> <${N13_PROVIDER_REVIEW}.json> <${N13_PROVIDER_REVIEW}-plan.json> <${N13_PROVIDER_REVIEW}-receipt.json> <${N13_PROVIDER_REVIEW}-reconciliation.json> --out <${N14_PROVIDER_REVIEW}.json>`,
    `  content-pipeline validate-review-${N14_PROVIDER_FOLLOW_UP} <latest-provider-post-delivery-routing-source-chain...> <${N13_PROVIDER_REVIEW}.json> <${N13_PROVIDER_REVIEW}-plan.json> <${N13_PROVIDER_REVIEW}-receipt.json> <${N13_PROVIDER_REVIEW}-reconciliation.json> <${N14_PROVIDER_REVIEW}.json>`,
    `  content-pipeline render-review-${N14_PROVIDER_FOLLOW_UP}-plan <latest-provider-post-delivery-routing-source-chain...> <${N13_PROVIDER_REVIEW}.json> <${N13_PROVIDER_REVIEW}-plan.json> <${N13_PROVIDER_REVIEW}-receipt.json> <${N13_PROVIDER_REVIEW}-reconciliation.json> <${N14_PROVIDER_REVIEW}.json> --out <${N14_PROVIDER_REVIEW}-plan.json>`,
    `  content-pipeline validate-review-${N14_PROVIDER_FOLLOW_UP}-plan <latest-provider-post-delivery-routing-source-chain...> <${N13_PROVIDER_REVIEW}.json> <${N13_PROVIDER_REVIEW}-plan.json> <${N13_PROVIDER_REVIEW}-receipt.json> <${N13_PROVIDER_REVIEW}-reconciliation.json> <${N14_PROVIDER_REVIEW}.json> <${N14_PROVIDER_REVIEW}-plan.json>`,
    `  content-pipeline render-review-${N14_PROVIDER_FOLLOW_UP}-receipt <latest-provider-post-delivery-routing-source-chain...> <${N13_PROVIDER_REVIEW}.json> <${N13_PROVIDER_REVIEW}-plan.json> <${N13_PROVIDER_REVIEW}-receipt.json> <${N13_PROVIDER_REVIEW}-reconciliation.json> <${N14_PROVIDER_REVIEW}.json> <${N14_PROVIDER_REVIEW}-plan.json> [--upsert-status <applied|already_applied|failed>] --out <${N14_PROVIDER_REVIEW}-receipt.json>`,
    `  content-pipeline validate-review-${N14_PROVIDER_FOLLOW_UP}-receipt <latest-provider-post-delivery-routing-source-chain...> <${N13_PROVIDER_REVIEW}.json> <${N13_PROVIDER_REVIEW}-plan.json> <${N13_PROVIDER_REVIEW}-receipt.json> <${N13_PROVIDER_REVIEW}-reconciliation.json> <${N14_PROVIDER_REVIEW}.json> <${N14_PROVIDER_REVIEW}-plan.json> <${N14_PROVIDER_REVIEW}-receipt.json>`,
    `  content-pipeline render-review-${N14_PROVIDER_FOLLOW_UP}-reconciliation <latest-provider-post-delivery-routing-source-chain...> <${N13_PROVIDER_REVIEW}.json> <${N13_PROVIDER_REVIEW}-plan.json> <${N13_PROVIDER_REVIEW}-receipt.json> <${N13_PROVIDER_REVIEW}-reconciliation.json> <${N14_PROVIDER_REVIEW}.json> <${N14_PROVIDER_REVIEW}-plan.json> <${N14_PROVIDER_REVIEW}-receipt.json> --out <${N14_PROVIDER_REVIEW}-reconciliation.json>`,
    `  content-pipeline validate-review-${N14_PROVIDER_FOLLOW_UP}-reconciliation <latest-provider-post-delivery-routing-source-chain...> <${N13_PROVIDER_REVIEW}.json> <${N13_PROVIDER_REVIEW}-plan.json> <${N13_PROVIDER_REVIEW}-receipt.json> <${N13_PROVIDER_REVIEW}-reconciliation.json> <${N14_PROVIDER_REVIEW}.json> <${N14_PROVIDER_REVIEW}-plan.json> <${N14_PROVIDER_REVIEW}-receipt.json> <${N14_PROVIDER_REVIEW}-reconciliation.json>`,
    `  content-pipeline render-review-${N15_PROVIDER_FOLLOW_UP} <latest-provider-post-delivery-routing-source-chain...> <${N14_PROVIDER_REVIEW}-plan.json> <${N14_PROVIDER_REVIEW}-receipt.json> <${N14_PROVIDER_REVIEW}-reconciliation.json> --out <${N15_PROVIDER_REVIEW}.json>`,
    `  content-pipeline validate-review-${N15_PROVIDER_FOLLOW_UP} <latest-provider-post-delivery-routing-source-chain...> <${N14_PROVIDER_REVIEW}-plan.json> <${N14_PROVIDER_REVIEW}-receipt.json> <${N14_PROVIDER_REVIEW}-reconciliation.json> <${N15_PROVIDER_REVIEW}.json>`,
    `  content-pipeline render-review-${N16_PROVIDER_FOLLOW_UP} <latest-provider-post-delivery-routing-source-chain...> <${N15_PROVIDER_REVIEW}-plan.json> <${N15_PROVIDER_REVIEW}-receipt.json> <${N15_PROVIDER_REVIEW}-reconciliation.json> --out <${N16_PROVIDER_REVIEW}.json>`,
    `  content-pipeline validate-review-${N16_PROVIDER_FOLLOW_UP} <latest-provider-post-delivery-routing-source-chain...> <${N15_PROVIDER_REVIEW}-plan.json> <${N15_PROVIDER_REVIEW}-receipt.json> <${N15_PROVIDER_REVIEW}-reconciliation.json> <${N16_PROVIDER_REVIEW}.json>`,
    `  content-pipeline render-review-${N16_PROVIDER_FOLLOW_UP}-plan <latest-provider-post-delivery-routing-source-chain...> <${N16_PROVIDER_REVIEW}.json> --out <${N16_PROVIDER_REVIEW}-plan.json>`,
    `  content-pipeline validate-review-${N16_PROVIDER_FOLLOW_UP}-plan <latest-provider-post-delivery-routing-source-chain...> <${N16_PROVIDER_REVIEW}.json> <${N16_PROVIDER_REVIEW}-plan.json>`,
    `  content-pipeline render-review-${N16_PROVIDER_FOLLOW_UP}-receipt <latest-provider-post-delivery-routing-source-chain...> <${N16_PROVIDER_REVIEW}.json> <${N16_PROVIDER_REVIEW}-plan.json> [--upsert-status <applied|already_applied|failed>] --out <${N16_PROVIDER_REVIEW}-receipt.json>`,
    `  content-pipeline validate-review-${N16_PROVIDER_FOLLOW_UP}-receipt <latest-provider-post-delivery-routing-source-chain...> <${N16_PROVIDER_REVIEW}.json> <${N16_PROVIDER_REVIEW}-plan.json> <${N16_PROVIDER_REVIEW}-receipt.json>`,
    `  content-pipeline render-review-${N16_PROVIDER_FOLLOW_UP}-reconciliation <latest-provider-post-delivery-routing-source-chain...> <${N16_PROVIDER_REVIEW}.json> <${N16_PROVIDER_REVIEW}-plan.json> <${N16_PROVIDER_REVIEW}-receipt.json> --out <${N16_PROVIDER_REVIEW}-reconciliation.json>`,
    `  content-pipeline validate-review-${N16_PROVIDER_FOLLOW_UP}-reconciliation <latest-provider-post-delivery-routing-source-chain...> <${N16_PROVIDER_REVIEW}.json> <${N16_PROVIDER_REVIEW}-plan.json> <${N16_PROVIDER_REVIEW}-receipt.json> <${N16_PROVIDER_REVIEW}-reconciliation.json>`,
    `  content-pipeline render-review-${N17_PROVIDER_FOLLOW_UP} <latest-provider-post-delivery-routing-source-chain...> <${N16_PROVIDER_REVIEW}-plan.json> <${N16_PROVIDER_REVIEW}-receipt.json> <${N16_PROVIDER_REVIEW}-reconciliation.json> --out <${N17_PROVIDER_REVIEW}.json>`,
    `  content-pipeline validate-review-${N17_PROVIDER_FOLLOW_UP} <latest-provider-post-delivery-routing-source-chain...> <${N16_PROVIDER_REVIEW}-plan.json> <${N16_PROVIDER_REVIEW}-receipt.json> <${N16_PROVIDER_REVIEW}-reconciliation.json> <${N17_PROVIDER_REVIEW}.json>`,
    `  content-pipeline render-review-${N17_PROVIDER_FOLLOW_UP}-plan <latest-provider-post-delivery-routing-source-chain...> <${N17_PROVIDER_REVIEW}.json> --out <${N17_PROVIDER_REVIEW}-plan.json>`,
    `  content-pipeline validate-review-${N17_PROVIDER_FOLLOW_UP}-plan <latest-provider-post-delivery-routing-source-chain...> <${N17_PROVIDER_REVIEW}.json> <${N17_PROVIDER_REVIEW}-plan.json>`,
    `  content-pipeline render-review-${N17_PROVIDER_FOLLOW_UP}-receipt <latest-provider-post-delivery-routing-source-chain...> <${N17_PROVIDER_REVIEW}.json> <${N17_PROVIDER_REVIEW}-plan.json> [--upsert-status <applied|already_applied|failed>] --out <${N17_PROVIDER_REVIEW}-receipt.json>`,
    `  content-pipeline validate-review-${N17_PROVIDER_FOLLOW_UP}-receipt <latest-provider-post-delivery-routing-source-chain...> <${N17_PROVIDER_REVIEW}.json> <${N17_PROVIDER_REVIEW}-plan.json> <${N17_PROVIDER_REVIEW}-receipt.json>`,
    `  content-pipeline render-review-${N17_PROVIDER_FOLLOW_UP}-reconciliation <latest-provider-post-delivery-routing-source-chain...> <${N17_PROVIDER_REVIEW}.json> <${N17_PROVIDER_REVIEW}-plan.json> <${N17_PROVIDER_REVIEW}-receipt.json> --out <${N17_PROVIDER_REVIEW}-reconciliation.json>`,
    `  content-pipeline validate-review-${N17_PROVIDER_FOLLOW_UP}-reconciliation <latest-provider-post-delivery-routing-source-chain...> <${N17_PROVIDER_REVIEW}.json> <${N17_PROVIDER_REVIEW}-plan.json> <${N17_PROVIDER_REVIEW}-receipt.json> <${N17_PROVIDER_REVIEW}-reconciliation.json>`,
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> [--upsert-status <applied|already_applied|failed>] --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json>",
    "  content-pipeline render-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> --out <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>",
    "  content-pipeline validate-review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation <latest-provider-post-delivery-routing-source-chain...> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-plan.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-receipt.json> <review-provider-execution-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-delivery-follow-up-reconciliation.json>",    "  content-pipeline render-review-inbox-item <review-artifact.json> --out <review-inbox-item.json>",
    "  content-pipeline render-review-inbox-delivery-plan <review-artifact.json> --out <review-inbox-delivery-plan.json>",
    "  content-pipeline validate-review-inbox-delivery-plan <review-inbox-delivery-plan.json>",
    "  content-pipeline render-review-inbox-delivery-receipt <review-inbox-delivery-plan.json> --out <review-inbox-delivery-receipt.json> [--executed-at <iso>] [--upsert-status <status>] [--close-status <status>]",
    "  content-pipeline validate-review-inbox-delivery-receipt <review-inbox-delivery-plan.json> <review-inbox-delivery-receipt.json>",
    "  content-pipeline render-review-inbox-delivery-reconciliation <review-inbox-delivery-plan.json> <review-inbox-delivery-receipt.json> --out <review-inbox-delivery-reconciliation.json>",
    "  content-pipeline validate-review-inbox-delivery-reconciliation <review-inbox-delivery-plan.json> <review-inbox-delivery-receipt.json> <review-inbox-delivery-reconciliation.json>",
    "  content-pipeline render-review-inbox-delivery-follow-up <review-inbox-delivery-plan.json> <review-inbox-delivery-reconciliation.json> --out <review-inbox-delivery-follow-up.json>",
    "  content-pipeline validate-review-inbox-delivery-follow-up <review-inbox-delivery-plan.json> <review-inbox-delivery-reconciliation.json> <review-inbox-delivery-follow-up.json>",
    "  content-pipeline render-review-inbox-delivery-follow-up-plan <review-inbox-delivery-follow-up.json> --out <review-inbox-delivery-follow-up-plan.json>",
    "  content-pipeline validate-review-inbox-delivery-follow-up-plan <review-inbox-delivery-follow-up.json> <review-inbox-delivery-follow-up-plan.json>",
    "  content-pipeline render-review-inbox-delivery-follow-up-receipt <review-inbox-delivery-follow-up.json> <review-inbox-delivery-follow-up-plan.json> [--upsert-status <applied|already_applied|failed>] --out <review-inbox-delivery-follow-up-receipt.json>",
    "  content-pipeline validate-review-inbox-delivery-follow-up-receipt <review-inbox-delivery-follow-up.json> <review-inbox-delivery-follow-up-plan.json> <review-inbox-delivery-follow-up-receipt.json>",
    "  content-pipeline render-review-inbox-delivery-follow-up-reconciliation <review-inbox-delivery-follow-up.json> <review-inbox-delivery-follow-up-plan.json> <review-inbox-delivery-follow-up-receipt.json> --out <review-inbox-delivery-follow-up-reconciliation.json>",
    "  content-pipeline validate-review-inbox-delivery-follow-up-reconciliation <review-inbox-delivery-follow-up.json> <review-inbox-delivery-follow-up-plan.json> <review-inbox-delivery-follow-up-receipt.json> <review-inbox-delivery-follow-up-reconciliation.json>",
    "  content-pipeline approve-review-artifact <review-artifact.json> --reviewer <reviewer-id> --out <approved-review-artifact.json> [--notes <notes>]",
    "  content-pipeline apply-reviewed-patch <unit.yaml> <review-artifact.json> --confirm-reviewed --out <candidate-unit.yaml>"
  ].join("\n");
}

if (process.argv[1] && import.meta.url === pathToFileURL(process.argv[1]).href) {
  const exitCode = await runContentPipelineCli(process.argv.slice(2));
  process.exitCode = exitCode;
}

























